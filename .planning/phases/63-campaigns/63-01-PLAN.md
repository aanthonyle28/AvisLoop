---
phase: 63-campaigns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/qa-v3.1/63-campaigns.md]
autonomous: true

must_haves:
  truths:
    - "Campaign list page displays all user campaigns with correct status badges (active/paused) and touch count per campaign"
    - "Campaign detail page shows touch sequence timeline, enrolled customers list, and analytics counts that match DB values"
    - "Campaign edit sheet opens pre-populated, allows changing touch delay hours, and saves changes verified by navigating to the detail page"
    - "Campaign preset picker (CreateCampaignDialog) shows 3 preset options with plain-English descriptions and the 'Custom Campaign' option"
    - "Pausing the HVAC Follow-up campaign sets all 3 AUDIT_ enrollments to status='frozen' in the database — verified by Supabase MCP query (not UI alone)"
    - "Resuming the paused campaign restores all 3 frozen enrollments to status='active' in the database — verified by Supabase MCP query"
    - "Template preview modal opens for each touch and shows template content (system template fallback or AI-default message)"
    - "Campaign analytics section shows enrollment count, touch performance stats, and values that match a direct DB query"
    - "A job with enrollment_resolution='conflict' displays the correct conflict badge in the dashboard Ready-to-Send queue"
    - "Creating a new campaign from the Standard preset completes end-to-end: preset selected, Continue clicked, campaign appears in campaign list"
    - "Findings document docs/qa-v3.1/63-campaigns.md exists with PASS/FAIL per requirement for CAMP-01 through CAMP-10"
  artifacts:
    - path: "docs/qa-v3.1/63-campaigns.md"
      provides: "Campaigns page QA findings with pass/fail, screenshots, DB verification, and bugs"
      min_lines: 150
  key_links: []
---

<objective>
Execute the Phase 63 QA E2E audit of the Campaigns module: list page, detail page, edit sheet, preset picker, pause/resume freeze, template preview, analytics, and enrollment conflict states.

Purpose: Campaigns are the automation engine of V2. "Set up once, runs forever." This audit verifies all 10 CAMP requirements and confirms the frozen enrollment behavior (the distinguishing V2 feature from Phase 46) works correctly at the DB level.

Output: `docs/qa-v3.1/63-campaigns.md` — findings document with PASS/FAIL per requirement (CAMP-01 through CAMP-10), DB verification evidence, screenshot references, and bugs.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-campaigns/63-RESEARCH.md
@docs/PROJECT_STATE.md

Test account: audit-test@avisloop.com / AuditTest123!
App URL: http://localhost:3000
Output file: docs/qa-v3.1/63-campaigns.md
Business name: "Audit Test HVAC" (id: 6ed94b54-6f35-4ede-8dcb-28f562052042)

Existing data context (from Phase 62):
- 1 campaign: "HVAC Follow-up" (hvac, active, 2 touches — email 24h, SMS 72h)
- 3 AUDIT_ enrollments: AUDIT_Patricia Johnson, AUDIT_Marcus Rodriguez, AUDIT_Sarah Chen — all active in HVAC Follow-up
- 3 system presets: names contain 'conservative', 'standard', 'aggressive' (verify via DB before CAMP-04)
- 16 system templates (is_default=true) for template preview fallback

Known gaps to document (not blockers):
- ENROLLMENT_STATUS_LABELS in lib/constants/campaigns.ts is MISSING 'frozen' — after pause, the enrollment list badge renders empty text where "Frozen" should appear. Screenshot this as a bug finding.
- getCampaignEnrollmentCounts() returns frozen count but the detail page only renders Active/Completed/Stopped stat cards — frozen enrollments disappear from stats UI. Document this.

CAMP-09 conflict state: No existing conflict data. Must create a conflict scenario during the test by adding a second HVAC job for an AUDIT_ customer who already has an active enrollment.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campaign list, detail, edit, preset picker, template preview, analytics (CAMP-01 through CAMP-04, CAMP-07, CAMP-08)</name>
  <files>docs/qa-v3.1/63-campaigns.md</files>
  <action>
This task tests CAMP-01, CAMP-02, CAMP-03, CAMP-04, CAMP-07, and CAMP-08. These are the "read and modify" requirements that do not involve pause/resume or conflict states.

**IMPORTANT:** The dev server must be running (`pnpm dev`). Before starting Playwright steps, verify with `browser_navigate` to `http://localhost:3000`. If the server is not running, start it in the background first.

**IMPORTANT:** Before starting UI tests, run prerequisite DB queries to confirm Phase 62 data exists and presets are correctly named.

**Step-by-step walkthrough:**

**Part A: Prerequisite DB verification (before any Playwright)**

1. **Verify AUDIT_ enrollments exist (Phase 62 dependency):**
   Run via `mcp__supabase__execute_sql`:
   ```sql
   SELECT ce.status, c.name as customer_name, ce.touch_1_status, ce.touch_1_scheduled_at
   FROM campaign_enrollments ce
   JOIN customers c ON ce.customer_id = c.id
   WHERE c.name LIKE 'AUDIT_%'
   ORDER BY c.name;
   ```
   Expected: 3 rows (AUDIT_Marcus Rodriguez, AUDIT_Patricia Johnson, AUDIT_Sarah Chen), all with status='active'.
   **If 0 rows:** STOP. Phase 62 prerequisites are not met. Document this and skip CAMP-02, CAMP-05, CAMP-06, CAMP-08.

2. **Get the HVAC Follow-up campaign ID:**
   ```sql
   SELECT id, name, service_type, status
   FROM campaigns
   WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042'
     AND is_preset = false
     AND service_type = 'hvac';
   ```
   Record the campaign ID — it is needed for all subsequent queries. Store as `<hvac-campaign-id>`.

3. **Verify DB preset names for CAMP-04:**
   ```sql
   SELECT id, name, is_preset
   FROM campaigns
   WHERE is_preset = true
   ORDER BY name;
   ```
   Each name must contain 'conservative', 'standard', or 'aggressive' (case-insensitive). Record the names. If none match, CAMP-04 will show no preset descriptions — document as a bug.

4. **Verify system templates exist for CAMP-07:**
   ```sql
   SELECT channel, COUNT(*) as count
   FROM message_templates
   WHERE is_default = true
   GROUP BY channel;
   ```
   Expected: email >= 8, sms >= 8. If 0, template preview will show "No template preview available" — still valid but document it.

**Part B: Login and navigate to Campaigns (CAMP-01)**

5. `browser_navigate` to `http://localhost:3000/auth/login`
6. `browser_snapshot` — Get refs for login form
7. `browser_fill_form` — Email: "audit-test@avisloop.com", Password: "AuditTest123!"
8. `browser_click` — Click "Login" button (or "Sign in")
9. `browser_wait_for` — Wait for dashboard content (max 10 seconds)
10. `browser_navigate` to `http://localhost:3000/campaigns`
11. `browser_wait_for` — Wait for campaigns page to load (look for "Campaigns" heading or campaign card content)
12. `browser_snapshot` — Record full page structure:
    - Number of campaign cards visible
    - Each card: name, service type badge, status (active/paused), touch count
    - "New Campaign" button location
    - Any other UI elements
13. `browser_take_screenshot` — filename: `qa-63-campaigns-list.png`

14. **CAMP-01 verification:** From the snapshot, confirm:
    - "HVAC Follow-up" card is visible
    - Status badge shows "Active" (or a green/active indicator)
    - Touch count shows "2 touches" (or "2")
    - Service type badge shows "HVAC"
    Record all campaign cards and their details. Note any other campaigns (from previous testing).

15. Record CAMP-01: PASS if campaign list displays campaigns with correct status badges and touch counts. FAIL if cards are missing, status is wrong, or touch count is incorrect.

**Part C: Campaign detail page (CAMP-02)**

16. **Navigate to HVAC Follow-up detail:** Click the HVAC Follow-up campaign card (click the name text, NOT the Switch or action buttons — clicking the card body navigates to the detail page).
17. `browser_wait_for` — Wait for detail page to load (look for campaign name heading or touch sequence)
18. `browser_snapshot` — Record the detail page structure:
    - Campaign name heading
    - Status toggle (Switch showing Active/Paused)
    - Touch sequence section (timeline/list of touches)
      - Touch 1: channel (email), delay (24h)
      - Touch 2: channel (sms), delay (72h)
    - Enrollment count cards (Active, Completed, Stopped)
    - Enrolled customers list (pagination if available)
    - Analytics/stats section (if enrollments > 0)
    - Action buttons (Edit, Duplicate, Delete)
19. `browser_take_screenshot` — filename: `qa-63-campaign-detail.png`

20. **Verify enrollment count cards:**
    - "Active" count should show 3 (Patricia, Marcus, Sarah)
    - "Completed" count should show 0
    - "Stopped" count should show 0
    Record exact values.

21. **Verify enrolled customers list:**
    - `browser_snapshot` — Look for the enrollment list section
    - Should show AUDIT_Patricia Johnson, AUDIT_Marcus Rodriguez, AUDIT_Sarah Chen
    - Each enrollment should show status badge ("Active"), touch progress, scheduled time
    - Record exact content
22. `browser_take_screenshot` — filename: `qa-63-campaign-enrollments.png`

23. **Verify touch sequence display:**
    - Touch 1: Email, 24 hours delay
    - Touch 2: SMS, 72 hours delay
    - Eye/preview buttons should be visible per touch (tested in Part E)
    Record exact display.

24. Record CAMP-02: PASS if detail page shows touch sequence, enrollment list with 3 AUDIT_ customers, and enrollment count cards. FAIL if any section is missing or counts are wrong.

**Part D: Campaign edit sheet (CAMP-03)**

25. **Open edit sheet:** From the detail page, click the "Edit" button (pencil icon or "Edit" text).
    - `browser_wait_for` — Wait for the edit sheet/panel to slide in
    - `browser_snapshot` — Record the edit form fields:
      - Campaign name
      - Service type (should be HVAC)
      - Touch sequence editor (2 touches with channel, delay hours, template selectors)
      - AI personalization toggle (if visible)
      - Save/Cancel buttons
26. `browser_take_screenshot` — filename: `qa-63-campaign-edit-sheet.png`

27. **Verify pre-populated values:**
    - Name: "HVAC Follow-up"
    - Service type: "hvac"
    - Touch 1: email, 24 hours
    - Touch 2: sms, 72 hours
    Record all values.

28. **Make a change — modify Touch 1 delay:**
    - Find the delay hours input for Touch 1 (should show "24")
    - Clear the field and type "48" (change from 24 to 48 hours)
    - `browser_snapshot` — Verify the value shows 48

29. **Save changes:**
    - Click "Save Changes" (or "Update Campaign" or similar save button)
    - `browser_wait_for` — Wait for success toast
    - Wait for the sheet to close completely (give it 2 seconds)

30. **Verify the change persisted:**
    After the sheet closes, the detail page should still be visible. Check if the touch sequence now shows 48 hours for Touch 1.
    - `browser_snapshot` — Verify Touch 1 delay updated to 48 hours
    - `browser_take_screenshot` — filename: `qa-63-campaign-edit-saved.png`

31. **DB verification — edit saved:**
    ```sql
    SELECT ct.touch_number, ct.channel, ct.delay_hours
    FROM campaign_touches ct
    WHERE ct.campaign_id = '<hvac-campaign-id>'
    ORDER BY ct.touch_number;
    ```
    Verify touch 1 delay_hours = 48 (changed from 24). Touch 2 should still be 72.

32. **Revert the change (restore to 24 hours):**
    - Open the edit sheet again
    - Change Touch 1 delay back from 48 to 24
    - Save
    - Wait for success toast + sheet close
    This ensures the campaign is in its original state for CAMP-05/06 testing.

33. **DB verification — revert confirmed:**
    ```sql
    SELECT ct.touch_number, ct.channel, ct.delay_hours
    FROM campaign_touches ct
    WHERE ct.campaign_id = '<hvac-campaign-id>'
    ORDER BY ct.touch_number;
    ```
    Verify touch 1 delay_hours = 24 (reverted).

34. Record CAMP-03: PASS if edit sheet opened pre-populated, saved the change (48h verified in DB), and reverted successfully. FAIL if edit didn't save or pre-population was wrong.

**Part E: Template preview modal (CAMP-07)**

35. **Navigate back to detail page** if not already there. Should still be on `/campaigns/<hvac-campaign-id>`.
    - `browser_snapshot` — Find the touch sequence display with Eye/preview buttons

36. **Open template preview for Touch 1 (email):**
    - Click the Eye/preview button for Touch 1
    - `browser_wait_for` — Wait for the template preview modal/dialog to appear
    - `browser_snapshot` — Record the modal content:
      - Is it showing template content (subject + body for email)?
      - Or is it showing "No template preview available. This touch uses an AI-generated default."?
      - If showing template content: record the subject line and first few lines of body
    - `browser_take_screenshot` — filename: `qa-63-template-preview-email.png`

37. **Close the preview modal** (click X or click outside)

38. **Open template preview for Touch 2 (SMS):**
    - Click the Eye/preview button for Touch 2
    - `browser_wait_for` — Wait for modal
    - `browser_snapshot` — Record the modal content:
      - SMS templates show as a chat bubble (no subject)
      - Should show system SMS template content or AI-default message
    - `browser_take_screenshot` — filename: `qa-63-template-preview-sms.png`

39. **Close the preview modal**

40. Record CAMP-07: PASS if template preview modal opens for both touches and shows content (either system template fallback or AI-default message). FAIL if modal doesn't open or shows an error.

**Part F: Campaign analytics (CAMP-08)**

41. **Still on the campaign detail page.** Scroll down or look for the analytics/stats section.
    - `browser_snapshot` — Find the CampaignStats section:
      - Touch Performance bar charts (touch 1 stats, touch 2 stats)
      - Stop reasons list (if any stopped enrollments)
      - Total enrollments count
    - If the analytics section is NOT visible, check: `totalEnrollments` may be 0 despite enrollments existing. Document this as a bug.

42. `browser_take_screenshot` — filename: `qa-63-campaign-analytics.png`

43. **Record UI analytics values:**
    - Total enrollments: [N]
    - Touch 1: sent [N], pending [N], skipped [N], failed [N]
    - Touch 2: sent [N], pending [N], skipped [N], failed [N]
    - Stop reasons: [list]

44. **DB verification — compare analytics:**
    ```sql
    SELECT
      COUNT(*) as total_enrollments,
      SUM(CASE WHEN touch_1_status = 'sent' THEN 1 ELSE 0 END) as touch_1_sent,
      SUM(CASE WHEN touch_1_status = 'pending' THEN 1 ELSE 0 END) as touch_1_pending,
      SUM(CASE WHEN touch_1_status IS NULL THEN 1 ELSE 0 END) as touch_1_null,
      SUM(CASE WHEN touch_2_status = 'sent' THEN 1 ELSE 0 END) as touch_2_sent,
      SUM(CASE WHEN touch_2_status = 'pending' THEN 1 ELSE 0 END) as touch_2_pending,
      SUM(CASE WHEN touch_2_status IS NULL THEN 1 ELSE 0 END) as touch_2_null
    FROM campaign_enrollments
    WHERE campaign_id = '<hvac-campaign-id>';
    ```
    Compare the DB values against the UI values recorded in step 43. Note any discrepancies.

45. Record CAMP-08: PASS if analytics section is visible and values match (or reasonably approximate) the DB query. FAIL if analytics section is missing (despite enrollments existing) or values are significantly wrong.

**Part G: Campaign preset picker (CAMP-04)**

46. `browser_navigate` to `http://localhost:3000/campaigns`
47. `browser_wait_for` — Wait for campaigns list page
48. `browser_snapshot` — Find the "New Campaign" button

49. **Open CreateCampaignDialog:**
    - Click "New Campaign" button
    - `browser_wait_for` — Wait for the dialog/modal to appear
    - `browser_snapshot` — Record the dialog content:
      - Number of preset radio cards visible
      - Each card: name, description, recommended_for text
      - "Custom Campaign" option at the bottom
    - `browser_take_screenshot` — filename: `qa-63-preset-picker.png`

50. **Verify 3 presets with descriptions:**
    Expected (from CAMPAIGN_PRESETS constant):
    1. "Gentle Follow-Up" — matched from DB preset with 'conservative' in name
       - Description: mentions fewer touches, respectful timing
       - Recommended for: premium/relationship-based services
    2. "Standard Follow-Up" — matched from DB preset with 'standard' in name
       - Description: balanced approach
       - Recommended for: most service businesses
    3. "Aggressive Follow-Up" — matched from DB preset with 'aggressive' in name
       - Description: more frequent touches, higher volume
       - Recommended for: high-volume/transactional services

    If descriptions are missing on any card, cross-reference with the DB preset names from step 3. If the name doesn't contain the expected keyword, document the mismatch.

51. Record CAMP-04: PASS if all 3 presets show with descriptions and the Custom Campaign option is visible. FAIL if fewer than 3 presets or descriptions are missing.

**Part H: Create new campaign from preset (CAMP-10)**

52. **From the still-open CreateCampaignDialog:**
    - Select the "Standard Follow-Up" preset (click its radio card)
    - `browser_snapshot` — Verify the card is selected (highlighted/checked)

53. Click "Continue" button
    - `browser_wait_for` — Wait for navigation to the new campaign detail page (the dialog creates a duplicate and redirects to `/campaigns/<new-id>?edit=true`)
    - `browser_snapshot` — Verify:
      - We are now on a campaign detail page for the new campaign
      - The edit sheet should be auto-opened (due to `?edit=true` query param)
      - The campaign name should contain "Standard" or "Follow-Up" (duplicated from preset)
    - `browser_take_screenshot` — filename: `qa-63-new-campaign-created.png`

54. **Close the edit sheet** (click Cancel or X if auto-opened) to see the detail page.

55. **Navigate back to campaign list to verify the new campaign appears:**
    - `browser_navigate` to `http://localhost:3000/campaigns`
    - `browser_wait_for` — Wait for campaigns list
    - `browser_snapshot` — Verify the new campaign card is visible alongside "HVAC Follow-up"
    - `browser_take_screenshot` — filename: `qa-63-campaigns-list-with-new.png`

56. **DB verification — new campaign exists:**
    ```sql
    SELECT id, name, service_type, status, is_preset,
           (SELECT COUNT(*) FROM campaign_touches WHERE campaign_id = campaigns.id) as touch_count
    FROM campaigns
    WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042'
      AND is_preset = false
    ORDER BY created_at DESC
    LIMIT 3;
    ```
    The most recently created campaign (is_preset=false) should be the duplicated Standard preset.

57. Record CAMP-10: PASS if a new campaign was created from the Standard preset, appeared in the list, and exists in the DB. FAIL if creation failed or campaign didn't appear.

**Part I: Initialize findings document**

58. Create `docs/qa-v3.1/63-campaigns.md` with findings from all tests in this task:

```markdown
# Phase 63: Campaigns — QA Findings

**Tested:** [current date]
**Tester:** Claude (Playwright MCP + Supabase MCP)
**App URL:** http://localhost:3000
**Test account:** audit-test@avisloop.com
**Business:** Audit Test HVAC (6ed94b54-6f35-4ede-8dcb-28f562052042)

---

## Summary

| Requirement | Status | Notes |
|-------------|--------|-------|
| CAMP-01: Campaign list + status badges | [PASS/FAIL] | [notes] |
| CAMP-02: Campaign detail (touches, enrollments, analytics) | [PASS/FAIL] | [notes] |
| CAMP-03: Campaign edit sheet | [PASS/FAIL] | [notes] |
| CAMP-04: Preset picker (3 options) | [PASS/FAIL] | [notes] |
| CAMP-05: Pause freezes enrollments (DB verified) | [pending] | |
| CAMP-06: Resume restores frozen enrollments (DB verified) | [pending] | |
| CAMP-07: Template preview modal | [PASS/FAIL] | [notes] |
| CAMP-08: Campaign analytics accuracy (DB verified) | [PASS/FAIL] | [notes] |
| CAMP-09: Enrollment conflict badges | [pending] | |
| CAMP-10: Create campaign from preset | [PASS/FAIL] | [notes] |

---

## Prerequisite Verification

### AUDIT_ enrollments (Phase 62 dependency)
```
[paste DB query result — 3 AUDIT_ enrollments with status=active]
```
- Enrollments found: [N] (expected: 3)
- All active: [YES/NO]

### HVAC Follow-up campaign ID
- Campaign ID: [id]
- Status: [active]
- Touches: [2]

### System presets for CAMP-04
```
[paste DB query result — 3 presets with names]
```
- Name matching verified: [YES/NO — each contains conservative/standard/aggressive]

### System templates for CAMP-07
```
[paste DB query result — template counts by channel]
```
- Email templates: [N]
- SMS templates: [N]

---

## CAMP-01: Campaign list + status badges

**Status:** [PASS/FAIL]

### Campaigns observed
| Campaign | Service Type | Status Badge | Touch Count |
|----------|-------------|--------------|-------------|
| [name] | [type] | [Active/Paused] | [N] |

### Evidence
- `qa-63-campaigns-list.png`

### Bugs found
- [none or list]

---

## CAMP-02: Campaign detail page

**Status:** [PASS/FAIL]

### Touch sequence
| Touch | Channel | Delay | Template |
|-------|---------|-------|----------|
| 1 | [email/sms] | [N]h | [template name or "system default"] |
| 2 | [email/sms] | [N]h | [template name or "system default"] |

### Enrollment count cards
| Card | Expected | Actual |
|------|----------|--------|
| Active | 3 | [actual] |
| Completed | 0 | [actual] |
| Stopped | 0 | [actual] |

### Enrolled customers list
| Customer | Status Badge | Touch Progress |
|----------|-------------|----------------|
| [name] | [Active] | [details] |
| [name] | [Active] | [details] |
| [name] | [Active] | [details] |

### Evidence
- `qa-63-campaign-detail.png`
- `qa-63-campaign-enrollments.png`

### Bugs found
- [none or list]

---

## CAMP-03: Campaign edit sheet

**Status:** [PASS/FAIL]

### Pre-populated values
| Field | Expected | Actual |
|-------|----------|--------|
| Name | HVAC Follow-up | [actual] |
| Service Type | hvac | [actual] |
| Touch 1 channel | email | [actual] |
| Touch 1 delay | 24h | [actual] |
| Touch 2 channel | sms | [actual] |
| Touch 2 delay | 72h | [actual] |

### Edit test
- Changed Touch 1 delay to 48h
- Save submitted: [success/error]
- DB verified (delay_hours=48): [YES/NO]
- Reverted to 24h: [YES/NO]
- DB verified (delay_hours=24): [YES/NO]

### Evidence
- `qa-63-campaign-edit-sheet.png`
- `qa-63-campaign-edit-saved.png`

### Bugs found
- [none or list]

---

## CAMP-04: Preset picker

**Status:** [PASS/FAIL]

### Presets observed
| Preset | Description visible? | Recommended for |
|--------|---------------------|-----------------|
| Gentle Follow-Up | [YES/NO] | [text] |
| Standard Follow-Up | [YES/NO] | [text] |
| Aggressive Follow-Up | [YES/NO] | [text] |

### Custom Campaign option
- Visible: [YES/NO]

### DB preset name matching
- DB names: [list]
- All match CAMPAIGN_PRESETS keywords: [YES/NO]

### Evidence
- `qa-63-preset-picker.png`

### Bugs found
- [none or list]

---

## CAMP-07: Template preview modal

**Status:** [PASS/FAIL]

### Touch 1 preview (email)
- Modal opened: [YES/NO]
- Content type: [system template / AI-default message / error]
- Subject line: [text or N/A]
- Body excerpt: [first line or "No template preview available..."]

### Touch 2 preview (SMS)
- Modal opened: [YES/NO]
- Content type: [system template / AI-default message / error]
- Message excerpt: [first line or "No template preview available..."]

### Evidence
- `qa-63-template-preview-email.png`
- `qa-63-template-preview-sms.png`

### Bugs found
- [none or list]

---

## CAMP-08: Campaign analytics accuracy

**Status:** [PASS/FAIL]

### UI values
- Total enrollments: [N]
- Touch 1: sent=[N], pending=[N], skipped=[N], failed=[N]
- Touch 2: sent=[N], pending=[N], skipped=[N], failed=[N]

### DB values
```
[paste DB query result]
```

### Comparison
| Metric | UI | DB | Match? |
|--------|----|----|--------|
| Total enrollments | [N] | [N] | [YES/NO] |
| Touch 1 sent | [N] | [N] | [YES/NO] |
| Touch 1 pending | [N] | [N] | [YES/NO] |
| Touch 2 sent | [N] | [N] | [YES/NO] |
| Touch 2 pending | [N] | [N] | [YES/NO] |

### Evidence
- `qa-63-campaign-analytics.png`

### Bugs found
- [none or list]

---

## CAMP-10: Create campaign from preset

**Status:** [PASS/FAIL]

### Flow
1. Clicked "New Campaign": [success/error]
2. Selected preset: Standard Follow-Up
3. Clicked "Continue": [success/error]
4. Redirected to new campaign detail: [YES/NO]
5. Edit sheet auto-opened: [YES/NO]
6. New campaign name: [text]

### DB verification
```
[paste DB query result — new campaign row]
```
- Campaign exists in DB: [YES/NO]
- is_preset: false
- touch_count: [N]

### Campaign list updated
- New campaign visible in list: [YES/NO]

### Evidence
- `qa-63-new-campaign-created.png`
- `qa-63-campaigns-list-with-new.png`

### Bugs found
- [none or list]

---
```

Fill in ALL [placeholder] values with actual observed results. Leave CAMP-05, CAMP-06, CAMP-09 sections as stubs (filled by Task 2).
  </action>
  <verify>
1. File `docs/qa-v3.1/63-campaigns.md` exists with CAMP-01, CAMP-02, CAMP-03, CAMP-04, CAMP-07, CAMP-08, CAMP-10 sections filled in
2. Prerequisite DB queries confirmed: 3 AUDIT_ enrollments exist, HVAC campaign ID recorded, preset names verified, system templates counted
3. Screenshots captured: qa-63-campaigns-list.png, qa-63-campaign-detail.png, qa-63-campaign-enrollments.png, qa-63-campaign-edit-sheet.png, qa-63-campaign-edit-saved.png, qa-63-template-preview-email.png, qa-63-template-preview-sms.png, qa-63-campaign-analytics.png, qa-63-preset-picker.png, qa-63-new-campaign-created.png, qa-63-campaigns-list-with-new.png
4. CAMP-03 edit was saved (DB verified touch_1 delay_hours=48) and then reverted (DB verified back to 24)
5. CAMP-10 new campaign exists in DB (is_preset=false, created from Standard preset)
6. CAMP-08 analytics values compared between UI and DB query
  </verify>
  <done>
- CAMP-01 marked PASS or FAIL with campaign card details documented
- CAMP-02 marked PASS or FAIL with touch sequence, enrollment counts, and enrolled customer list
- CAMP-03 marked PASS or FAIL with pre-populated values, edit save (DB verified), and revert
- CAMP-04 marked PASS or FAIL with 3 preset descriptions and Custom Campaign option
- CAMP-07 marked PASS or FAIL with template preview content for both email and SMS touches
- CAMP-08 marked PASS or FAIL with UI vs DB analytics comparison
- CAMP-10 marked PASS or FAIL with new campaign creation end-to-end
- Campaign edit reverted (Touch 1 back to 24h) so campaign is in original state for Task 2
- Findings document initialized with all Task 1 sections filled
  </done>
</task>

<task type="auto">
  <name>Task 2: Pause/resume freeze, conflict states, responsive, dark mode, finalize findings (CAMP-05, CAMP-06, CAMP-09)</name>
  <files>docs/qa-v3.1/63-campaigns.md</files>
  <action>
This task tests CAMP-05, CAMP-06, and CAMP-09. These are the "state transition" requirements that modify enrollment statuses and require DB verification. It also checks responsive layout and dark mode. Must run AFTER Task 1 because the campaign must be in its original "active" state with 3 active enrollments.

**IMPORTANT:** If still logged in from Task 1, proceed directly. If session expired, re-login with audit-test@avisloop.com / AuditTest123!

**IMPORTANT:** CAMP-05 and CAMP-06 verification MUST use Supabase MCP queries. The UI toast alone is NOT sufficient. The frozen label gap means the UI will show empty badges — screenshot this as a known bug but do NOT fail the requirement if the DB shows the correct status.

**Step-by-step walkthrough:**

**Part A: CAMP-05 — Pause campaign (freeze enrollments)**

1. `browser_navigate` to `http://localhost:3000/campaigns`
2. `browser_wait_for` — Wait for campaigns list to load
3. `browser_snapshot` — Find the HVAC Follow-up campaign card and its Switch (should show "Active")

4. **Pre-pause DB check — confirm all 3 enrollments are active:**
   ```sql
   SELECT ce.status, COUNT(*) as count
   FROM campaign_enrollments ce
   WHERE ce.campaign_id = '<hvac-campaign-id>'
   GROUP BY ce.status;
   ```
   Expected: active: 3 (may have more from non-AUDIT enrollments — record exact breakdown).

5. **Pause the campaign:** Click the Switch toggle on the HVAC Follow-up card.
   - Use `browser_snapshot` first to find the exact Switch ref. The Switch has `role="switch"`. If multiple campaigns exist, locate the switch within the HVAC Follow-up card specifically.
   - `browser_click` — Click the Switch
   - `browser_wait_for` — Wait for status change (toast message or visual update, max 5 seconds)
   - `browser_snapshot` — Verify:
     - The Switch now shows "Paused" (or toggled off)
     - The campaign card status badge changes to "Paused"
     - A toast message appears ("Campaign paused" or similar)
6. `browser_take_screenshot` — filename: `qa-63-campaign-paused.png`

7. **CAMP-05 DB verification — frozen enrollments:**
   ```sql
   SELECT ce.id, ce.status, c.name as customer_name
   FROM campaign_enrollments ce
   JOIN customers c ON ce.customer_id = c.id
   WHERE ce.campaign_id = '<hvac-campaign-id>'
   ORDER BY c.name;
   ```
   **Critical check:** All enrollments that were 'active' must now be 'frozen' (NOT 'stopped').
   Record exact results.

8. **Check the frozen label gap on the detail page:**
   - `browser_navigate` to the campaign detail page: `http://localhost:3000/campaigns/<hvac-campaign-id>`
   - `browser_wait_for` — Wait for detail page
   - `browser_snapshot` — Look at the enrollment list:
     - Each AUDIT_ enrollment should have a status badge
     - The badge text should be empty/undefined (because ENROLLMENT_STATUS_LABELS['frozen'] is undefined)
     - The enrollment count stat cards: Active should show 0, Stopped should show 0 (frozen count not displayed)
   - `browser_take_screenshot` — filename: `qa-63-frozen-label-gap.png`
   - Document this as a known bug: "ENROLLMENT_STATUS_LABELS missing 'frozen' key — enrollment list shows empty badge text for frozen enrollments"

9. Record CAMP-05: PASS if DB query shows all enrollments with status='frozen' (not 'stopped'). The UI label gap is a known bug but does NOT fail CAMP-05 because the requirement says "verified by direct DB query."

**Part B: CAMP-06 — Resume campaign (restore enrollments)**

10. **Resume the campaign:** Navigate back to campaigns list OR use the Switch on the detail page.
    - `browser_snapshot` — Find the Switch (should show paused state)
    - `browser_click` — Click the Switch to resume
    - `browser_wait_for` — Wait for status change (toast, max 5 seconds)
    - `browser_snapshot` — Verify:
      - Switch shows "Active" again
      - Toast message ("Campaign resumed" or similar)
11. `browser_take_screenshot` — filename: `qa-63-campaign-resumed.png`

12. **CAMP-06 DB verification — active enrollments restored:**
    ```sql
    SELECT ce.id, ce.status, c.name as customer_name,
           ce.touch_1_scheduled_at,
           ce.touch_1_status
    FROM campaign_enrollments ce
    JOIN customers c ON ce.customer_id = c.id
    WHERE ce.campaign_id = '<hvac-campaign-id>'
    ORDER BY c.name;
    ```
    **Critical check:** All enrollments that were 'frozen' must now be 'active' again.
    Also check: if any `touch_1_scheduled_at` was in the past, it should have been bumped to >= NOW().
    Record exact results.

13. **Verify enrollment list labels restored:**
    - If still on the detail page, `browser_snapshot` — check the enrollment list badges
    - Status badges should now show "Active" text (ENROLLMENT_STATUS_LABELS['active'] exists)
    - `browser_take_screenshot` — filename: `qa-63-enrollments-restored.png`

14. Record CAMP-06: PASS if DB query shows all enrollments with status='active' (restored from frozen). FAIL if any enrollment is still 'frozen' or changed to 'stopped'.

**Part C: CAMP-09 — Enrollment conflict states**

15. **Create a conflict scenario:** To test CAMP-09, we need a job with `enrollment_resolution='conflict'`. This requires completing a second HVAC job for a customer who already has an active enrollment.

16. `browser_navigate` to `http://localhost:3000/jobs`
17. `browser_wait_for` — Wait for jobs table
18. `browser_snapshot` — Verify the jobs page loaded

19. **Add a second HVAC job for AUDIT_Patricia Johnson (she already has an active enrollment):**
    - Click "Add Job" button
    - `browser_wait_for` — Wait for Add Job sheet
    - `browser_snapshot` — Get form refs

20. **Search for existing customer:**
    - Type "AUDIT_Patricia" in the customer field
    - `browser_snapshot` — Look for autocomplete dropdown showing AUDIT_Patricia Johnson
    - Click the AUDIT_Patricia Johnson suggestion

21. **Fill job details:**
    - Service type: HVAC
    - Status: Completed
    - Notes: "AUDIT Phase 63 - conflict test job"

22. **Submit the form:**
    - Click submit/save button
    - `browser_wait_for` — Wait for response (toast or sheet close)
    - The system should detect an enrollment conflict because Patricia already has an active HVAC enrollment
    - `browser_snapshot` — Record what happens:
      - Did the job create with enrollment_resolution='conflict'?
      - Did a conflict dialog/alert appear?
      - Did the toast mention a conflict?
    - `browser_take_screenshot` — filename: `qa-63-conflict-created.png`

23. **DB verification — conflict state:**
    ```sql
    SELECT j.id, j.status, j.enrollment_resolution, j.conflict_detected_at,
           c.name as customer_name
    FROM jobs j
    JOIN customers c ON j.customer_id = c.id
    WHERE c.name = 'AUDIT_Patricia Johnson'
    ORDER BY j.created_at DESC
    LIMIT 2;
    ```
    The most recent job should have `enrollment_resolution = 'conflict'` (or 'queue_after' or 'skipped' if auto-resolved).
    Record exact values.

24. **Check the dashboard Ready-to-Send queue for the conflict badge:**
    - `browser_navigate` to `http://localhost:3000/dashboard`
    - `browser_wait_for` — Wait for dashboard to load
    - `browser_snapshot` — Look in the Ready-to-Send queue section for the conflict job:
      - Should show AUDIT_Patricia Johnson's second job with a conflict badge/indicator
      - Badge could say "Conflict" with Replace/Skip/Queue action buttons
      - Or if auto-resolved, could show "Queued" or "Skipped" badge
    - `browser_take_screenshot` — filename: `qa-63-conflict-badge-dashboard.png`

25. If the conflict job does NOT appear in the Ready-to-Send queue (because it was auto-resolved or the queue doesn't show conflicts), check the jobs table:
    - `browser_navigate` to `http://localhost:3000/jobs`
    - `browser_snapshot` — Look for the conflict job's enrollment resolution badge in the Campaign column
    - `browser_take_screenshot` — filename: `qa-63-conflict-badge-jobs.png`

26. Record CAMP-09: PASS if the conflict was detected (DB shows enrollment_resolution IS NOT NULL for the second Patricia job) and a conflict-related badge is visible in either the dashboard queue or jobs table. FAIL if no conflict was detected or no badge is visible.

**Part D: Responsive layout checks**

27. `browser_navigate` to `http://localhost:3000/campaigns`
28. `browser_wait_for` — Wait for campaigns list

29. **Tablet viewport:** `browser_resize` to 768x1024
30. `browser_snapshot` — Verify:
    - Campaign cards render without overflow
    - Switch and action buttons accessible
    - "New Campaign" button visible
31. `browser_take_screenshot` — filename: `qa-63-campaigns-tablet.png`

32. **Mobile viewport:** `browser_resize` to 390x844
33. `browser_snapshot` — Verify:
    - Campaign cards render properly (may stack vertically)
    - Content doesn't overflow horizontally
    - Navigation accessible (bottom nav)
34. `browser_take_screenshot` — filename: `qa-63-campaigns-mobile.png`

35. `browser_resize` to 1440x900 (reset to desktop)

**Part E: Dark mode check**

36. Look for theme toggle in the sidebar or page header.
    - If accessible: switch to dark mode
    - `browser_navigate` to `http://localhost:3000/campaigns`
    - `browser_snapshot` — Verify campaign cards render without:
      - Muddy colors
      - Incorrect contrast
      - Visual artifacts
      - Unreadable text
    - `browser_take_screenshot` — filename: `qa-63-campaigns-dark-mode.png`
    - Switch back to light mode
37. If theme toggle is not accessible, note: "Dark mode toggle not tested from campaigns page."

**Part F: Cleanup — delete the test campaign from CAMP-10**

38. **Optional cleanup:** If the campaign created in CAMP-10 (Task 1) should be cleaned up:
    - Navigate to campaigns list
    - Find the new campaign (the one created from Standard preset)
    - Click its delete button (or navigate to detail and delete)
    - Confirm deletion
    - This is optional — it doesn't affect other tests. Skip if the delete flow is complex.

**Part G: Finalize findings document**

39. Update `docs/qa-v3.1/63-campaigns.md` — append the CAMP-05, CAMP-06, CAMP-09 sections and finalize:

```markdown

## CAMP-05: Pause freezes enrollments (DB verified)

**Status:** [PASS/FAIL]

### Before pause
```
[paste DB query result — enrollment status counts before pause]
```

### After pause — UI
- Switch toggled to "Paused": [YES/NO]
- Toast message: [text]
- Campaign status badge changed: [YES/NO]

### After pause — DB verification
```
[paste DB query result — all enrollments should show status='frozen']
```
- All enrollments frozen: [YES/NO]
- Any enrollments stopped (wrong): [YES/NO]

### Frozen label gap (known bug)
- Enrollment list badge text for frozen enrollments: [empty/undefined — as expected]
- Enrollment count stat cards: Active=[N], Completed=[N], Stopped=[N] (frozen count NOT displayed)
- **Bug:** ENROLLMENT_STATUS_LABELS in lib/constants/campaigns.ts missing 'frozen' key — renders empty badge
- **Bug:** Campaign detail stat cards don't display frozen count — enrollments disappear from stats after pause

### Evidence
- `qa-63-campaign-paused.png`
- `qa-63-frozen-label-gap.png`

### Bugs found
- ENROLLMENT_STATUS_LABELS missing 'frozen' — empty badge text on frozen enrollments
- Frozen count not shown in stat cards — Active drops to 0 after pause

---

## CAMP-06: Resume restores frozen enrollments (DB verified)

**Status:** [PASS/FAIL]

### After resume — UI
- Switch toggled back to "Active": [YES/NO]
- Toast message: [text]

### After resume — DB verification
```
[paste DB query result — all enrollments should show status='active']
```
- All enrollments restored to active: [YES/NO]
- Any touch times bumped (stale scheduled times updated): [YES/NO/N/A]

### Enrollment list labels restored
- Badge text shows "Active" again: [YES/NO]

### Evidence
- `qa-63-campaign-resumed.png`
- `qa-63-enrollments-restored.png`

### Bugs found
- [none or list]

---

## CAMP-09: Enrollment conflict badges

**Status:** [PASS/FAIL]

### Conflict scenario
- Created second HVAC job for AUDIT_Patricia Johnson
- Job submitted: [success/error]
- Conflict detected: [YES/NO]

### DB verification
```
[paste DB query result — two Patricia jobs, newest with enrollment_resolution]
```
- Latest job enrollment_resolution: [conflict/queue_after/skipped/NULL]
- conflict_detected_at: [timestamp or NULL]

### Dashboard Ready-to-Send queue
- Conflict job visible: [YES/NO]
- Badge/indicator shown: [text or description]
- Action buttons (Replace/Skip/Queue): [visible/not visible]

### Jobs table
- Conflict badge visible in Campaign column: [YES/NO]
- Badge text: [text]

### Evidence
- `qa-63-conflict-created.png`
- `qa-63-conflict-badge-dashboard.png`
- `qa-63-conflict-badge-jobs.png` (if applicable)

### Bugs found
- [none or list]

---

## Responsive & Dark Mode

### Viewport checks
| Viewport | Status | Notes |
|----------|--------|-------|
| Desktop (1440x900) | [PASS/FAIL] | [notes] |
| Tablet (768x1024) | [PASS/FAIL] | [notes] |
| Mobile (390x844) | [PASS/FAIL] | [notes] |

### Dark mode
- [PASS/FAIL/not tested] | [notes]

### Evidence
- `qa-63-campaigns-tablet.png`
- `qa-63-campaigns-mobile.png`
- `qa-63-campaigns-dark-mode.png` (if tested)

---

## Overall Assessment

[Summary paragraph covering:
- How many requirements passed (N/10)
- Known bugs found (frozen label gap, frozen stat cards)
- DB verification results (CAMP-05, CAMP-06, CAMP-08 all DB-confirmed)
- Conflict state testing outcome (CAMP-09)
- Any limitations or caveats
- Confirmation that campaign module is functional with identified UI gaps
- Status of the new campaign created in CAMP-10 (kept or cleaned up)]

---

## Bugs Summary

| Bug ID | Severity | Description | Location | Recommendation |
|--------|----------|-------------|----------|----------------|
| CAMP-BUG-01 | Medium | ENROLLMENT_STATUS_LABELS missing 'frozen' key | lib/constants/campaigns.ts | Add `frozen: 'Frozen'` to labels object |
| CAMP-BUG-02 | Low | Frozen count not displayed in stat cards | campaigns/[id]/page.tsx | Add 4th stat card for frozen enrollments |
| [additional bugs] | | | | |
```

Fill in ALL [placeholder] values. Update the Summary table at the top of the document with final statuses for all 10 requirements (no "pending" remaining). Ensure no placeholder text remains.
  </action>
  <verify>
1. `docs/qa-v3.1/63-campaigns.md` has all 10 requirement sections filled (CAMP-01 through CAMP-10)
2. Summary table at top shows status for all 10 requirements (no "pending" remaining)
3. Screenshots captured: qa-63-campaign-paused.png, qa-63-frozen-label-gap.png, qa-63-campaign-resumed.png, qa-63-enrollments-restored.png, qa-63-conflict-created.png, qa-63-conflict-badge-dashboard.png, qa-63-campaigns-tablet.png, qa-63-campaigns-mobile.png, qa-63-campaigns-dark-mode.png (if tested)
4. CAMP-05 DB query shows status='frozen' for all enrollments after pause (not 'stopped')
5. CAMP-06 DB query shows status='active' for all enrollments after resume
6. CAMP-09 conflict scenario: second Patricia job created, enrollment_resolution IS NOT NULL in DB
7. Frozen label gap documented as CAMP-BUG-01 with exact location and fix recommendation
8. Overall Assessment section exists with bug summary table
9. No placeholder text ([brackets]) remains in the document
  </verify>
  <done>
- CAMP-05 marked PASS or FAIL with DB-verified frozen enrollment status
- CAMP-06 marked PASS or FAIL with DB-verified active enrollment restoration
- CAMP-09 marked PASS or FAIL with conflict scenario created and badge verified
- Frozen label gap documented as known bug (CAMP-BUG-01) with fix recommendation
- Frozen stat card gap documented as known bug (CAMP-BUG-02)
- Responsive checks completed (desktop, tablet, mobile)
- Dark mode check completed or documented as not accessible
- All 10 CAMP requirements have final PASS/FAIL status
- Overall Assessment provides summary with bug table
- Findings document is complete with no remaining placeholders
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Read `docs/qa-v3.1/63-campaigns.md` in full
2. Confirm the Summary table has all 10 requirements with PASS/FAIL status (no "pending")
3. Confirm at least 17 screenshots were captured (list, detail, enrollments, edit sheet, edit saved, preview email, preview sms, analytics, preset picker, new campaign, list with new, paused, frozen label, resumed, restored, conflict, dashboard conflict, tablet, mobile, dark mode)
4. Confirm no placeholder text remains in the document
5. Confirm the Overall Assessment section exists with bug summary table
6. Confirm CAMP-05 and CAMP-06 have DB query evidence (not just UI toast)
7. Confirm the frozen label gap is documented as CAMP-BUG-01
8. If any requirement is FAIL, confirm the bug is clearly described
9. Run final DB verification:
```sql
SELECT ce.status, COUNT(*)
FROM campaign_enrollments ce
WHERE ce.campaign_id = '<hvac-campaign-id>'
GROUP BY ce.status;
```
Expected: all enrollments back to 'active' after CAMP-06 resume
</verification>

<success_criteria>
- All 10 CAMP requirements (CAMP-01 through CAMP-10) have been tested and have a documented PASS/FAIL status
- The findings document `docs/qa-v3.1/63-campaigns.md` is complete with evidence and DB verification
- At minimum: CAMP-01 (list), CAMP-02 (detail), CAMP-05 (freeze DB verified), and CAMP-06 (restore DB verified) must PASS
- CAMP-05 and CAMP-06 verified via Supabase MCP query, not UI toast alone
- Frozen label gap documented as CAMP-BUG-01 (not a test failure, but a bug finding)
- CAMP-09 conflict scenario created and badge verified in dashboard or jobs table
- CAMP-10 new campaign created from preset and exists in DB
- Any bugs found are recorded with clear description, location, and severity
</success_criteria>

<output>
After completion, create `.planning/phases/63-campaigns/63-01-SUMMARY.md`
</output>
