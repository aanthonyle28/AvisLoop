---
phase: 25-problem-solution-storytelling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/marketing/v2/problem-solution.tsx
  - components/marketing/v2/how-it-works.tsx
autonomous: true

must_haves:
  truths:
    - "Problem section displays 3 pain point cards (forgetting to ask, awkward asks, complex tools) with emotional PAS-framework copy"
    - "Each pain point card has a Phosphor icon with colored accent background and 2-3 sentence agitation copy"
    - "Problem section includes a solution bridge line 'AvisLoop fixes all three' transitioning to How It Works"
    - "How It Works section shows 3 numbered steps (Add Contact, Write Message, Send) with alternating left/right layout"
    - "Each step has a GeometricMarker accent, large muted step number, action-verb title, and under-15-word description"
    - "Screenshot placeholders are present for each step with proper aspect ratio and styling"
    - "All animations use FadeIn with staggered delays and motion-safe: prefix"
    - "All components use semantic color tokens (bg-card, text-foreground, border-border, bg-muted, text-muted-foreground)"
  artifacts:
    - path: "components/marketing/v2/problem-solution.tsx"
      provides: "Empathy-driven problem/solution section using PAS framework"
      contains: "ProblemSolutionSection"
    - path: "components/marketing/v2/how-it-works.tsx"
      provides: "3-step visual walkthrough with alternating layout"
      contains: "HowItWorksSection"
  key_links:
    - from: "components/marketing/v2/problem-solution.tsx"
      to: "components/ui/fade-in.tsx"
      via: "import FadeIn"
      pattern: "import.*FadeIn.*fade-in"
    - from: "components/marketing/v2/how-it-works.tsx"
      to: "components/ui/fade-in.tsx"
      via: "import FadeIn"
      pattern: "import.*FadeIn.*fade-in"
    - from: "components/marketing/v2/how-it-works.tsx"
      to: "components/ui/geometric-marker.tsx"
      via: "import GeometricMarker"
      pattern: "import.*GeometricMarker.*geometric-marker"
---

<objective>
Create the ProblemSolutionSection (PAS empathy section) and HowItWorksSection (3-step visual walkthrough) components for the landing page storytelling flow.

Purpose: Build emotional connection with visitors by addressing specific review request pain points (forgetting, awkwardness, complexity) before showing the simple 3-step solution -- transforming the landing page from feature-focused to empathy-driven.

Output: Two new components in `components/marketing/v2/` ready for landing page integration.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-problem-solution-storytelling/25-RESEARCH.md

# Existing components to import
@components/ui/fade-in.tsx — FadeIn scroll-triggered wrapper (Phase 24)
@components/ui/geometric-marker.tsx — Visual accent markers (Phase 24)

# Current sections for reference (patterns to follow)
@components/marketing/v2/hero-v2.tsx — V2 component conventions (color tokens, motion-safe:)
@components/marketing/v2/social-proof-strip.tsx — V2 component conventions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProblemSolutionSection with PAS framework</name>
  <files>components/marketing/v2/problem-solution.tsx</files>
  <action>
Create `components/marketing/v2/problem-solution.tsx` as a `'use client'` component (uses FadeIn which is client).

**Imports:**
- `FadeIn` from `@/components/ui/fade-in`
- `CalendarX`, `Megaphone`, `Wrench` from `@phosphor-icons/react` (duotone weight for icons)

**Pain points data array (3 items):**
```ts
const painPoints = [
  {
    icon: CalendarX,
    problem: "Forgetting to Ask",
    agitation: "You finish a great service, the customer leaves happy, and then... nothing. You meant to follow up but got busy. Another review opportunity lost.",
    color: "lime" as const,
  },
  {
    icon: Megaphone,
    problem: "Awkward Asks",
    agitation: "Asking for reviews in person feels pushy. You don't want to pressure customers, so you say nothing and hope they remember on their own.",
    color: "coral" as const,
  },
  {
    icon: Wrench,
    problem: "Complex Tools",
    agitation: "Marketing platforms promise automation but require campaigns, workflows, and hours of setup. You just want to send one message.",
    color: "lime" as const,
  },
]
```

**Section structure:**
1. `<section className="py-20 md:py-28 bg-muted/30">`
2. Container: `container mx-auto max-w-5xl px-4`
3. Header wrapped in `<FadeIn direction="up">`:
   - `<h2>`: "Stop Losing Reviews to Forgotten Follow-Ups" -- `text-3xl md:text-4xl font-bold text-balance text-center mb-4`
   - `<p>`: "You're not alone. Every business struggles with the same review request challenges." -- `text-lg text-muted-foreground max-w-2xl mx-auto text-center`
4. Cards grid: `grid md:grid-cols-3 gap-8 mt-12`
5. Each card wrapped in `<FadeIn direction="up" delay={i * 150}>`:
   - Card container: `bg-card border border-border rounded-xl p-6 hover:shadow-lg motion-safe:transition-shadow`
   - Icon wrapper: `inline-flex items-center justify-center w-12 h-12 rounded-lg mb-4` with color-conditional bg (`bg-[hsl(var(--accent-lime))]/10` for lime, `bg-[hsl(var(--accent-coral))]/10` for coral -- check how hero-v2.tsx handles lime/coral colors and follow that pattern exactly)
   - Icon: `w-6 h-6` with matching text color, `weight="duotone"`
   - Problem heading: `text-xl font-semibold mb-3 text-foreground`
   - Agitation copy: `text-muted-foreground`
6. Solution bridge wrapped in `<FadeIn direction="up" delay={450}>`:
   - `<div className="mt-12 text-center">`
   - `<p className="text-lg font-medium text-foreground mb-2">AvisLoop fixes all three problems.</p>`
   - `<p className="text-muted-foreground">Here's how it works:</p>`

**Export:** `export function ProblemSolutionSection()`

**Critical:**
- Check how lime/coral accent colors are used in existing v2 components (hero-v2.tsx, social-proof-strip.tsx) and follow the EXACT same pattern for the icon color backgrounds. The project likely uses CSS variables `--accent-lime` and `--accent-coral` or Tailwind custom colors.
- NO hardcoded grays or whites -- all semantic tokens
- ALL animation classes prefixed with `motion-safe:`
  </action>
  <verify>
Run `pnpm typecheck` -- no errors in problem-solution.tsx. Grep for `bg-white`, `bg-gray`, `text-gray` in file -- zero matches. Confirm exports `ProblemSolutionSection`. Confirm `'use client'` directive. Confirm 3 pain point cards with CalendarX, Megaphone, Wrench icons.
  </verify>
  <done>ProblemSolutionSection renders with headline, 3 emotional pain point cards using PAS framework (icon + problem + agitation), staggered FadeIn animations, solution bridge text, and semantic color tokens throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Create HowItWorksSection with alternating 3-step layout</name>
  <files>components/marketing/v2/how-it-works.tsx</files>
  <action>
Create `components/marketing/v2/how-it-works.tsx` as a `'use client'` component.

**Imports:**
- `FadeIn` from `@/components/ui/fade-in`
- `GeometricMarker` from `@/components/ui/geometric-marker`

**Steps data array (3 items):**
```ts
const steps = [
  {
    number: 1,
    title: "Add Contact",
    description: "Import your customer list or add one contact. Takes 10 seconds.",
    color: "lime" as const,
  },
  {
    number: 2,
    title: "Write Message",
    description: "Use our template or write your own. We pre-fill the contact's name.",
    color: "coral" as const,
  },
  {
    number: 3,
    title: "Send",
    description: "Click send. That's it. We track delivery, opens, and clicks for you.",
    color: "lime" as const,
  },
]
```

**Section structure:**
1. `<section className="py-20 md:py-28">`
2. Container: `container mx-auto max-w-5xl px-4`
3. Header wrapped in `<FadeIn direction="up">`:
   - `<h2>`: "How It Works" -- `text-3xl md:text-4xl font-bold text-balance text-center mb-4`
   - `<p>`: "Three steps. Two minutes. Zero complexity." -- `text-lg text-muted-foreground text-center`
4. Steps container: `space-y-16 md:space-y-24 mt-16`
5. Each step wrapped in `<FadeIn direction="up" delay={i * 200}>`:
   - Grid: `grid md:grid-cols-2 gap-8 items-center` with `md:grid-flow-dense` for odd index steps (i % 2 === 1)
   - **Text content column** (for odd steps add `md:col-start-2`):
     - Step indicator row: `flex items-center gap-3 mb-4`
       - `<GeometricMarker variant="circle" color={step.color} size="md" />`
       - Large step number: `text-5xl font-bold text-muted-foreground/20`
     - Title: `text-2xl font-bold mb-3 text-foreground`
     - Description: `text-lg text-muted-foreground`
   - **Screenshot placeholder column** (for odd steps add `md:col-start-1 md:row-start-1`):
     - Container: `rounded-xl border border-border/50 bg-card shadow-lg overflow-hidden`
     - Inner: `relative aspect-[4/3] bg-muted/30`
     - Placeholder text centered: `absolute inset-0 flex items-center justify-center`
       - `<span className="text-sm text-muted-foreground">Screenshot: {step.title}</span>`

**Export:** `export function HowItWorksSection()`

**Critical:**
- Alternating layout: Steps 1 and 3 have text on left + image on right. Step 2 has image on left + text on right (using `md:grid-flow-dense` + `md:col-start-2` for text + `md:col-start-1 md:row-start-1` for image).
- All descriptions must be under 15 words each.
- NO hardcoded grays or whites -- semantic tokens only.
- Follow existing GeometricMarker color prop pattern from other v2 components.
  </action>
  <verify>
Run `pnpm typecheck` -- no errors. Grep for `bg-white`, `bg-gray`, `text-gray` in file -- zero matches. Confirm exports `HowItWorksSection`. Confirm 3 steps with alternating layout logic. Confirm GeometricMarker imported and used. Confirm `'use client'` directive.
  </verify>
  <done>HowItWorksSection renders 3 numbered steps (Add Contact, Write Message, Send) with alternating left/right layout, GeometricMarker accents, large muted step numbers, screenshot placeholders, staggered FadeIn animations, and semantic color tokens.</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes with zero errors
2. `pnpm lint` passes
3. Both files exist: `components/marketing/v2/problem-solution.tsx`, `components/marketing/v2/how-it-works.tsx`
4. Both files marked `'use client'`
5. No hardcoded color values (grep for `bg-white`, `bg-gray`, `text-gray` in v2/ directory returns empty)
6. ProblemSolutionSection has 3 pain point cards with Phosphor icons
7. HowItWorksSection has 3 steps with alternating layout
8. Both use FadeIn with staggered delays
</verification>

<success_criteria>
- ProblemSolutionSection uses PAS framework with emotional copy addressing forgetting, awkwardness, and complexity
- HowItWorksSection displays 3 steps with alternating left/right screenshot placeholders
- Both components import FadeIn and use motion-safe: animation prefixes
- All colors use semantic tokens (bg-card, text-foreground, etc.)
- pnpm typecheck and pnpm lint both pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-problem-solution-storytelling/25-01-SUMMARY.md`
</output>
