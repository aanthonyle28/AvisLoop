---
phase: 67-public-form-edge-cases-report
plan: 03
type: execute
wave: 2
depends_on: [67-01, 67-02]
files_modified: [docs/qa-v3.1/SUMMARY-REPORT.md, docs/qa-v3.1/64-feedback.md]
autonomous: true

must_haves:
  truths:
    - "docs/qa-v3.1/SUMMARY-REPORT.md exists with a health scorecard table listing every tested route, its status (PASS/WARN/FAIL), and bug counts by severity"
    - "SUMMARY-REPORT.md contains a prioritized top-10 fix list with bug ID, description, severity, and location for each item"
    - "SUMMARY-REPORT.md contains a bug tally section showing total counts by severity (Critical, High, Medium, Low)"
    - "All tested routes have at least one findings file in docs/qa-v3.1/ — no route is missing coverage"
    - "The 64-feedback.md findings file exists (created if Phase 64-03 was not previously executed, or verified if it was)"
    - "Cross-cutting patterns section in SUMMARY-REPORT.md identifies recurring issues found across multiple pages"
    - "Known limitations section documents items not testable in dev (rate limiting, Twilio A2P, cross-subdomain middleware)"
  artifacts:
    - path: "docs/qa-v3.1/SUMMARY-REPORT.md"
      provides: "Consolidated QA summary report with health scorecard, bug tally, top-10 fix list, cross-cutting patterns, and known limitations"
      min_lines: 150
    - path: "docs/qa-v3.1/64-feedback.md"
      provides: "Feedback page QA findings (created or verified)"
      min_lines: 30
  key_links: []
---

<objective>
Compile the final QA summary report for the v3.1 E2E Audit milestone. Fill any gaps in findings file coverage and produce the consolidated SUMMARY-REPORT.md.

Purpose: This is the capstone deliverable of the 9-phase QA audit. It aggregates all findings across phases 59-67 into a single actionable document with a health scorecard, prioritized bug list, and cross-cutting pattern analysis.

Output: `docs/qa-v3.1/SUMMARY-REPORT.md` — the definitive QA report. Also `docs/qa-v3.1/64-feedback.md` if not yet created.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Expected findings files in docs/qa-v3.1/ after Plans 67-01 and 67-02:
- 59-auth-flows.md (EXISTS — covers /auth/login, /auth/sign-up, /auth/forgot-password)
- 60-onboarding-wizard.md (EXISTS — covers /onboarding)
- 61-dashboard.md (EXISTS — covers /dashboard)
- 62-jobs.md (EXISTS — covers /jobs)
- 63-campaigns.md (EXISTS — covers /campaigns)
- 64-history.md (EXISTS — covers /history)
- 64-analytics.md (EXISTS — covers /analytics)
- 64-feedback.md (MAY NOT EXIST — Plan 64-03 may not have been executed)
- 65-settings-general-templates.md (EXISTS — covers /settings General + Templates tabs)
- 65-settings-services-customers.md (EXISTS — covers /settings Services + Customers tabs)
- 65-billing.md (EXISTS — covers /billing)
- 66-businesses.md (EXISTS — covers /businesses)
- 67-public-form.md (CREATED by Plan 67-01 — covers /complete/[token])
- 67-edge-cases.md (CREATED by Plan 67-02 — covers cross-cutting all routes)

Known bugs from all prior phases (compile from STATE.md and findings files):
- BUG-CAMP-04 (CRITICAL): frozen enrollment migration not applied to DB
- BUG-ONB-01 (Medium): software_used column missing from businesses table
- BUG-DASH-06 (Medium): KPIWidgets removed from dashboard — no large KPI cards
- BUG-DASH-10 (Medium): Mobile header overflow at 375px
- BUG-HIST-01 (Medium): getSendLogs timezone bug — setHours uses local time
- BUG-JOBS-01 (Low): Column header sorting not working
- BUG-CAMP-01 (Medium): ENROLLMENT_STATUS_LABELS missing 'frozen' key
- BUG-CAMP-02 (Low): No "Frozen" stat card on campaign detail
- BUG-CAMP-03 (Low): resolveTemplate() shows wrong service type in preview
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill findings gaps and create 64-feedback.md if missing</name>
  <files>docs/qa-v3.1/64-feedback.md</files>
  <action>
Check whether `docs/qa-v3.1/64-feedback.md` exists. If it does NOT exist, create it by running a minimal Feedback page audit using Playwright MCP tools and Supabase MCP.

**Step 1: Check file existence**
Read `docs/qa-v3.1/64-feedback.md`. If it exists and has content, skip to Step 3.

**Step 2: If 64-feedback.md does NOT exist — run minimal Feedback page audit**

First, check if there is any feedback data in the test business:
```sql
SELECT COUNT(*) as count FROM customer_feedback
WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
```

If zero feedback rows (likely): The page should show the empty state.

1. Log in as audit-test@avisloop.com
2. Navigate to http://localhost:3000/feedback
3. Document what renders:
   - If empty state: capture "No feedback yet" heading, icon, description text
   - If feedback cards: capture the feedback list layout, rating stars, resolve button
4. Take screenshot: `qa-67-feedback-page.png`

Create `docs/qa-v3.1/64-feedback.md` with:
- Header: Phase 64 QA -- Feedback Page
- Test date, tester, account info
- Summary table with FDBK-01 (list display), FDBK-02 (resolution workflow), FDBK-03 (empty state)
- For each requirement: document what was tested and PASS/FAIL (empty state = code-verified for display, PASS for empty state itself)
- Note that this is a gap-fill audit (Plan 64-03 was not executed during Phase 64)

**Step 3: Verify all findings files exist**
List all files in `docs/qa-v3.1/` and confirm every expected file is present. Document any remaining gaps (there should be none after this task).
  </action>
  <verify>
`docs/qa-v3.1/64-feedback.md` exists with content. All expected findings files (14 files) are present in `docs/qa-v3.1/`.
  </verify>
  <done>64-feedback.md exists. All routes have at least one findings file covering them.</done>
</task>

<task type="auto">
  <name>Task 2: Compile SUMMARY-REPORT.md — health scorecard, bug tally, top-10 fix list</name>
  <files>docs/qa-v3.1/SUMMARY-REPORT.md</files>
  <action>
Read ALL findings files in `docs/qa-v3.1/` and compile the consolidated summary report. This is a document synthesis task — read each findings file, extract bugs and PASS/FAIL verdicts, then produce the report.

**Step 1: Read all findings files**
Read every `.md` file in `docs/qa-v3.1/` (excluding SUMMARY-REPORT.md itself). For each file, extract:
- Route(s) covered
- Requirement IDs and PASS/FAIL verdicts
- Bugs found (bug ID, severity, description, location)
- Notable observations

**Step 2: Build the health scorecard**

Create a table with one row per tested route:

```markdown
| Route | Findings File | Tests | Pass | Fail | Bugs (C/H/M/L) | Status |
|-------|---------------|-------|------|------|-----------------|--------|
| /auth/login | 59-auth-flows.md | N | N | N | 0/0/0/0 | PASS |
| /auth/sign-up | 59-auth-flows.md | N | N | N | 0/0/0/0 | PASS |
| ... | ... | ... | ... | ... | ... | ... |
```

Status rules:
- PASS: All requirements pass, 0 bugs
- WARN: All requirements pass but has Medium/Low bugs
- FAIL: Any requirement fails or has Critical/High bugs

**Step 3: Build the bug tally**

Aggregate all bugs across all findings files:

```markdown
| Severity | Count | Bug IDs |
|----------|-------|---------|
| Critical | N | BUG-CAMP-04 |
| High | N | ... |
| Medium | N | BUG-ONB-01, BUG-DASH-06, ... |
| Low | N | BUG-JOBS-01, ... |
```

Include bugs from:
- STATE.md blockers/concerns section (these are known bugs from prior phases)
- Each findings file's "Bugs Found" section
- Plan 67-01 and 67-02 findings

**Step 4: Build the top-10 priority fix list**

Rank the top 10 bugs/issues by impact (Critical first, then High, then Medium):

```markdown
## Top 10 Priority Fixes

1. **BUG-CAMP-04** (CRITICAL): Frozen enrollment migration not applied to DB — `toggleCampaignStatus()` silently fails on pause/resume. Fix: Apply migration `20260226_add_frozen_enrollment_status.sql` + add error handling in server action.
   - Location: Database migration + `lib/actions/campaign.ts`

2. **BUG-ONB-01** (Medium): `software_used` column missing from businesses table — `saveSoftwareUsed()` silently fails. Fix: `ALTER TABLE businesses ADD COLUMN software_used TEXT;`
   - Location: Database schema

...
```

**Step 5: Cross-cutting patterns**

Identify issues that appear across multiple pages:
- Truncation patterns (working or broken?)
- Dark mode issues (any consistent failures?)
- Mobile overflow (which routes?)
- Loading state consistency
- Empty state consistency
- Accessibility gaps (from UX-AUDIT.md + findings)

**Step 6: Known limitations**

Document items that could NOT be tested in dev:
- Rate limiting (no Upstash in dev)
- Twilio A2P / SMS sending (campaign pending)
- Cross-subdomain middleware (localhost-only)
- Google OAuth full flow (requires real Google account)
- Review funnel HMAC token verification (requires live campaign sends)
- Email delivery (Resend not configured in dev for actual sending)

**Step 7: Write SUMMARY-REPORT.md**

Create `docs/qa-v3.1/SUMMARY-REPORT.md` with this structure:

```markdown
# QA v3.1 Audit — Summary Report

**Audit period:** 2026-02-27 to 2026-03-03
**Phases:** 59-67 (9 phases)
**Tester:** Claude Code (Playwright MCP + Supabase MCP)
**Account:** audit-test@avisloop.com
**Primary business:** Audit Test HVAC

---

## Executive Summary

[2-3 sentences on overall health]

## Health Scorecard

[Route-by-route table]

## Bug Tally

[Severity counts]

## Top 10 Priority Fixes

[Ranked list]

## Cross-Cutting Patterns

[Recurring issues]

## Test Coverage

[What was tested, what was not]

## Known Limitations

[Items not testable in dev]

## Recommendations

[Next steps]

## Appendix: Findings Files Index

[List of all findings files with route mapping]
```
  </action>
  <verify>
`docs/qa-v3.1/SUMMARY-REPORT.md` exists with: health scorecard (all routes listed), bug tally (severity counts), top-10 fix list (ranked by severity), cross-cutting patterns section, known limitations section. File is at least 150 lines.
  </verify>
  <done>SUMMARY-REPORT.md exists with all required sections. Every tested route appears in the health scorecard. All known bugs are tallied. Top-10 fix list is prioritized by severity. Cross-cutting patterns identified. Known limitations documented.</done>
</task>

</tasks>

<verification>
- `docs/qa-v3.1/SUMMARY-REPORT.md` exists and is comprehensive (150+ lines)
- `docs/qa-v3.1/64-feedback.md` exists (created or verified)
- Health scorecard covers all 15 tested routes
- Bug tally includes ALL known bugs from all phases (59-67)
- Top-10 fix list is ordered by severity (Critical > High > Medium > Low)
- Cross-cutting patterns section has at least 2 identified patterns
- Known limitations section documents at least 4 untestable items
- All findings files listed in the appendix
</verification>

<success_criteria>
- SUMMARY-REPORT.md is the single reference document for the v3.1 QA audit
- Every route has findings coverage (no gaps)
- Bugs are ranked and actionable
- Report is useful for a developer who wants to fix the highest-priority issues first
</success_criteria>

<output>
After completion, create `.planning/phases/67-public-form-edge-cases-report/67-03-SUMMARY.md`
</output>
