---
phase: 70-reputation-audit-lead-gen
plan: 03
type: execute
wave: 3
depends_on: ["70-02"]
files_modified:
  - components/audit/audit-results.tsx
  - app/(marketing)/audit/[reportId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "A shareable report URL at /audit/[reportId] displays the full reputation score card"
    - "The report shows letter grade, score, rating, review count, gap analysis, and recommendations"
    - "The report includes a clear CTA driving to AvisLoop signup"
    - "The report page includes Google data disclosure with audited_at timestamp"
    - "An invalid or nonexistent reportId shows a 404 page"
  artifacts:
    - path: "components/audit/audit-results.tsx"
      provides: "Full audit results display component with score card, gaps, and CTA"
      contains: "ScoreBadge"
    - path: "app/(marketing)/audit/[reportId]/page.tsx"
      provides: "Shareable report page with OG metadata for social sharing"
      exports: ["generateMetadata"]
      contains: "notFound"
  key_links:
    - from: "app/(marketing)/audit/[reportId]/page.tsx"
      to: "lib/supabase/service-role.ts"
      via: "Server Component fetches report from DB"
      pattern: "createServiceRoleClient"
    - from: "app/(marketing)/audit/[reportId]/page.tsx"
      to: "components/audit/audit-results.tsx"
      via: "Renders AuditResults with report data"
      pattern: "AuditResults"
    - from: "components/audit/audit-results.tsx"
      to: "components/audit/score-badge.tsx"
      via: "Renders ScoreBadge with grade"
      pattern: "ScoreBadge"
---

<objective>
Build the shareable report page and full results display component. This is what prospects see after entering their email — the full reputation score card with gap analysis, recommendations, and a CTA to AvisLoop signup.

Purpose: The shareable report URL is the viral loop — prospects share their score card, driving more traffic to /audit. The CTA converts audit users into AvisLoop leads/signups.

Output: 1 page route + 1 results component.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-reputation-audit-lead-gen/70-RESEARCH.md
@.planning/phases/70-reputation-audit-lead-gen/70-01-SUMMARY.md
@.planning/phases/70-reputation-audit-lead-gen/70-02-SUMMARY.md

Key patterns:
- Service role client: @lib/supabase/service-role.ts
- Card component: @components/ui/card.tsx (Card, CardHeader, CardContent, CardFooter)
- Marketing layout: @app/(marketing)/layout.tsx
- Score badge: components/audit/score-badge.tsx (from Plan 02)
- Types: lib/audit/types.ts (AuditReport, GapAnalysis from Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audit results component and shareable report page</name>
  <files>components/audit/audit-results.tsx, app/(marketing)/audit/[reportId]/page.tsx</files>
  <action>
  Create `components/audit/audit-results.tsx`:
  - Plain function component (NOT 'use client' — rendered inside Server Component page). If any interactivity needed (copy share link button), make it a client component.
  - Props: `report` (AuditReport type from `lib/audit/types.ts`)
  - Layout structure (top to bottom):

  1. **Hero section** — centered
     - Business name as H1 (text-2xl sm:text-3xl font-bold)
     - Address below (text-muted-foreground)
     - ScoreBadge (lg size) centered
     - Score "X/100" below badge (text-lg font-semibold)

  2. **Key Metrics row** — 2-column grid (grid-cols-2, gap-4)
     - Card 1: "Google Rating" — show rating_snapshot as "X.X" with Star Phosphor icon (filled, amber-500). If null, show "No rating" in muted text.
     - Card 2: "Review Count" — show review_count_snapshot as "X reviews" with ChatCircleDots Phosphor icon. If null, show "No reviews" in muted text.

  3. **Gap Analysis section** — only shown if gaps_json has items
     - Section heading: "Where You Can Improve" (text-xl font-semibold)
     - For each gap item, render a Card with:
       - Gap area as card title (font-semibold)
       - "Current: {current}" in muted text
       - "Benchmark: {benchmark}" in muted text
       - "Recommendation: {recommendation}" in normal text with a Lightbulb Phosphor icon
     - Use Card variant="subtle" (bg-muted border-border/50) for gap cards

  4. **CTA section** — prominent call-to-action
     - Card with variant="amber" (warm palette, draws attention)
     - Heading: "Want to improve your score?" (text-xl font-semibold)
     - Body: "AvisLoop automates review collection for home service businesses. Complete a job, and our system handles the rest — multi-touch follow-ups, review funnels, and reputation monitoring."
     - Two buttons:
       - Primary: "Book a Call" -> Link to "/#pricing" (existing marketing page anchor)
       - Secondary (outline): "Learn More" -> Link to "/" (marketing homepage)
     - Use Phosphor icons: Rocket for Book a Call, ArrowRight for Learn More

  5. **Data disclosure footer**
     - Small text (text-xs text-muted-foreground): "Rating data retrieved on {format(audited_at, 'MMMM d, yyyy')}. Run a new audit for current data."
     - Use `date-fns` format function (already in project)
     - Link "Run a new audit" to `/audit`

  6. **Share section** (make this a small client component if needed for clipboard copy)
     - "Share this report" label
     - Display the current URL as shareable link
     - Copy button using `navigator.clipboard.writeText()`
     - Use Link Phosphor icon

  Create `app/(marketing)/audit/[reportId]/page.tsx`:
  - Server Component
  - `generateMetadata` function:
    - Fetch report from audit_reports by `params.reportId` using `createServiceRoleClient()`
    - If not found, return empty metadata
    - Return: title "{business_name} Reputation Score: {grade} | AvisLoop", description "{business_name} in {city} scored {score}/100 on the AvisLoop Reputation Audit. See the full breakdown."
    - OpenGraph: title, description, type "website"
  - Page component:
    - Await `params` (Next.js 15 async params pattern)
    - Fetch report from audit_reports by `params.reportId` using `createServiceRoleClient()`
    - If not found, call `notFound()` from `next/navigation`
    - Increment view_count: `supabase.from('audit_reports').update({ view_count: report.view_count + 1 }).eq('id', reportId)` (fire-and-forget, don't block render)
    - Render `AuditResults` component with the report data
    - Wrap in centered container: `max-w-2xl mx-auto px-4 py-16`
    - Parse `gaps_json` from JSONB to `GapAnalysis[]` (it may come as string or object from Supabase)

  IMPORTANT: The `[reportId]` page is in the `(marketing)` route group, so it inherits marketing nav/footer. This is intentional — the report page IS a marketing touchpoint.

  IMPORTANT: Use `await params` pattern for Next.js 15 dynamic route params (they are async now). Example:
  ```typescript
  export default async function ReportPage({ params }: { params: Promise<{ reportId: string }> }) {
    const { reportId } = await params
    // ...
  }
  ```

  Same async pattern applies to `generateMetadata`:
  ```typescript
  export async function generateMetadata({ params }: { params: Promise<{ reportId: string }> }) {
    const { reportId } = await params
    // ...
  }
  ```
  </action>
  <verify>
  Run `pnpm typecheck` — both files compile.
  Run `pnpm lint` — no lint errors.
  Run `pnpm dev` and verify:
  - Visit `/audit/nonexistent-uuid` — should show 404 page.
  - If a report exists in DB, visit `/audit/[valid-id]` — should render full score card with grade, metrics, gaps, and CTA.
  - Check page source for OG metadata tags (title, description).
  </verify>
  <done>
  Shareable report page at /audit/[reportId] renders the full reputation score card with letter grade, metrics, gap analysis, recommendations, CTA to AvisLoop signup, data disclosure, and share link. Invalid reportIds show 404. OG metadata enables social sharing preview.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final verification and polish</name>
  <files>components/audit/audit-results.tsx, app/(marketing)/audit/[reportId]/page.tsx</files>
  <action>
  After Task 1 is complete, verify the full end-to-end flow works:

  1. Run `pnpm typecheck` and `pnpm lint` — must both pass clean.

  2. Verify file structure is correct:
     - `lib/audit/types.ts` exists with all type exports
     - `lib/audit/scoring.ts` exists with `computeReputationScore`
     - `lib/audit/places-client.ts` exists with `import 'server-only'`
     - `lib/rate-limit.ts` has `checkAuditRateLimit` export
     - `app/api/audit/search/route.ts` exists with POST export
     - `app/api/audit/submit/route.ts` exists with POST export
     - `app/(marketing)/audit/page.tsx` exists
     - `app/(marketing)/audit/[reportId]/page.tsx` exists
     - `components/audit/audit-form.tsx` exists with 'use client'
     - `components/audit/audit-results.tsx` exists
     - `components/audit/score-badge.tsx` exists
     - `supabase/migrations/20260303_audit_tables.sql` exists

  3. Check that `/audit` is NOT in the APP_ROUTES array in `middleware.ts`. It should NOT be — it's a public marketing route. Verify by reading middleware.ts and confirming `/audit` is not listed.

  4. Verify dark mode support: All components should use semantic tokens (text-foreground, bg-card, text-muted-foreground) and dark: variants where hardcoded colors are used (score badge, gap cards).

  5. Verify mobile responsiveness: Key breakpoints should be handled — the form and results should stack vertically on mobile (max-w-md + responsive text sizes).

  6. If any issues found, fix them. Common issues:
     - Missing Phosphor icon imports (use `@phosphor-icons/react` for client components, `@phosphor-icons/react/dist/ssr` for server components)
     - Missing 'use client' on components that use hooks (useState, useRouter)
     - TypeScript errors from Supabase response typing

  7. Review the CTA section in audit-results.tsx:
     - "Book a Call" link must point to `/#pricing` (existing marketing page pricing section)
     - "Learn More" link must point to `/` (marketing homepage)
     - Both must use Next.js `Link` component (NOT raw `<a>` tags)
  </action>
  <verify>
  `pnpm typecheck` passes.
  `pnpm lint` passes.
  All 12 files from the phase exist in the correct locations.
  `/audit` is NOT in middleware APP_ROUTES.
  CTA links point to correct destinations.
  </verify>
  <done>
  Full end-to-end flow verified: /audit landing -> search form -> preview with grade -> email gate -> /audit/[reportId] full report with gaps, recommendations, CTA, disclosure, and share link. All files compile, lint passes, dark mode and mobile supported.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `/audit/[valid-reportId]` renders full score card with all sections
- `/audit/[invalid-uuid]` returns 404
- OG metadata generates correct title and description for social sharing
- CTA "Book a Call" links to /#pricing
- Data disclosure shows audited_at date
- Share link shows the report URL
- All 12 phase files exist in correct locations
</verification>

<success_criteria>
- Shareable report URL at /audit/[reportId] displays complete reputation score card
- Report shows letter grade, score, rating, review count, gap analysis with recommendations
- Report includes clear CTA driving to AvisLoop signup/booking
- Report includes Google data disclosure with audit timestamp
- Invalid reportId shows 404 page
- OG metadata enables rich social sharing previews
- Full end-to-end flow works: /audit -> search -> preview -> email -> /audit/[reportId]
</success_criteria>

<output>
After completion, create `.planning/phases/70-reputation-audit-lead-gen/70-03-SUMMARY.md`
</output>
