---
phase: 45
plan: 45-02
title: "Rename Activity to History in sidebar and bottom nav"
wave: 1
depends_on: []
files_modified:
  - components/layout/sidebar.tsx
  - components/layout/bottom-nav.tsx
autonomous: true
requirements: [NAV-01]
---

## Objective

Change the navigation label "Activity" to "History" in both the desktop sidebar and mobile bottom nav. The route `/history` and the icon (`ClockCounterClockwise`) remain unchanged.

## Context

@components/layout/sidebar.tsx — `mainNav` array, line 37: `{ icon: ClockCounterClockwise, label: 'Activity', href: '/history' }`
@components/layout/bottom-nav.tsx — `items` array, line 14: `{ icon: ClockCounterClockwise, label: 'Activity', href: '/history' }`

## Tasks

<task id="1">
### Rename label in sidebar.tsx

In `components/layout/sidebar.tsx`, line 37, change:

```typescript
{ icon: ClockCounterClockwise, label: 'Activity', href: '/history' },
```

to:

```typescript
{ icon: ClockCounterClockwise, label: 'History', href: '/history' },
```

This is the only change in this file. The icon and route stay the same.
</task>

<task id="2">
### Rename label in bottom-nav.tsx

In `components/layout/bottom-nav.tsx`, line 14, change:

```typescript
{ icon: ClockCounterClockwise, label: 'Activity', href: '/history' },
```

to:

```typescript
{ icon: ClockCounterClockwise, label: 'History', href: '/history' },
```

This is the only change in this file. The icon and route stay the same.
</task>

## Verification

```bash
pnpm typecheck   # must pass
pnpm lint        # must pass
```

### must_haves (goal-backward from phase success criteria)

- [ ] `sidebar.tsx` displays "History" (not "Activity") in the `mainNav` array
- [ ] `bottom-nav.tsx` displays "History" (not "Activity") in the `items` array
- [ ] The string `'Activity'` does NOT appear in either nav array (grep confirms)
- [ ] The `/history` route and `ClockCounterClockwise` icon are unchanged
- [ ] `pnpm typecheck` passes
- [ ] `pnpm lint` passes
