# Plan 10-01: Design System Update

## Metadata
```yaml
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - tailwind.config.ts
  - components/ui/geometric-marker.tsx
autonomous: true
```

## Objective
Update design system to match reference: white background, lime/coral accents, new typography scale, geometric marker components.

## Tasks

<task id="1">
Update CSS variables in globals.css for new color scheme:
- Change `--background` to pure white (0 0% 100%)
- Add `--accent-lime` (75 85% 55%)
- Add `--accent-coral` (0 85% 65%)
- Lighten `--muted` for subtler card backgrounds
- Keep dark mode values appropriate

Light mode changes:
```css
--background: 0 0% 100%;
--accent-lime: 75 85% 55%;
--accent-coral: 0 85% 65%;
--muted: 0 0% 97%;
```
</task>

<task id="2">
Update tailwind.config.ts to expose new accent colors:
- Add `lime` color using CSS variable
- Add `coral` color using CSS variable
- These will be used as `text-lime`, `bg-lime/10`, etc.

```typescript
lime: {
  DEFAULT: "hsl(var(--accent-lime))",
  foreground: "hsl(0 0% 10%)",
},
coral: {
  DEFAULT: "hsl(var(--accent-coral))",
  foreground: "hsl(0 0% 100%)",
},
```
</task>

<task id="3">
Create new GeometricMarker component at components/ui/geometric-marker.tsx:
- Triangle marker (for stats/metrics)
- Circle marker (for bullet points)
- Props: variant (triangle | circle), color (lime | coral | primary), size (sm | md)

```tsx
import { cn } from "@/lib/utils";

interface GeometricMarkerProps {
  variant: "triangle" | "circle";
  color?: "lime" | "coral" | "primary";
  size?: "sm" | "md";
  className?: string;
}

export function GeometricMarker({
  variant,
  color = "lime",
  size = "sm",
  className,
}: GeometricMarkerProps) {
  const colorClasses = {
    lime: "bg-lime text-lime-foreground",
    coral: "bg-coral text-coral-foreground",
    primary: "bg-primary text-primary-foreground",
  };

  const sizeClasses = {
    sm: variant === "triangle" ? "w-0 h-0 border-l-[6px] border-r-[6px] border-b-[10px]" : "w-2 h-2",
    md: variant === "triangle" ? "w-0 h-0 border-l-[8px] border-r-[8px] border-b-[14px]" : "w-3 h-3",
  };

  if (variant === "triangle") {
    return (
      <span
        className={cn(
          "inline-block border-l-transparent border-r-transparent",
          color === "lime" && "border-b-lime",
          color === "coral" && "border-b-coral",
          color === "primary" && "border-b-primary",
          sizeClasses[size],
          className
        )}
        aria-hidden="true"
      />
    );
  }

  return (
    <span
      className={cn(
        "inline-block rounded-full",
        colorClasses[color],
        sizeClasses[size],
        className
      )}
      aria-hidden="true"
    />
  );
}
```
</task>

<task id="4">
Update marketing layout background:
- Change footer bg from `bg-muted/20` to `bg-muted/50`
- Ensure main content area uses white background
- Adjust border colors for cleaner look

In app/(marketing)/layout.tsx:
- Footer: `bg-muted/50` instead of `bg-muted/20`
- Nav border: `border-border/30` for subtler line
</task>

## Verification
```bash
pnpm lint && pnpm typecheck
```

## must_haves
- [ ] Background is pure white in light mode
- [ ] Lime accent color available (--accent-lime)
- [ ] Coral accent color available (--accent-coral)
- [ ] GeometricMarker component renders triangle and circle variants
- [ ] Tailwind config exposes lime and coral colors
