---
phase: 62-jobs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/qa-v3.1/62-jobs.md]
autonomous: true

must_haves:
  truths:
    - "Jobs table renders with 6 columns (Customer, Service, Status, Campaign, Created, Actions) and column header clicks reorder rows"
    - "Add Job sheet creates a job with inline customer creation (create mode) and the job appears in the table immediately"
    - "Add Job sheet creates a job by searching an existing customer (search mode) and the job appears in the table"
    - "Edit Job sheet opens pre-populated and saves changes to notes and service type"
    - "Job detail drawer displays customer info, service type, status, campaign, notes, and timestamps"
    - "Service type filter shows all 8 types (fallback for empty service_types_enabled) and clicking one scopes visible rows"
    - "Status filter pills (Scheduled, Completed, Do Not Send) correctly scope displayed rows"
    - "Mark Complete button transitions a scheduled job to completed status, verified by DB query showing status=completed and completed_at IS NOT NULL"
    - "Completing an HVAC job triggers campaign enrollment — campaign_enrollments row exists in DB with touch_1_scheduled_at approximately 24h after completed_at"
    - "Campaign selector dropdown in Add Job shows 'HVAC Follow-up' campaign and 'One-off' option when HVAC service type is selected"
    - "Applying filters that match zero jobs shows the filtered empty state ('No jobs match your filters')"
    - "Findings document docs/qa-v3.1/62-jobs.md exists with PASS/FAIL per requirement for JOBS-01 through JOBS-10"
  artifacts:
    - path: "docs/qa-v3.1/62-jobs.md"
      provides: "Jobs page QA findings with pass/fail, screenshots, DB verification, and bugs"
      min_lines: 100
  key_links: []
---

<objective>
Execute the Phase 62 QA E2E audit of the Jobs page: table rendering, Add/Edit/Detail drawers, filters, Mark Complete flow, campaign enrollment trigger, campaign selector, and empty state.

Purpose: The Jobs page is the V2 core action surface. "Completing a job" is the ONLY manual action that triggers campaign automation. This audit verifies all 10 JOBS requirements and creates 3 AUDIT_ test jobs that Phase 63 (Campaigns) depends on for enrollment data.

Output: `docs/qa-v3.1/62-jobs.md` — findings document with PASS/FAIL per requirement (JOBS-01 through JOBS-10), DB verification evidence for enrollment, screenshot references, and bugs.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-jobs/62-RESEARCH.md
@docs/PROJECT_STATE.md

Test account: audit-test@avisloop.com / AuditTest123!
App URL: http://localhost:3000
Output file: docs/qa-v3.1/62-jobs.md
Business name: "Audit Test HVAC" (id: 6ed94b54-6f35-4ede-8dcb-28f562052042)
Business service_types_enabled: [] (empty — filter fallback shows all 8 types)

Existing data context (from Phase 59-61 testing):
- 4 existing customers: Test Technician, Bob Wilson, Jane Doe, John Smith
- 4 existing jobs: Test Technician (hvac/completed), Bob Wilson (electrical/do_not_send), John Smith (hvac/completed), Jane Doe (plumbing/completed)
- 1 campaign: "HVAC Follow-up" (hvac, active, 2 touches — email 24h, SMS 72h)
- 1 existing enrollment: Test Technician → HVAC Follow-up (active)
- DO NOT use Test Technician for new enrollment tests (would trigger conflict)

AUDIT_ jobs to create via UI (NOT pre-seeded in DB):
| Customer | Email | Service | Status | Purpose |
|----------|-------|---------|--------|---------|
| AUDIT_Patricia Johnson | audit-patricia@example.com | HVAC | Completed | JOBS-02 (create mode), JOBS-08 (enrollment) |
| AUDIT_Marcus Rodriguez | audit-marcus@example.com | HVAC | Scheduled | JOBS-07 (mark complete), JOBS-08 (enrollment) |
| AUDIT_Sarah Chen | audit-sarah@example.com | HVAC | Completed | JOBS-02 (search mode), Phase 63 dep |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Table rendering, filters, Add Job (create + search modes), campaign selector, enrollment verification</name>
  <files>docs/qa-v3.1/62-jobs.md</files>
  <action>
This task tests JOBS-01, JOBS-02, JOBS-05, JOBS-06, JOBS-08, JOBS-09. It also creates AUDIT_Patricia Johnson and AUDIT_Marcus Rodriguez as test data for subsequent tests.

**IMPORTANT:** The dev server must be running (`pnpm dev`). Before starting Playwright steps, verify with `browser_navigate` to `http://localhost:3000`. If the server is not running, start it in the background first.

**IMPORTANT:** After every `markJobComplete` or job creation action, follow up with a `mcp__supabase__execute_sql` DB query. Do NOT rely on the UI toast alone — enrollment can fail silently.

**Step-by-step Playwright MCP walkthrough:**

**Part A: Login and navigate to Jobs (setup)**

1. `browser_navigate` to `http://localhost:3000/auth/login`
2. `browser_snapshot` — Get refs for login form
3. `browser_fill_form` — Email: "audit-test@avisloop.com", Password: "AuditTest123!"
4. `browser_click` — Click "Login" button
5. `browser_wait_for` — Wait for dashboard content (max 10 seconds)
6. `browser_navigate` to `http://localhost:3000/jobs`
7. `browser_wait_for` — Wait for "Jobs" heading or table content to appear
8. `browser_snapshot` — Record full page structure
9. `browser_take_screenshot` — filename: `qa-62-jobs-table-initial.png`

**Part B: JOBS-01 — Table columns and sorting**

10. From the snapshot in step 8, verify the table has these 6 column headers:
    - Customer
    - Service
    - Status
    - Campaign
    - Created
    - Actions
    Record which columns are visible and their exact names.

11. Count the number of data rows. Should be 4 based on existing test data (Test Technician HVAC, Bob Wilson electrical, John Smith HVAC, Jane Doe plumbing). Record the actual count.

12. **Test column sorting:** Click the "Customer" column header.
    - `browser_snapshot` — Record row order after click (should sort alphabetically by customer name)
    - `browser_take_screenshot` — filename: `qa-62-jobs-sorted-customer.png`

13. Click the "Customer" column header again (toggle descending sort).
    - `browser_snapshot` — Verify row order reversed

14. Click the "Status" column header.
    - `browser_snapshot` — Verify rows reorder by status

15. Record JOBS-01: PASS if all 6 columns visible and clicking headers reorders rows. FAIL if any column missing or sorting does not work.

**Part C: JOBS-05 — Service type filter**

16. `browser_snapshot` — Look for the service type filter pills in the filters section.
    - The test business has `service_types_enabled = []` (empty), so the fallback shows ALL 8 service types: HVAC, Plumbing, Electrical, Cleaning, Roofing, Painting, Handyman, Other.
    - Count the visible service type pills and list their labels.
    - Expected: 8 pills (all types shown as fallback).

17. Click the "HVAC" service type filter pill.
    - `browser_snapshot` — Verify only HVAC jobs are shown (should see Test Technician and John Smith).
    - Record the row count.
    - `browser_take_screenshot` — filename: `qa-62-filter-service-hvac.png`

18. Click the "HVAC" pill again to deselect it (toggle off).
    - `browser_snapshot` — Verify all jobs return to the table.

19. Click the "Electrical" service type filter pill.
    - `browser_snapshot` — Should see only Bob Wilson's job.
    - Record the row count.

20. Click "Electrical" again to deselect. Verify all jobs return.

21. Record JOBS-05: PASS if filter shows 8 service types (correct fallback for empty service_types_enabled) and clicking a type scopes rows correctly. Document that the business has service_types_enabled=[] and the filter falls back to all 8 types.

**Part D: JOBS-06 — Status filter**

22. `browser_snapshot` — Look for status filter pills: "Scheduled", "Completed", "Do Not Send".
    - List the visible status pills and their labels.

23. Click the "Completed" status filter pill.
    - `browser_snapshot` — Verify only completed jobs are shown (Test Technician, John Smith, Jane Doe — 3 jobs).
    - Record row count.
    - `browser_take_screenshot` — filename: `qa-62-filter-status-completed.png`

24. Click "Completed" again to deselect.

25. Click "Do Not Send" status filter pill.
    - `browser_snapshot` — Should see only Bob Wilson's job (1 job).
    - Record row count.

26. Click "Do Not Send" again to deselect.

27. Click "Scheduled" status filter pill.
    - `browser_snapshot` — Should see 0 jobs (no scheduled jobs exist yet).
    - This may trigger the filtered empty state ("No jobs match your filters").
    - `browser_take_screenshot` — filename: `qa-62-filter-status-scheduled-empty.png`

28. Click "Scheduled" again to deselect (or click "Clear" if available).

29. Record JOBS-06: PASS if all 3 status filters scope rows correctly. FAIL if filter pills are missing or filtering is incorrect.

**Part E: JOBS-02 (Part 1) + JOBS-09 — Add Job with inline customer creation (create mode) for AUDIT_Patricia Johnson**

30. `browser_snapshot` — Find the "Add Job" button
31. `browser_click` — Click the "Add Job" button
32. `browser_wait_for` — Wait for the Add Job sheet/drawer to appear
33. `browser_snapshot` — Record the Add Job sheet structure:
    - Customer field (autocomplete search input)
    - Service Type select
    - Status toggle buttons
    - Campaign selector (appears after service type chosen)
    - Notes textarea
    - Submit button
    - Record the exact labels and element refs

34. **Enter customer name (create-new mode):** Type "AUDIT_Patricia Johnson" into the Customer field.
    - `browser_type` — Ref for Customer input, text: "AUDIT_Patricia Johnson"
    - `browser_snapshot` — Look for autocomplete dropdown. Since "AUDIT_Patricia Johnson" does not match any existing customer, there should be a "Create new customer" option or similar affordance.
    - `browser_take_screenshot` — filename: `qa-62-add-job-create-new-option.png`

35. Click the "Create new customer" option (or the button/link that triggers create mode).
    - `browser_snapshot` — Verify the form switches to "create" mode with additional fields:
      - Customer Name (pre-filled with "AUDIT_Patricia Johnson")
      - Email (required)
      - Phone (optional)
    - Record the exact form state

36. Fill in the create-mode fields:
    - Email: "audit-patricia@example.com"
    - Phone: leave empty (optional)

37. **Select service type:** Click the Service Type dropdown/select.
    - `browser_snapshot` — Record which service types appear
    - Click "HVAC" option
    - `browser_snapshot` — Verify HVAC is selected

38. **JOBS-09: Verify campaign selector:**
    After selecting HVAC, the Campaign Selector should appear and auto-load campaigns matching HVAC.
    - `browser_snapshot` — Record the campaign selector state:
      - Should show "HVAC Follow-up" as an option (the only active HVAC campaign)
      - Should show "One-off" option (or "Send one-off request")
      - Should show "Do not send" option
      - Record all options visible
    - `browser_take_screenshot` — filename: `qa-62-campaign-selector-hvac.png`

39. Leave the campaign selector on its auto-selected value (should auto-select "HVAC Follow-up" as recommended).

40. **Select status:** Click the "Completed" status toggle button.
    - `browser_snapshot` — Verify "Completed" is visually selected/active

41. **Add notes (optional):** Type "AUDIT Phase 62 test job - create mode" into the Notes field.

42. `browser_take_screenshot` — filename: `qa-62-add-job-filled-patricia.png`

43. **Submit the form:** Click the submit/save button (likely "Create Job" or "Add Job").
    - `browser_wait_for` — Wait for success toast ("Job created" or similar)
    - `browser_snapshot` — Verify:
      - Sheet closed (or closing)
      - Table now has a new row for AUDIT_Patricia Johnson
      - Service type shows "HVAC"
      - Status shows "Completed"

44. `browser_take_screenshot` — filename: `qa-62-job-created-patricia.png`

45. **DB verification — job created:**
    ```sql
    SELECT j.id, j.status, j.service_type, j.completed_at,
           j.campaign_override, j.enrollment_resolution,
           c.name as customer_name, c.email as customer_email
    FROM jobs j
    JOIN customers c ON j.customer_id = c.id
    WHERE c.name = 'AUDIT_Patricia Johnson'
    ORDER BY j.created_at DESC
    LIMIT 1;
    ```
    Record the job ID. Verify: status=completed, service_type=hvac, completed_at IS NOT NULL.

46. **JOBS-08: DB verification — enrollment created for Patricia:**
    Using the job ID from step 45:
    ```sql
    SELECT ce.id, ce.status, ce.current_touch,
           ce.touch_1_scheduled_at, ce.touch_1_status,
           ca.name as campaign_name,
           EXTRACT(EPOCH FROM (ce.touch_1_scheduled_at - j.completed_at)) / 3600 as delay_hours_actual
    FROM campaign_enrollments ce
    JOIN campaigns ca ON ce.campaign_id = ca.id
    JOIN jobs j ON ce.job_id = j.id
    WHERE j.id = '<patricia-job-id>'
    ORDER BY ce.enrolled_at DESC
    LIMIT 1;
    ```
    Verify:
    - Enrollment row EXISTS
    - ce.status = 'active'
    - ce.current_touch = 1
    - ce.touch_1_status = 'pending'
    - campaign_name = 'HVAC Follow-up'
    - delay_hours_actual is approximately 24 (the HVAC default delay)
    Record exact values.

47. Record JOBS-02 (Part 1 - create mode): PASS if job created with inline customer creation and appears in table. Record JOBS-08 (Part 1): PASS if enrollment row exists with correct scheduled timing.

**Part F: JOBS-02 (Part 2) — Add Job with existing customer search (search mode) for AUDIT_Marcus Rodriguez**

48. Click "Add Job" button again.
49. `browser_wait_for` — Wait for Add Job sheet
50. `browser_snapshot` — Get refs

51. **Create AUDIT_Marcus first via create mode (he doesn't exist yet):**
    Type "AUDIT_Marcus Rodriguez" into the Customer field.
    - Look for "Create new customer" option, click it.
    - Fill Email: "audit-marcus@example.com"
    - Select Service Type: HVAC
    - Select Status: "Scheduled" (NOT completed — this is the job for JOBS-07 mark complete test)
    - Notes: "AUDIT Phase 62 test job - scheduled for mark complete test"

52. Submit the form.
    - `browser_wait_for` — Wait for success toast
    - `browser_snapshot` — Verify new row in table for AUDIT_Marcus Rodriguez with status "Scheduled"

53. `browser_take_screenshot` — filename: `qa-62-job-created-marcus-scheduled.png`

54. **DB verification — Marcus job created:**
    ```sql
    SELECT j.id, j.status, j.service_type, j.completed_at,
           c.name as customer_name
    FROM jobs j
    JOIN customers c ON j.customer_id = c.id
    WHERE c.name = 'AUDIT_Marcus Rodriguez'
    ORDER BY j.created_at DESC
    LIMIT 1;
    ```
    Verify: status=scheduled, service_type=hvac, completed_at IS NULL. Record the job ID (needed for Task 2 mark-complete test).

55. **Now create AUDIT_Sarah Chen using search mode:**
    First, create Sarah as a customer via a completed job (this tests the search-mode path in Part 2):

    Actually — Sarah doesn't exist yet either, so creating her also requires create mode. We need to test the SEARCH mode path. Instead, create Sarah via create mode first. Then to test the SEARCH mode, we can add a SECOND job for an existing customer (e.g., search for "AUDIT_Patricia Johnson" who now exists from step 34-43).

    **Revised approach for testing search mode:**
    - Click "Add Job"
    - In the Customer field, type "AUDIT_Patricia" (partial name of existing customer)
    - `browser_snapshot` — The autocomplete dropdown should show "AUDIT_Patricia Johnson" as a match
    - `browser_take_screenshot` — filename: `qa-62-add-job-search-autocomplete.png`
    - Click the "AUDIT_Patricia Johnson" suggestion to select her
    - `browser_snapshot` — Verify the customer is selected (field shows her name, no create-new form)

    Then CLOSE the sheet without submitting (we don't need a duplicate job for Patricia; we just proved search mode works).
    - Close the Add Job sheet (click the X or click outside)

    Record JOBS-02 (Part 2 - search mode): PASS if autocomplete showed existing customer and selecting her populated the form correctly.

56. **Now create AUDIT_Sarah Chen (create mode — Phase 63 dependency):**
    - Click "Add Job"
    - Type "AUDIT_Sarah Chen" in Customer field
    - Click "Create new customer" option
    - Fill Email: "audit-sarah@example.com"
    - Select Service Type: HVAC
    - Select Status: Completed
    - Notes: "AUDIT Phase 62 test job - Phase 63 dependency"
    - Submit form
    - `browser_wait_for` — Wait for success toast
    - `browser_snapshot` — Verify new row in table

57. `browser_take_screenshot` — filename: `qa-62-job-created-sarah.png`

58. **DB verification — Sarah enrollment:**
    ```sql
    SELECT ce.id, ce.status, ce.current_touch,
           ce.touch_1_scheduled_at, ce.touch_1_status,
           ca.name as campaign_name
    FROM campaign_enrollments ce
    JOIN campaigns ca ON ce.campaign_id = ca.id
    JOIN jobs j ON ce.job_id = j.id
    JOIN customers cu ON j.customer_id = cu.id
    WHERE cu.name = 'AUDIT_Sarah Chen'
    ORDER BY ce.enrolled_at DESC
    LIMIT 1;
    ```
    Verify enrollment exists for Sarah's job too (HVAC completed = auto-enroll in HVAC Follow-up).

59. Record JOBS-09: PASS if campaign selector showed "HVAC Follow-up", "One-off", and "Do not send" options when HVAC was selected as service type.

**Part G: Initialize findings document**

60. Create `docs/qa-v3.1/62-jobs.md` with findings from all tests in this task:

```markdown
# Phase 62: Jobs — QA Findings

**Tested:** [current date]
**Tester:** Claude (Playwright MCP + Supabase MCP)
**App URL:** http://localhost:3000
**Test account:** audit-test@avisloop.com
**Business:** Audit Test HVAC (6ed94b54-6f35-4ede-8dcb-28f562052042)

---

## Summary

| Requirement | Status | Notes |
|-------------|--------|-------|
| JOBS-01: Table columns + sorting | [PASS/FAIL] | [notes] |
| JOBS-02: Add Job (create + search modes) | [PASS/FAIL] | [notes] |
| JOBS-03: Edit Job | [pending] | |
| JOBS-04: Job detail drawer | [pending] | |
| JOBS-05: Service type filter | [PASS/FAIL] | [notes] |
| JOBS-06: Status filter | [PASS/FAIL] | [notes] |
| JOBS-07: Mark Complete | [pending] | |
| JOBS-08: Enrollment trigger (DB verified) | [PASS/FAIL] | [notes] |
| JOBS-09: Campaign selector | [PASS/FAIL] | [notes] |
| JOBS-10: Empty state | [pending] | |

---

## JOBS-01: Table columns and sorting

**Status:** [PASS/FAIL]

### Columns observed
[List all 6 columns with exact names]

### Sorting test
| Column clicked | Rows reordered? | Notes |
|---------------|----------------|-------|
| Customer | [YES/NO] | [order observed] |
| Status | [YES/NO] | [order observed] |

### Data rows
- Initial row count: [N]
- Expected: 4 (Test Technician, Bob Wilson, John Smith, Jane Doe)

### Evidence
- `qa-62-jobs-table-initial.png`
- `qa-62-jobs-sorted-customer.png`

### Bugs found
- [none or list]

---

## JOBS-02: Add Job — create and search modes

**Status:** [PASS/FAIL]

### Create mode (AUDIT_Patricia Johnson)
1. Opened Add Job sheet
2. Typed "AUDIT_Patricia Johnson" in customer field
3. "Create new customer" option appeared: [YES/NO]
4. Filled email: audit-patricia@example.com
5. Selected service type: HVAC
6. Selected status: Completed
7. Submitted form
8. Result: [job created / error]
9. New row visible in table: [YES/NO]

### Search mode (existing customer lookup)
1. Opened Add Job sheet
2. Typed "AUDIT_Patricia" in customer field
3. Autocomplete showed "AUDIT_Patricia Johnson": [YES/NO]
4. Selected suggestion: [YES/NO]
5. Customer populated in form: [YES/NO]

### DB verification — Patricia's job
```
[paste SQL result]
```

### Evidence
- `qa-62-add-job-create-new-option.png`
- `qa-62-add-job-filled-patricia.png`
- `qa-62-job-created-patricia.png`
- `qa-62-add-job-search-autocomplete.png`

### Bugs found
- [none or list]

---

## JOBS-05: Service type filter

**Status:** [PASS/FAIL]

### Configuration
- Business service_types_enabled: [] (empty)
- Expected behavior: Fallback shows all 8 service types

### Filter pills observed
[List all service type pills with exact labels]
- Total count: [N] (expected: 8)

### Filter scoping test
| Filter applied | Expected rows | Actual rows | Correct? |
|---------------|--------------|-------------|----------|
| HVAC | 2 (Test Technician, John Smith) + new AUDIT_ | [actual] | [YES/NO] |
| Electrical | 1 (Bob Wilson) | [actual] | [YES/NO] |

### Evidence
- `qa-62-filter-service-hvac.png`

### Bugs found
- [none or list]

---

## JOBS-06: Status filter

**Status:** [PASS/FAIL]

### Status pills observed
[List all status filter pills]

### Filter scoping test
| Status | Expected rows | Actual rows | Correct? |
|--------|--------------|-------------|----------|
| Completed | 3+ (all completed jobs) | [actual] | [YES/NO] |
| Do Not Send | 1 (Bob Wilson) | [actual] | [YES/NO] |
| Scheduled | 0 or 1 (Marcus if created) | [actual] | [YES/NO] |

### Evidence
- `qa-62-filter-status-completed.png`
- `qa-62-filter-status-scheduled-empty.png`

### Bugs found
- [none or list]

---

## JOBS-08: Enrollment trigger (DB verified)

**Status:** [PASS/FAIL]

### Patricia Johnson enrollment
```
[paste DB query result — enrollment row, status, touch_1_scheduled_at, delay hours]
```
- Enrollment exists: [YES/NO]
- Status: [active/stopped/missing]
- Campaign: [name]
- Delay hours actual: [N] (expected: ~24)

### Sarah Chen enrollment
```
[paste DB query result]
```
- Enrollment exists: [YES/NO]
- Status: [active/stopped/missing]

### Evidence
- DB query results above

### Bugs found
- [none or list]

---

## JOBS-09: Campaign selector

**Status:** [PASS/FAIL]

### Options observed when HVAC selected
[List all campaign selector options]
- "HVAC Follow-up" present: [YES/NO]
- "One-off" option present: [YES/NO]
- "Do not send" option present: [YES/NO]

### Auto-select behavior
- Recommended campaign auto-selected: [YES/NO]
- Which campaign was auto-selected: [name]

### Evidence
- `qa-62-campaign-selector-hvac.png`

### Bugs found
- [none or list]

---
```

Fill in ALL [placeholder] values with actual observed results. Leave JOBS-03, JOBS-04, JOBS-07, JOBS-10 sections as stubs (filled by Task 2).
  </action>
  <verify>
1. File `docs/qa-v3.1/62-jobs.md` exists with JOBS-01, JOBS-02, JOBS-05, JOBS-06, JOBS-08, JOBS-09 sections filled in
2. Screenshots captured: qa-62-jobs-table-initial.png, qa-62-jobs-sorted-customer.png, qa-62-filter-service-hvac.png, qa-62-filter-status-completed.png, qa-62-filter-status-scheduled-empty.png, qa-62-add-job-create-new-option.png, qa-62-add-job-filled-patricia.png, qa-62-job-created-patricia.png, qa-62-campaign-selector-hvac.png, qa-62-job-created-marcus-scheduled.png, qa-62-add-job-search-autocomplete.png, qa-62-job-created-sarah.png
3. DB verification for Patricia's enrollment shows campaign_enrollments row with status=active and touch_1_scheduled_at ~24h after completed_at
4. DB verification for Sarah's enrollment also shows enrollment row
5. DB verification for Marcus's job shows status=scheduled, completed_at IS NULL (ready for Task 2)
6. 3 AUDIT_ jobs exist in the database: AUDIT_Patricia Johnson (completed), AUDIT_Marcus Rodriguez (scheduled), AUDIT_Sarah Chen (completed)
  </verify>
  <done>
- JOBS-01 marked PASS or FAIL with column list and sorting behavior documented
- JOBS-02 marked PASS or FAIL with both create-mode and search-mode paths exercised
- JOBS-05 marked PASS or FAIL with filter pill count (expected 8 for empty service_types_enabled fallback)
- JOBS-06 marked PASS or FAIL with all 3 status filters tested
- JOBS-08 marked PASS or FAIL with DB-verified enrollment rows for Patricia and Sarah
- JOBS-09 marked PASS or FAIL with campaign selector options listed
- 3 AUDIT_ test jobs created and verified in DB
- Findings document initialized with all Task 1 sections filled
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit Job, detail drawer, Mark Complete + enrollment, empty state, responsive, finalize findings</name>
  <files>docs/qa-v3.1/62-jobs.md</files>
  <action>
This task tests JOBS-03, JOBS-04, JOBS-07, JOBS-08 (second enrollment via mark complete), and JOBS-10. It also checks responsive layout and dark mode. Must run AFTER Task 1 because it depends on AUDIT_Marcus Rodriguez's scheduled job existing.

**IMPORTANT:** If still logged in from Task 1, proceed directly to `/jobs`. If session expired, re-login first with audit-test@avisloop.com / AuditTest123!

**IMPORTANT:** After markJobComplete on Marcus, IMMEDIATELY run a DB query to verify enrollment. Do NOT rely on UI toast — enrollment can fail silently.

**Step-by-step Playwright MCP walkthrough:**

**Part A: JOBS-03 — Edit Job**

1. `browser_navigate` to `http://localhost:3000/jobs`
2. `browser_wait_for` — Wait for table to load with AUDIT_ jobs visible
3. `browser_snapshot` — Find the row for AUDIT_Patricia Johnson

4. **Open Edit Job sheet:** Look in the Actions column for AUDIT_Patricia Johnson's row. There should be an edit button (pencil icon or "Edit" text) or a dropdown menu with an edit option.
   - `browser_snapshot` — Get the ref for the edit action
   - Click the edit action for AUDIT_Patricia Johnson's row

5. `browser_wait_for` — Wait for Edit Job sheet to appear
6. `browser_snapshot` — Verify the Edit Job sheet is pre-populated:
   - Customer field shows "AUDIT_Patricia Johnson" (or a customer selector with her selected)
   - Service Type shows "HVAC"
   - Status shows "Completed"
   - Notes show "AUDIT Phase 62 test job - create mode"
   - Record all pre-populated values
7. `browser_take_screenshot` — filename: `qa-62-edit-job-prepopulated.png`

8. **Make changes:**
   - Change the Notes field to: "AUDIT Phase 62 - EDITED notes for verification"
   - `browser_snapshot` — Verify notes changed

9. **Submit the edit:** Click the save/update button.
   - `browser_wait_for` — Wait for success toast
   - `browser_snapshot` — Verify sheet closes and table row still shows AUDIT_Patricia Johnson

10. **DB verification — edit saved:**
    ```sql
    SELECT j.notes, j.service_type, j.status
    FROM jobs j
    JOIN customers c ON j.customer_id = c.id
    WHERE c.name = 'AUDIT_Patricia Johnson'
    ORDER BY j.created_at DESC
    LIMIT 1;
    ```
    Verify notes = "AUDIT Phase 62 - EDITED notes for verification".

11. Record JOBS-03: PASS if edit sheet opened pre-populated and saved changes correctly (verified in DB). FAIL if pre-population was wrong or save failed.

**Part B: JOBS-04 — Job detail drawer**

12. `browser_snapshot` — Find AUDIT_Patricia Johnson's row in the table
13. **Open detail drawer:** Click on the row itself (not the action buttons — clicking the row should open the detail drawer).
    - `browser_snapshot` — Record what opens. The detail drawer should show:
      - Customer name: AUDIT_Patricia Johnson
      - Customer email: audit-patricia@example.com
      - Service type: HVAC
      - Status: Completed (with badge/dot)
      - Campaign: HVAC Follow-up (or enrollment info)
      - Notes: "AUDIT Phase 62 - EDITED notes for verification"
      - Timestamps (created_at, completed_at)
      - Any action buttons (Mark Complete if scheduled, conflict resolution buttons)
    - Record all information displayed

14. `browser_take_screenshot` — filename: `qa-62-job-detail-drawer.png`

15. Close the detail drawer (click X or click outside).

16. Record JOBS-04: PASS if detail drawer shows customer info, service type, status, campaign, notes, and timestamps. FAIL if any critical field is missing.

**Part C: JOBS-07 + JOBS-08 — Mark Complete on AUDIT_Marcus Rodriguez**

17. `browser_snapshot` — Find AUDIT_Marcus Rodriguez's row (status should be "Scheduled")
18. `browser_take_screenshot` — filename: `qa-62-marcus-before-complete.png`

19. **Mark Complete:** Find the "Complete" or "Mark Complete" button in Marcus's row Actions column.
    - `browser_snapshot` — Get the ref for the complete button
    - Click the Mark Complete button

20. `browser_wait_for` — Wait for success toast ("Job marked complete" or similar)
21. `browser_snapshot` — Verify:
    - Marcus's row status changed from "Scheduled" to "Completed"
    - Campaign column may now show enrollment info
    - Toast message content
22. `browser_take_screenshot` — filename: `qa-62-marcus-after-complete.png`

23. **JOBS-07 DB verification — status change:**
    ```sql
    SELECT j.id, j.status, j.completed_at,
           c.name as customer_name
    FROM jobs j
    JOIN customers c ON j.customer_id = c.id
    WHERE c.name = 'AUDIT_Marcus Rodriguez'
    ORDER BY j.created_at DESC
    LIMIT 1;
    ```
    Verify: status = 'completed', completed_at IS NOT NULL. Record the job ID.

24. **JOBS-08 DB verification — enrollment created for Marcus:**
    ```sql
    SELECT ce.id, ce.status, ce.current_touch,
           ce.touch_1_scheduled_at, ce.touch_1_status,
           ca.name as campaign_name,
           EXTRACT(EPOCH FROM (ce.touch_1_scheduled_at - j.completed_at)) / 3600 as delay_hours_actual
    FROM campaign_enrollments ce
    JOIN campaigns ca ON ce.campaign_id = ca.id
    JOIN jobs j ON ce.job_id = j.id
    WHERE j.customer_id IN (
      SELECT id FROM customers WHERE name = 'AUDIT_Marcus Rodriguez'
    )
    ORDER BY ce.enrolled_at DESC
    LIMIT 1;
    ```
    Verify:
    - Enrollment row EXISTS (the critical check — enrollment can fail silently)
    - ce.status = 'active'
    - ce.current_touch = 1
    - ce.touch_1_status = 'pending'
    - campaign_name = 'HVAC Follow-up'
    - delay_hours_actual is approximately 24

25. Record JOBS-07: PASS if status changed from scheduled to completed (DB verified). FAIL if status did not change.
26. Record JOBS-08 (Part 2 - via mark complete): PASS if enrollment row created with correct timing. FAIL if enrollment missing.

**Part D: JOBS-10 — Empty state**

27. **Filtered empty state:** Apply filters that match zero jobs.
    - Click the "Scheduled" status filter pill (no scheduled jobs exist now — Marcus is completed)
    - AND click a service type filter that has no scheduled jobs (e.g., "Roofing")
    - `browser_snapshot` — Verify the empty state renders:
      - Heading: "No jobs match your filters" (filtered empty state)
      - "Clear Filters" button visible
      - No JavaScript errors in console
    - `browser_take_screenshot` — filename: `qa-62-empty-state-filtered.png`

28. Click "Clear Filters" button.
    - `browser_snapshot` — Verify all jobs return to the table.

29. **True empty state (hasFilters=false):**
    The test account always has jobs, so we cannot reach the true "No jobs yet" state without a second business.
    - Check via DB if a second business exists for the test user:
    ```sql
    SELECT id, name, onboarding_completed_at
    FROM businesses
    WHERE user_id = (SELECT id FROM auth.users WHERE email = 'audit-test@avisloop.com')
    ORDER BY created_at;
    ```
    - If a second business exists with zero jobs, switch to it (via business switcher in sidebar), navigate to /jobs, and screenshot the "No jobs yet" empty state.
    - If no second business exists, document: "True empty state (hasFilters=false) not testable — test account has existing jobs. Code path verified by reading empty-state.tsx: renders 'No jobs yet' heading + 'Add Job' CTA when hasFilters=false."

30. Record JOBS-10: PASS if filtered empty state rendered correctly (heading + clear filters button). Note if true empty state was also tested.

**Part E: Responsive layout checks**

31. `browser_snapshot` — Ensure on /jobs page with data visible at desktop viewport (1440x900)

32. **Tablet viewport:** `browser_resize` to 768x1024
33. `browser_navigate` to `http://localhost:3000/jobs`
34. `browser_snapshot` — Verify:
    - Table renders without horizontal overflow
    - Filters visible and usable
    - "Add Job" button accessible
35. `browser_take_screenshot` — filename: `qa-62-jobs-tablet.png`

36. **Mobile viewport:** `browser_resize` to 390x844
37. `browser_snapshot` — Verify:
    - Content renders without horizontal overflow
    - Table may switch to a card/list layout or scroll horizontally
    - "Add Job" button accessible
    - Filters accessible
38. `browser_take_screenshot` — filename: `qa-62-jobs-mobile.png`

39. `browser_resize` to 1440x900 (reset to desktop)

**Part F: Dark mode check**

40. Look for theme toggle (usually in sidebar or settings). If accessible from the Jobs page:
    - Switch to dark mode
    - `browser_snapshot` — Verify Jobs table renders without:
      - Muddy colors
      - Incorrect contrast
      - Visual artifacts
      - Unreadable text
    - `browser_take_screenshot` — filename: `qa-62-jobs-dark-mode.png`
    - Switch back to light mode
41. If theme toggle is not accessible from Jobs page, note: "Dark mode toggle not tested from Jobs page — requires navigation to settings."

**Part G: Final DB summary — verify all 3 AUDIT_ jobs + enrollments**

42. Run a comprehensive DB query to verify all AUDIT_ test data:
    ```sql
    SELECT j.id, j.status, j.service_type, j.completed_at,
           c.name as customer_name, c.email as customer_email,
           ce.id as enrollment_id, ce.status as enrollment_status,
           ce.touch_1_scheduled_at, ca.name as campaign_name
    FROM jobs j
    JOIN customers c ON j.customer_id = c.id
    LEFT JOIN campaign_enrollments ce ON ce.job_id = j.id
    LEFT JOIN campaigns ca ON ce.campaign_id = ca.id
    WHERE c.name LIKE 'AUDIT_%'
    ORDER BY j.created_at ASC;
    ```
    Record complete results. This is the data handoff to Phase 63.

**Part H: Finalize findings document**

43. Update `docs/qa-v3.1/62-jobs.md` — append the JOBS-03, JOBS-04, JOBS-07, JOBS-10 sections:

```markdown

## JOBS-03: Edit Job

**Status:** [PASS/FAIL]

### Pre-populated values observed
| Field | Expected | Actual |
|-------|----------|--------|
| Customer | AUDIT_Patricia Johnson | [actual] |
| Service Type | HVAC | [actual] |
| Status | Completed | [actual] |
| Notes | AUDIT Phase 62 test job - create mode | [actual] |

### Edit saved
- Changed notes to: "AUDIT Phase 62 - EDITED notes for verification"
- Save submitted: [success/error]
- DB verified: [YES/NO]

### Evidence
- `qa-62-edit-job-prepopulated.png`

### Bugs found
- [none or list]

---

## JOBS-04: Job detail drawer

**Status:** [PASS/FAIL]

### Information displayed
| Field | Present? | Value |
|-------|----------|-------|
| Customer name | [YES/NO] | [value] |
| Customer email | [YES/NO] | [value] |
| Service type | [YES/NO] | [value] |
| Status | [YES/NO] | [value] |
| Campaign | [YES/NO] | [value] |
| Notes | [YES/NO] | [value] |
| Created at | [YES/NO] | [value] |
| Completed at | [YES/NO] | [value] |

### Evidence
- `qa-62-job-detail-drawer.png`

### Bugs found
- [none or list]

---

## JOBS-07: Mark Complete

**Status:** [PASS/FAIL]

### Before
- Customer: AUDIT_Marcus Rodriguez
- Status: Scheduled
- completed_at: NULL

### After
- Status changed to: [Completed/unchanged]
- completed_at: [timestamp or NULL]
- UI toast: [message text]

### DB verification
```
[paste SQL result showing status=completed, completed_at IS NOT NULL]
```

### Evidence
- `qa-62-marcus-before-complete.png`
- `qa-62-marcus-after-complete.png`

### Bugs found
- [none or list]

---

## JOBS-08: Enrollment trigger — Marcus Rodriguez (via Mark Complete)

### Marcus enrollment
```
[paste DB query result — enrollment row, status, touch_1_scheduled_at, delay hours]
```
- Enrollment exists: [YES/NO]
- Status: [active/stopped/missing]
- Campaign: [name]
- Delay hours actual: [N] (expected: ~24)

---

## JOBS-10: Empty state

**Status:** [PASS/FAIL]

### Filtered empty state (hasFilters=true)
- Filters applied: [status + service type combination]
- Heading shown: [exact text]
- "Clear Filters" button visible: [YES/NO]
- No JavaScript errors: [YES/NO]

### True empty state (hasFilters=false)
- [Tested with second business / Not testable — single business with existing jobs]
- Code path verified: empty-state.tsx renders "No jobs yet" + "Add Job" CTA

### Evidence
- `qa-62-empty-state-filtered.png`

### Bugs found
- [none or list]

---

## Responsive & Dark Mode

### Viewport checks
| Viewport | Status | Notes |
|----------|--------|-------|
| Desktop (1440x900) | [PASS/FAIL] | [notes] |
| Tablet (768x1024) | [PASS/FAIL] | [notes] |
| Mobile (390x844) | [PASS/FAIL] | [notes] |

### Dark mode
- [PASS/FAIL/not tested] | [notes]

### Evidence
- `qa-62-jobs-tablet.png`
- `qa-62-jobs-mobile.png`
- `qa-62-jobs-dark-mode.png` (if tested)

---

## AUDIT_ Test Data Summary (Phase 63 Handoff)

All 3 AUDIT_ jobs created for downstream phases:

```
[paste comprehensive DB query result from Part G]
```

### Enrollment summary
| Customer | Job Status | Campaign | Enrollment Status | Touch 1 Scheduled |
|----------|-----------|----------|-------------------|-------------------|
| AUDIT_Patricia Johnson | completed | [name] | [status] | [timestamp] |
| AUDIT_Marcus Rodriguez | completed | [name] | [status] | [timestamp] |
| AUDIT_Sarah Chen | completed | [name] | [status] | [timestamp] |

---

## Overall Assessment

[Summary paragraph covering:
- How many requirements passed (N/10)
- Any critical bugs found
- Any limitations or caveats
- Confirmation that 3 AUDIT_ jobs exist with enrollments for Phase 63
- Readiness statement for Phase 63 (Campaigns)]
```

Fill in ALL [placeholder] values. Update the Summary table at the top with final statuses for all 10 requirements. Ensure no placeholder text remains.
  </action>
  <verify>
1. `docs/qa-v3.1/62-jobs.md` has all 10 requirement sections filled (JOBS-01 through JOBS-10)
2. Summary table at top shows status for all 10 requirements (no "pending" remaining)
3. Screenshots captured: qa-62-edit-job-prepopulated.png, qa-62-job-detail-drawer.png, qa-62-marcus-before-complete.png, qa-62-marcus-after-complete.png, qa-62-empty-state-filtered.png, qa-62-jobs-tablet.png, qa-62-jobs-mobile.png, qa-62-jobs-dark-mode.png (if tested)
4. DB verification for Marcus's mark-complete shows status=completed and enrollment row exists
5. Comprehensive AUDIT_ test data summary shows all 3 jobs + enrollments (Phase 63 handoff)
6. Overall Assessment section exists with readiness statement for Phase 63
7. No placeholder text ([brackets]) remains in the document
  </verify>
  <done>
- JOBS-03 marked PASS or FAIL with pre-populated values documented and edit verified in DB
- JOBS-04 marked PASS or FAIL with all detail drawer fields listed
- JOBS-07 marked PASS or FAIL with DB-verified status transition (scheduled → completed)
- JOBS-08 (Part 2) marked PASS or FAIL with Marcus's enrollment row verified in DB
- JOBS-10 marked PASS or FAIL with filtered empty state tested (and true empty state if second business available)
- Responsive checks completed (desktop, tablet, mobile)
- Dark mode check completed or documented as not accessible
- All 10 JOBS requirements have final PASS/FAIL status
- AUDIT_ test data summary confirms 3 jobs + 3 enrollments for Phase 63
- Overall Assessment provides readiness statement for Phase 63
- Findings document is complete with no remaining placeholders
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Read `docs/qa-v3.1/62-jobs.md` in full
2. Confirm the Summary table has all 10 requirements with PASS/FAIL status (no "pending")
3. Confirm at least 15 screenshots were captured (initial table, sorted, filters x3, add job x4, campaign selector, marcus before/after, detail drawer, edit, empty state, tablet, mobile, dark mode)
4. Confirm no placeholder text remains in the document
5. Confirm the Overall Assessment section exists and states readiness for Phase 63
6. Confirm the AUDIT_ test data summary section shows all 3 jobs with enrollment data
7. If any requirement is FAIL, confirm the bug is clearly described with reproduction steps
8. Run the comprehensive AUDIT_ DB query one final time to confirm all 3 enrollments exist:
```sql
SELECT c.name, j.status, ce.status as enrollment_status
FROM jobs j
JOIN customers c ON j.customer_id = c.id
LEFT JOIN campaign_enrollments ce ON ce.job_id = j.id
WHERE c.name LIKE 'AUDIT_%'
ORDER BY j.created_at;
```
Expected: 3 rows, all with j.status=completed and ce.enrollment_status=active
</verification>

<success_criteria>
- All 10 JOBS requirements (JOBS-01 through JOBS-10) have been tested and have a documented PASS/FAIL status
- The findings document `docs/qa-v3.1/62-jobs.md` is complete with evidence and DB verification
- At minimum: JOBS-01 (table), JOBS-02 (add job), JOBS-07 (mark complete), and JOBS-08 (enrollment) must PASS for Phase 63 to proceed
- JOBS-05 documents the fallback behavior (8 types for empty service_types_enabled) — this is correct behavior, not a bug
- JOBS-08 enrollment verified via DB query, not UI toast alone
- 3 AUDIT_ test jobs exist with enrollments in the database (Phase 63 dependency met)
- Any bugs found are recorded with clear reproduction steps and severity
</success_criteria>

<output>
After completion, create `.planning/phases/62-jobs/62-01-SUMMARY.md`
</output>
