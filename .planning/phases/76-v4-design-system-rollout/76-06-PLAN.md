---
wave: 4
depends_on: [76-02, 76-03, 76-04, 76-05]
files_modified:
  - components/marketing/v2/*
  - components/marketing/v3/*
  - components/marketing/hero.tsx
  - components/marketing/features.tsx
  - components/marketing/stats-section.tsx
  - components/marketing/social-proof.tsx
  - components/marketing/pricing-table.tsx
  - components/marketing/faq-section.tsx
autonomous: true
---

# Plan 76-06: Cleanup Old V2/V3 Marketing Components

## Objective

Remove all unused V2 and V3 marketing components that are no longer imported by any page. The V4 components and redesigned pages should be the only marketing code in use.

## Context

After plans 76-01 through 76-05, the following should no longer be imported:
- `components/marketing/v2/*` (hero-v2, social-proof-strip, features-bento, services-section, how-it-works, animated-stats, animated-demo, pricing-section)
- `components/marketing/v3/*` (hero-webdesign, services-webdesign, process-section, pricing-webdesign, faq-webdesign) — note: `client-portal-lookup.tsx` is STILL IN USE by the client portal page
- `components/marketing/hero.tsx`, `features.tsx`, `stats-section.tsx`, `social-proof.tsx`, `pricing-table.tsx`, `faq-section.tsx` (base components that may have been replaced)

## Tasks

<task id="1">
Run a comprehensive import scan to identify which marketing components are still imported:

```bash
# For each file in components/marketing/, check if it's imported anywhere
grep -r "from.*components/marketing" app/ components/ --include="*.tsx" --include="*.ts" -l
```

Build a definitive list of:
- Files that ARE still imported (keep these)
- Files that are NOT imported anywhere (safe to delete)

**Critical: Do NOT delete:**
- `components/marketing/v4/*` (new shared components)
- `components/marketing/mobile-nav.tsx` (if still used by marketing layout)
- `components/marketing/user-menu.tsx` (if still used by marketing layout)
- `components/marketing/testimonials.tsx` (check if still imported — may have been replaced by V4Testimonials)
- `components/marketing/cta-section.tsx` (check if still imported)
- `components/marketing/v3/client-portal-lookup.tsx` (used by client-portal page)
</task>

<task id="2">
Delete all confirmed unused component files. For each deletion, verify no import references remain.

Expected deletions (verify each before deleting):
- `components/marketing/v2/` — likely entire directory
- `components/marketing/v3/hero-webdesign.tsx`
- `components/marketing/v3/services-webdesign.tsx`
- `components/marketing/v3/process-section.tsx`
- `components/marketing/v3/pricing-webdesign.tsx`
- `components/marketing/v3/faq-webdesign.tsx`
- Various base marketing components
</task>

<task id="3">
Clean up the `components/ui/fade-in.tsx` if it's no longer imported (it was the V3 animation system, replaced by Framer Motion). Verify before deleting.
</task>

<task id="4">
Verify: `pnpm lint && pnpm typecheck` pass. No broken imports.
</task>

## Verification

```bash
pnpm lint && pnpm typecheck
```

- [ ] All unused V2/V3 marketing components deleted
- [ ] No broken imports anywhere in the codebase
- [ ] `components/marketing/v4/` intact with all shared components
- [ ] Still-used components preserved (mobile-nav, user-menu, client-portal-lookup, etc.)
- [ ] `pnpm lint && pnpm typecheck` pass

## must_haves

1. No unused V2 or V3 marketing components remain in the codebase
2. All actively-imported components preserved
3. Zero broken imports — `pnpm lint && pnpm typecheck` clean
4. `components/marketing/v4/` is the canonical marketing component directory
