---
wave: 3
depends_on: [76-02]
files_modified:
  - app/(marketing)/pricing/page.tsx
  - app/(marketing)/client-portal/page.tsx
autonomous: true
---

# Plan 76-04: Redesign Pricing + Client Portal Pages

## Objective

Redesign the `/pricing` and `/client-portal` pages with V4 design language. These are currently minimal pages (69 and 34 lines) that can be significantly enhanced.

## Context

Both pages still use the shared `(marketing)` layout (nav + footer). That's fine — the layout will be updated in 76-05. For now, focus on the page content itself using V4 patterns.

## Tasks

<task id="1">
Redesign `app/(marketing)/pricing/page.tsx`:

This page should feel like a standalone pricing destination, not just a sub-section.

**Structure:**
- Hero-style header with AccentBar + oversized headline: "Simple pricing." / "No surprises."
- Subtitle: "No setup fees. No contracts. Cancel anytime."
- 3-column pricing grid (same data as homepage): Basic $199/mo, Advanced $299/mo (highlighted/dark), Review Add-On $99/mo
- Each card: plan name, price, tagline, feature list with checks, pill CTA
- The Advanced card uses inverted dark styling with "Popular" badge
- Below cards: FAQ section using V4FAQ component with pricing-specific questions:
  1. What's included in the monthly fee?
  2. What if I need more revisions?
  3. Do I own my website?
  4. Can I cancel anytime?
  5. How is this different from hiring a designer?
  6. What is the Review Add-On?
- Use Framer Motion scroll-triggered animations on cards (staggered entrance)

Keep metadata export for SEO.

The page is a client component for Framer Motion. Export metadata from a separate server file or use the pattern: default export is server component that renders metadata + client component.
</task>

<task id="2">
Redesign `app/(marketing)/client-portal/page.tsx`:

The portal lookup page should feel premium and purposeful, not like an afterthought.

**Structure:**
- Centered layout with FloatingShapes background (subtle, not overwhelming)
- AccentBar + headline: "Client Portal"
- Subtitle: "Access your project, submit revision requests, and track progress."
- The ClientPortalLookup component (import from `components/marketing/v3/client-portal-lookup.tsx` — keep existing component, just wrap with V4 styling)
- Below the form: 3 small info blocks explaining what the portal provides:
  1. "Submit Revisions" — description
  2. "Track Progress" — description
  3. "View History" — description
- Help text with email contact link
- Framer Motion fade-in animations on the form and info blocks

This page keeps the existing `ClientPortalLookup` component — just wraps it in V4 visual styling.
</task>

<task id="3">
Verify: `pnpm lint && pnpm typecheck` pass. Both pages render correctly.
</task>

## Verification

```bash
pnpm lint && pnpm typecheck
```

- [ ] `/pricing` shows 3-column pricing grid with V4 styling and animations
- [ ] `/pricing` has FAQ section with 6 pricing-specific questions
- [ ] `/client-portal` shows lookup form with FloatingShapes background and info blocks
- [ ] Both pages responsive at 375px
- [ ] Metadata preserved on both pages

## must_haves

1. `/pricing` has V4-styled 3-column pricing grid with Framer Motion stagger animations, inverted dark card for Advanced plan, and FAQ section
2. `/client-portal` wraps existing lookup component with V4 visual treatment (FloatingShapes, AccentBar, info blocks)
3. Both pages use Framer Motion for scroll-triggered entrance animations
4. `pnpm lint && pnpm typecheck` pass
