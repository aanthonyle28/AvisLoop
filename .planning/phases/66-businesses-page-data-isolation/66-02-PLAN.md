---
phase: 66-businesses-page-data-isolation
plan: 02
type: execute
wave: 2
depends_on: [66-01]
files_modified: [docs/qa-v3.1/66-switcher.md]
autonomous: true

must_haves:
  truths:
    - "Business switcher dropdown in desktop sidebar shows both 'Audit Test HVAC' and 'AUDIT_ Test Plumbing' as options with a checkmark on the active business"
    - "Selecting a different business from the dropdown updates the dashboard page to show that business's data (different KPIs, different job counts)"
    - "After switching to AUDIT_ Test Plumbing and navigating to /jobs, the jobs page shows zero or only Business B's jobs (not Business A's 7+ jobs)"
    - "After switching to AUDIT_ Test Plumbing and navigating to /campaigns, only Business B's campaign is visible (not Business A's HVAC Follow-up)"
    - "Mobile business switcher at 375px viewport width is accessible, renders the business name, and the dropdown is tappable"
    - "'View all clients' link in the dropdown navigates to /businesses"
    - "Findings document docs/qa-v3.1/66-switcher.md exists with PASS/FAIL per requirement for MULTI-01, MULTI-02, MULTI-03"
  artifacts:
    - path: "docs/qa-v3.1/66-switcher.md"
      provides: "Business switcher QA findings with pass/fail, screenshots, and cross-page data verification"
      min_lines: 100
  key_links: []
---

<objective>
Execute the Phase 66 QA audit of the Business Switcher component: desktop dropdown rendering (MULTI-01), data refresh on switch across dashboard/jobs/campaigns (MULTI-02), and mobile switcher accessibility (MULTI-03).

Purpose: The business switcher is the user's mechanism for toggling between businesses. This audit verifies the dropdown renders correctly with all businesses, that switching actually updates page data (not just the name label), and that the mobile experience is functional. Depends on Plan 66-01 having created the second business.

Output: `docs/qa-v3.1/66-switcher.md` -- findings document with PASS/FAIL per requirement (MULTI-01, MULTI-02, MULTI-03), screenshots, and cross-page data verification.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-businesses-page-data-isolation/66-RESEARCH.md
@docs/PROJECT_STATE.md

Test account: audit-test@avisloop.com / AuditTest123!
App URL: http://localhost:3000
Output file: docs/qa-v3.1/66-switcher.md
Business A: "Audit Test HVAC" (id: 6ed94b54-6f35-4ede-8dcb-28f562052042)
Business B: "AUDIT_ Test Plumbing" (id from 66-01 findings -- read from docs/qa-v3.1/66-businesses.md)

**PREREQUISITE:** Plan 66-01 must be complete. Read docs/qa-v3.1/66-businesses.md to get Business B's ID.

Existing data:
- Business A has 7+ jobs, 3 AUDIT_ customers, 2 campaigns, 4+ enrollments
- Business B has 0 jobs, 0 customers, 1 campaign (created by wizard)
- User has 2 businesses -- BusinessSwitcher will render as dropdown (not plain text)

Key switcher behavior:
- Desktop: DropdownMenu in sidebar with button trigger (aria-label contains "Current business")
- Mobile: Same BusinessSwitcher component in PageHeader (md:hidden header)
- switchBusiness() sets httpOnly cookie + calls revalidatePath('/', 'layout')
- After switch, wait for revalidation before navigating (toast appearance is a good signal)
- Dropdown items use `getByRole('menuitem')` -- includes business names + "View all clients" link
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desktop business switcher dropdown and cross-page data refresh (MULTI-01, MULTI-02)</name>
  <files>docs/qa-v3.1/66-switcher.md</files>
  <action>
This task tests MULTI-01 (dropdown shows all businesses) and MULTI-02 (switching updates all page data).

**IMPORTANT:** Before starting, read docs/qa-v3.1/66-businesses.md to get Business B's ID. If the file doesn't exist or Business B wasn't created, STOP -- Plan 66-01 must complete first.

**Step-by-step walkthrough:**

**Part A: Read Business B ID from Plan 66-01 output**

1. Read `docs/qa-v3.1/66-businesses.md` and extract Business B's UUID. Store it as `<business-b-id>`.
   If the file doesn't contain a Business B ID, run:
   ```sql
   SELECT id, name FROM businesses
   WHERE user_id = (SELECT id FROM auth.users WHERE email = 'audit-test@avisloop.com')
   ORDER BY created_at DESC;
   ```
   The second row (newest) is Business B. Record its ID.

**Part B: Verify starting state**

2. **Confirm active business is Audit Test HVAC:**
   ```sql
   SELECT id, name FROM businesses
   WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
   ```
   This is Business A. Plan 66-01's Task 2 should have switched back to it.

3. **Confirm Business B data expectations:**
   ```sql
   SELECT
     (SELECT COUNT(*) FROM jobs WHERE business_id = '<business-b-id>') as job_count,
     (SELECT COUNT(*) FROM customers WHERE business_id = '<business-b-id>') as customer_count,
     (SELECT COUNT(*) FROM campaigns WHERE business_id = '<business-b-id>' AND is_preset = false) as campaign_count;
   ```
   Expected: job_count=0, customer_count=0, campaign_count=1 (or similar -- Business B was just created via wizard).

4. **Confirm Business A data expectations:**
   ```sql
   SELECT
     (SELECT COUNT(*) FROM jobs WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042') as job_count,
     (SELECT COUNT(*) FROM customers WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042') as customer_count,
     (SELECT COUNT(*) FROM campaigns WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042' AND is_preset = false) as campaign_count;
   ```
   Expected: job_count >= 7, customer_count >= 3, campaign_count >= 2.

Record all counts -- these are the ground truth for verifying data refresh after switch.

**Part C: Login and verify desktop switcher (MULTI-01)**

5. `browser_navigate` to `http://localhost:3000/auth/login`
6. Login with audit-test@avisloop.com / AuditTest123!
7. `browser_wait_for` -- Wait for dashboard (max 15 seconds)

8. **Find the business switcher in the sidebar:**
   `browser_snapshot` -- Look for the sidebar area with business name. The switcher trigger should have an aria-label containing "Current business" or show the business name with a dropdown chevron.

9. **Verify switcher shows as dropdown (not plain text):**
   The switcher must be an interactive button (not a plain span), because user now has 2 businesses. If it shows as plain text, BIZ-07 from Plan 66-01 may not have created the second business.

10. **Open the dropdown:** Click the switcher trigger button.
    `browser_wait_for` -- Wait for dropdown to appear
    `browser_snapshot` -- Record:
    - Number of business options
    - Each option: business name text, checkmark on active business
    - "View all clients" link at the bottom
    `browser_take_screenshot` -- filename: `qa-66-switcher-dropdown.png`

11. **MULTI-01 verification:** From the snapshot, confirm:
    - "Audit Test HVAC" option is visible with a checkmark (it is active)
    - "AUDIT_ Test Plumbing" option is visible without a checkmark
    - "View all clients" link is present
    - Total business options = 2

12. Record MULTI-01: PASS if dropdown shows all user-owned businesses with correct active indicator. FAIL if any business is missing, active indicator is wrong, or dropdown doesn't open.

**Part D: Switch to Business B and verify data refresh (MULTI-02)**

13. **Click "AUDIT_ Test Plumbing" in the dropdown:**
    `browser_click` -- Click the menu item for "AUDIT_ Test Plumbing"
    `browser_wait_for` -- Wait for:
    - Toast notification (e.g., "Switched to AUDIT_ Test Plumbing")
    - Page content to update
    Wait 2-3 seconds for revalidatePath to complete.

14. **Verify sidebar shows new business:**
    `browser_snapshot` -- The switcher trigger should now show "AUDIT_ Test Plumbing"
    `browser_take_screenshot` -- filename: `qa-66-switched-to-plumbing.png`

15. **Verify Dashboard data changed:**
    The dashboard should now show Business B's data (mostly zeros since it's new).
    `browser_snapshot` -- Record:
    - KPI values (should be 0 or "--" for a new business with no activity)
    - Ready-to-Send queue (should be empty)
    - Any other data-driven sections
    `browser_take_screenshot` -- filename: `qa-66-dashboard-business-b.png`

16. **Navigate to /jobs and verify:**
    `browser_navigate` to `http://localhost:3000/jobs`
    `browser_wait_for` -- Wait for jobs page
    `browser_snapshot` -- Record:
    - Number of jobs visible (should be 0 for Business B)
    - Empty state message visible?
    - No AUDIT_ jobs from Business A should appear
    `browser_take_screenshot` -- filename: `qa-66-jobs-business-b.png`

17. **Navigate to /campaigns and verify:**
    `browser_navigate` to `http://localhost:3000/campaigns`
    `browser_wait_for` -- Wait for campaigns page
    `browser_snapshot` -- Record:
    - Number of campaigns visible
    - Should show only Business B's campaign (Standard preset duplicate)
    - Should NOT show "HVAC Follow-up" or "Standard Follow-Up" from Business A
    `browser_take_screenshot` -- filename: `qa-66-campaigns-business-b.png`

18. **Switch BACK to Business A:**
    - Open the switcher dropdown (click the trigger)
    - Click "Audit Test HVAC"
    - Wait for toast + revalidation (2-3 seconds)
    - `browser_snapshot` -- Verify sidebar shows "Audit Test HVAC"

19. **Verify Dashboard shows Business A data:**
    `browser_navigate` to `http://localhost:3000/dashboard`
    `browser_wait_for` -- Wait for dashboard
    `browser_snapshot` -- Record:
    - KPI values should be non-zero (Business A has real data)
    - Ready-to-Send queue may have items
    `browser_take_screenshot` -- filename: `qa-66-dashboard-business-a-restored.png`

20. **Navigate to /jobs and verify Business A data restored:**
    `browser_navigate` to `http://localhost:3000/jobs`
    `browser_wait_for` -- Wait for jobs page
    `browser_snapshot` -- Record:
    - Jobs visible (should be 7+ from Business A)
    - AUDIT_ jobs should be present

21. Record MULTI-02: PASS if:
    - Switching to Business B showed empty/minimal data (matching DB counts from step 3)
    - Switching back to Business A showed full data (matching DB counts from step 4)
    - Dashboard, Jobs, and Campaigns all reflected the correct active business
    FAIL if any page showed the wrong business's data after a switch.

**Part E: Test "View all clients" link**

22. Open the switcher dropdown again.
23. Click "View all clients" menu item.
24. `browser_wait_for` -- Wait for navigation to /businesses
25. `browser_snapshot` -- Verify we are on the Businesses page
26. Record whether "View all clients" correctly navigated to /businesses.

**Part F: Initialize findings document**

27. Create `docs/qa-v3.1/66-switcher.md`:

```markdown
# Phase 66: Business Switcher -- QA Findings

**Tested:** [current date]
**Tester:** Claude (Playwright MCP + Supabase MCP)
**App URL:** http://localhost:3000
**Test account:** audit-test@avisloop.com
**Business A:** Audit Test HVAC (6ed94b54-6f35-4ede-8dcb-28f562052042)
**Business B:** AUDIT_ Test Plumbing (<business-b-id>)

---

## Summary

| Requirement | Status | Notes |
|-------------|--------|-------|
| MULTI-01: Switcher shows all businesses | [PASS/FAIL] | [notes] |
| MULTI-02: Switch updates all page data | [PASS/FAIL] | [notes] |
| MULTI-03: Mobile switcher functional | [pending] | Tested in Task 2 |

---

## Data Baseline

### Business A counts
| Entity | Count |
|--------|-------|
| Jobs | [N] |
| Customers | [N] |
| Campaigns | [N] |

### Business B counts
| Entity | Count |
|--------|-------|
| Jobs | [N] |
| Customers | [N] |
| Campaigns | [N] |

---

## MULTI-01: Switcher shows all businesses

**Status:** [PASS/FAIL]

### Dropdown contents
| Option | Business Name | Active Indicator | Position |
|--------|--------------|-----------------|----------|
| 1 | [name] | [checkmark/none] | [top/bottom] |
| 2 | [name] | [checkmark/none] | [top/bottom] |
| Link | View all clients | -- | bottom |

### Evidence
- `qa-66-switcher-dropdown.png`

### Bugs found
- [none or list]

---

## MULTI-02: Switch updates all page data

**Status:** [PASS/FAIL]

### Switch to Business B: data verification
| Page | Expected (Business B) | Actual |
|------|----------------------|--------|
| Dashboard KPIs | ~0 / minimal | [actual] |
| Jobs count | 0 | [actual] |
| Campaigns visible | 1 (Standard copy) | [actual campaign names] |

### Switch back to Business A: data verification
| Page | Expected (Business A) | Actual |
|------|----------------------|--------|
| Dashboard KPIs | Non-zero | [actual] |
| Jobs count | 7+ | [actual] |
| Campaigns visible | 2+ (HVAC Follow-up, Standard Follow-Up) | [actual campaign names] |

### "View all clients" link
- Navigated to: [/businesses or actual URL]
- Correct: [Yes/No]

### Evidence
- `qa-66-switched-to-plumbing.png`
- `qa-66-dashboard-business-b.png`
- `qa-66-jobs-business-b.png`
- `qa-66-campaigns-business-b.png`
- `qa-66-dashboard-business-a-restored.png`

### Bugs found
- [none or list]

---

## MULTI-03: Mobile switcher functional

**Status:** [pending -- tested in Task 2]

---

## Bugs Summary

| Bug ID | Severity | Requirement | Description |
|--------|----------|-------------|-------------|
| [BUG-MULTI-XX] | [Critical/High/Medium/Low] | [MULTI-XX] | [description] |

---

## Screenshots

| File | Description |
|------|-------------|
| qa-66-switcher-dropdown.png | Desktop switcher dropdown with both businesses |
| qa-66-switched-to-plumbing.png | Sidebar after switching to Business B |
| qa-66-dashboard-business-b.png | Dashboard showing Business B data |
| qa-66-jobs-business-b.png | Jobs page for Business B (empty) |
| qa-66-campaigns-business-b.png | Campaigns page for Business B |
| qa-66-dashboard-business-a-restored.png | Dashboard after switching back to A |
```

Fill in all [placeholders] with actual values from the test results.
  </action>
  <verification>
- docs/qa-v3.1/66-switcher.md exists with MULTI-01 and MULTI-02 results filled in (MULTI-03 pending)
- Screenshots exist at project root for all switcher tests
- Data refresh verified across dashboard, jobs, and campaigns pages
- Active business is "Audit Test HVAC" at end of task
  </verification>
  <done>MULTI-01 and MULTI-02 each have PASS/FAIL with evidence. Switcher dropdown shows both businesses. Data refresh verified across 3 pages in both directions (A->B and B->A). Findings document initialized.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile business switcher (MULTI-03) and complete findings</name>
  <files>docs/qa-v3.1/66-switcher.md</files>
  <action>
This task tests MULTI-03 (mobile business switcher). Requires resizing the viewport to mobile width.

**Step-by-step walkthrough:**

**Part A: Set mobile viewport**

1. The browser should still be logged in. If not, login first.

2. **Resize viewport to mobile (375px width):**
   Use `browser_resize` or create a new browser context with viewport `{ width: 375, height: 844 }`.
   If using an existing page, resize the window.
   `browser_navigate` to `http://localhost:3000/dashboard` -- force a full load at mobile width

3. `browser_wait_for` -- Wait for dashboard to render at mobile size
4. `browser_snapshot` -- Record:
   - Desktop sidebar should be HIDDEN (md:hidden logic)
   - Mobile header should be visible at the top
   - Business name should appear in the mobile header (PageHeader component)
   - Bottom navigation should be visible
5. `browser_take_screenshot` -- filename: `qa-66-mobile-dashboard.png`

**Part B: Test mobile switcher (MULTI-03)**

6. **Find the mobile business switcher:** Look in the mobile header area for the business name or switcher trigger.
   `browser_snapshot` -- Identify the clickable business name/switcher element in the header.

7. **Open the mobile switcher dropdown:** Click/tap the business name or switcher trigger in the mobile header.
   `browser_wait_for` -- Wait for dropdown to appear
   `browser_snapshot` -- Record:
   - Both business names visible
   - Active indicator (checkmark) on the current business
   - Menu items are large enough to tap (touch-friendly)
   `browser_take_screenshot` -- filename: `qa-66-mobile-switcher-open.png`

8. **Switch to the other business:** Click "AUDIT_ Test Plumbing" (or "Audit Test HVAC" if B is currently active).
   `browser_wait_for` -- Wait for switch to complete (toast + revalidation, 2-3 seconds)
   `browser_snapshot` -- Verify the mobile header now shows the switched-to business name
   `browser_take_screenshot` -- filename: `qa-66-mobile-switcher-switched.png`

9. **Switch back to Business A:** Open the mobile switcher again and select "Audit Test HVAC".
   `browser_wait_for` -- Wait for switch to complete

10. Record MULTI-03: PASS if:
    - Mobile switcher was accessible (visible in mobile header)
    - Dropdown opened and showed both businesses
    - Tapping a different business successfully switched
    - Business name updated in the header after switch
    FAIL if switcher was not visible at mobile width, dropdown didn't open, or switch didn't work.

**Part C: Restore desktop viewport**

11. Resize viewport back to desktop (1440x900) or close mobile context.

**Part D: Update findings document**

12. Update `docs/qa-v3.1/66-switcher.md`:
    - Fill in MULTI-03 results
    - Update summary table with all 3 MULTI results
    - Add mobile screenshots to the screenshots table
    - Add any new bugs found

Add the MULTI-03 section:
```markdown
## MULTI-03: Mobile switcher functional

**Status:** [PASS/FAIL]

### Mobile viewport (375x844)
| Check | Expected | Actual |
|-------|----------|--------|
| Desktop sidebar hidden | Yes | [actual] |
| Mobile header visible | Yes | [actual] |
| Business name in header | Yes | [actual] |
| Switcher dropdown opens | Yes | [actual] |
| Both businesses listed | Yes | [actual] |
| Switch works | Yes | [actual] |
| Header updates after switch | Yes | [actual] |

### Evidence
- `qa-66-mobile-dashboard.png`
- `qa-66-mobile-switcher-open.png`
- `qa-66-mobile-switcher-switched.png`

### Bugs found
- [none or list]
```

Add mobile screenshots to the screenshots table:
```markdown
| qa-66-mobile-dashboard.png | Mobile dashboard at 375px |
| qa-66-mobile-switcher-open.png | Mobile switcher dropdown open |
| qa-66-mobile-switcher-switched.png | Mobile after switching business |
```

Ensure the summary table shows all 3 MULTI requirements with PASS/FAIL status.
  </action>
  <verification>
- docs/qa-v3.1/66-switcher.md has all 3 MULTI requirements (MULTI-01, MULTI-02, MULTI-03) with PASS/FAIL status
- Screenshots qa-66-mobile-dashboard.png, qa-66-mobile-switcher-open.png, qa-66-mobile-switcher-switched.png exist
- Active business restored to "Audit Test HVAC" at end of task
- Viewport restored to desktop (1440x900)
  </verification>
  <done>MULTI-01, MULTI-02, MULTI-03 all have PASS/FAIL status with evidence. Mobile switcher tested at 375px. Findings document complete.</done>
</task>

</tasks>

<verification>
- All 3 MULTI requirements (MULTI-01, MULTI-02, MULTI-03) have PASS/FAIL verdicts in docs/qa-v3.1/66-switcher.md
- Switcher dropdown verified to show both businesses with correct active indicator
- Data refresh verified across dashboard, jobs, and campaigns for both switch directions
- Mobile switcher tested at 375px viewport with functional dropdown and switch
- Active business is "Audit Test HVAC" at end of plan
- All screenshots saved to project root
</verification>

<success_criteria>
- MULTI-01 through MULTI-03 each assessed
- Desktop switcher dropdown shows all businesses with active indicator
- Switch updates page data across dashboard, jobs, campaigns (verified in both directions)
- Mobile switcher accessible and functional at 375px
- Findings document complete with evidence
</success_criteria>

<output>
After completion, create `.planning/phases/66-businesses-page-data-isolation/66-02-SUMMARY.md`
</output>
