---
phase: 59-auth-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/qa-v3.1/59-auth-flows.md]
autonomous: true

must_haves:
  truths:
    - "Login with audit-test@avisloop.com / AuditTest123! lands on dashboard with business data visible"
    - "Signup form submits and redirects to /verify-email with confirmation message"
    - "Forgot-password form shows success state with 'Check Your Email' heading"
    - "Update-password form renders with both fields and password strength checklist"
    - "Session persists across page refresh, back/forward navigation, and cross-route navigation"
    - "Empty login form shows 'Email is required' and 'Password is required' errors"
    - "Wrong credentials show 'Invalid login credentials' — not raw server error or blank page"
    - "Findings document docs/qa-v3.1/59-auth-flows.md exists with pass/fail per requirement"
  artifacts:
    - path: "docs/qa-v3.1/59-auth-flows.md"
      provides: "Auth flows QA findings with pass/fail, screenshots, and bugs"
      min_lines: 50
  key_links: []
---

<objective>
Execute the Phase 59 QA E2E audit of all authentication flows: login, signup, password reset, session durability, and error handling.

Purpose: Establish that all auth paths are functional before any other QA audit phase begins. Auth is the gate — if login fails, nothing else can be tested.

Output: `docs/qa-v3.1/59-auth-flows.md` — a findings document with pass/fail per requirement (AUTH-01 through AUTH-05), screenshot evidence references, and any bugs discovered.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-auth-flows/59-RESEARCH.md
@docs/PROJECT_STATE.md

Test account: audit-test@avisloop.com / AuditTest123!
App URL: http://localhost:3000
Output file: docs/qa-v3.1/59-auth-flows.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUTH-01 + AUTH-04 — Login flow and session durability</name>
  <files>docs/qa-v3.1/59-auth-flows.md</files>
  <action>
This task tests two tightly coupled requirements: successful login (AUTH-01) and session durability (AUTH-04). Session tests depend on a valid login, so combining them avoids redundant login cycles.

**IMPORTANT:** The dev server must be running (`pnpm dev`). Before starting Playwright steps, verify with a quick `browser_navigate` to `http://localhost:3000`. If the server is not running, start it in the background first.

**Step-by-step Playwright MCP walkthrough:**

**Part A: Login flow (AUTH-01)**

1. `browser_navigate` to `http://localhost:3000/auth/login`
2. `browser_snapshot` — Verify page structure:
   - Heading "Welcome back" is present
   - Email input labeled "Email" exists
   - Password input labeled "Password" exists
   - Button "Login" exists
   - Link "Forgot your password?" exists
   - Link "Sign up" exists
   - "Continue with Google" button exists
   - Record all findings
3. `browser_take_screenshot` — filename: `qa-59-login-page-desktop.png`
4. `browser_fill_form` — Fill both fields:
   - Email (textbox, label "Email"): "audit-test@avisloop.com"
   - Password (textbox, label "Password"): "AuditTest123!"
5. `browser_click` — Click the "Login" button
6. `browser_wait_for` — Wait for text "Dashboard" OR any dashboard content (KPI heading, business name). Max 10 seconds.
7. `browser_snapshot` — Verify dashboard landed:
   - URL should contain `/dashboard`
   - Business name should be visible somewhere (sidebar or header)
   - KPI widgets should be rendering (look for numeric values or "Reviews", "Rating", "Conversion" text)
   - No error messages, no blank page
   - Record the business name observed
8. `browser_take_screenshot` — filename: `qa-59-dashboard-after-login.png`
9. Record AUTH-01 result: PASS if dashboard with business data is visible, FAIL otherwise with details.

**Part B: Session durability (AUTH-04)**

Starting from the dashboard after successful login in Part A:

10. **Refresh test:** `browser_navigate` to `http://localhost:3000/dashboard` (simulates page refresh)
11. `browser_wait_for` — Wait for dashboard content to appear (not "Login" or "Welcome back")
12. `browser_snapshot` — Verify still on dashboard, data visible (not redirected to login)
13. `browser_take_screenshot` — filename: `qa-59-session-after-refresh.png`

14. **Cross-route test 1:** `browser_navigate` to `http://localhost:3000/jobs`
15. `browser_snapshot` — Verify jobs page loaded (look for "Jobs" heading or job-related content, not login redirect)
16. `browser_take_screenshot` — filename: `qa-59-session-jobs-page.png`

17. **Back navigation:** `browser_navigate_back`
18. `browser_snapshot` — Verify still authenticated (on dashboard, not login page)

19. **Cross-route test 2:** `browser_navigate` to `http://localhost:3000/settings`
20. `browser_snapshot` — Verify settings page loaded (not login redirect)

21. **Cross-route test 3:** `browser_navigate` to `http://localhost:3000/campaigns`
22. `browser_snapshot` — Verify campaigns page loaded (not login redirect)
23. `browser_take_screenshot` — filename: `qa-59-session-campaigns-page.png`

24. Record AUTH-04 result: PASS if session persisted across all navigation tests, FAIL if any redirect to login occurred.

**Part C: Responsive checks for login page**

25. Sign out: `browser_navigate` to `http://localhost:3000` then find and click sign-out/logout (or navigate to an auth action URL). Alternatively, `browser_navigate` to `http://localhost:3000/auth/login` — if still authenticated, middleware redirects to dashboard. In that case, look for a sign-out mechanism in the dashboard sidebar (user menu at bottom).
    - Approach: `browser_snapshot` on dashboard, find sign-out button/link, click it, verify redirect to login.

26. **Tablet viewport:** `browser_resize` to 768x1024
27. `browser_navigate` to `http://localhost:3000/auth/login`
28. `browser_snapshot` — Verify form renders correctly in single-column layout (right gradient panel should be hidden below lg breakpoint)
29. `browser_take_screenshot` — filename: `qa-59-login-tablet.png`

30. **Mobile viewport:** `browser_resize` to 390x844
31. `browser_navigate` to `http://localhost:3000/auth/login` (may already be there)
32. `browser_snapshot` — Verify form is usable, inputs are full-width, no overflow
33. `browser_take_screenshot` — filename: `qa-59-login-mobile.png`

34. **Reset to desktop:** `browser_resize` to 1440x900

35. **Dark theme check:** Navigate to login page, check if there is a theme toggle. If accessible, switch to dark theme, take screenshot.
36. `browser_take_screenshot` — filename: `qa-59-login-dark-theme.png` (only if dark mode is toggleable from login page; skip if not)

Record viewport/theme findings in the output file.

**Part D: Initialize the findings document**

37. Create `docs/qa-v3.1/59-auth-flows.md` with the initial structure:

```markdown
# Phase 59: Auth Flows — QA Findings

**Tested:** 2026-02-27
**Tester:** Claude (Playwright MCP)
**App URL:** http://localhost:3000
**Test account:** audit-test@avisloop.com

---

## Summary

| Requirement | Status | Notes |
|-------------|--------|-------|
| AUTH-01: Login → dashboard | [PASS/FAIL] | [notes] |
| AUTH-02: Signup → verify-email | [pending] | |
| AUTH-03: Password reset flow | [pending] | |
| AUTH-04: Session durability | [PASS/FAIL] | [notes] |
| AUTH-05: Error messages | [pending] | |

---

## AUTH-01: Login with email/password

**Status:** [PASS/FAIL]

### Test steps
1. Navigated to /auth/login
2. Verified page structure: [details]
3. Filled credentials: audit-test@avisloop.com
4. Submitted login form
5. Result: [what happened]

### Evidence
- `qa-59-login-page-desktop.png` — Login page structure
- `qa-59-dashboard-after-login.png` — Dashboard after login

### Business data observed
- Business name: [observed name]
- KPIs visible: [yes/no, which ones]

### Bugs found
- [none or list]

---

## AUTH-04: Session durability

**Status:** [PASS/FAIL]

### Test steps
1. Page refresh: [result]
2. Navigate to /jobs: [result]
3. Browser back: [result]
4. Navigate to /settings: [result]
5. Navigate to /campaigns: [result]

### Evidence
- `qa-59-session-after-refresh.png`
- `qa-59-session-jobs-page.png`
- `qa-59-session-campaigns-page.png`

### Bugs found
- [none or list]

---

## Viewport & Theme

### Login page responsive
| Viewport | Status | Notes |
|----------|--------|-------|
| Desktop (1440x900) | [result] | |
| Tablet (768x1024) | [result] | |
| Mobile (390x844) | [result] | |

### Dark theme
- [result or "not testable from login page"]

---
```

Fill in all [placeholder] values with actual observed results. Leave AUTH-02, AUTH-03, AUTH-05 sections as stubs (filled by Task 2).
  </action>
  <verify>
1. File `docs/qa-v3.1/59-auth-flows.md` exists with AUTH-01 and AUTH-04 sections filled in
2. Screenshots captured: qa-59-login-page-desktop.png, qa-59-dashboard-after-login.png, qa-59-session-after-refresh.png, qa-59-session-jobs-page.png, qa-59-session-campaigns-page.png, qa-59-login-tablet.png, qa-59-login-mobile.png
3. AUTH-01 has a clear PASS or FAIL with evidence
4. AUTH-04 has a clear PASS or FAIL with evidence for each navigation test
  </verify>
  <done>
- AUTH-01 is marked PASS or FAIL with observed business name and KPI data
- AUTH-04 is marked PASS or FAIL with results for refresh, back, forward, and 3 cross-route navigations
- Viewport/theme section filled with tablet, mobile, and dark theme results
- All screenshots captured and referenced in findings document
  </done>
</task>

<task type="auto">
  <name>Task 2: AUTH-02, AUTH-03, AUTH-05 — Signup, password reset, and error messages</name>
  <files>docs/qa-v3.1/59-auth-flows.md</files>
  <action>
This task tests the remaining three requirements. Must be executed AFTER Task 1 because Task 1 establishes the findings document structure.

**IMPORTANT:** Start this task signed out. If still logged in from Task 1, sign out first. The executor should be on the login page or a public page with no active session.

**Step-by-step Playwright MCP walkthrough:**

**Part A: Error messages (AUTH-05) — test FIRST while signed out**

1. `browser_resize` to 1440x900 (ensure desktop viewport)
2. `browser_navigate` to `http://localhost:3000/auth/login`
3. `browser_snapshot` — Confirm on login page, no active session

**Test 1: Empty form submission**
4. `browser_click` — Click the "Login" button without entering anything
5. `browser_snapshot` — Look for validation errors:
   - Expect "Email is required" near email field
   - Expect "Password is required" near password field
   - Verify errors have `role="alert"` or are visually styled as errors (red text)
   - Verify NO raw JSON, no stack traces, no "undefined" text
6. `browser_take_screenshot` — filename: `qa-59-login-empty-errors.png`

**Test 2: Invalid email format**
7. `browser_navigate` to `http://localhost:3000/auth/login` (fresh state)
8. `browser_snapshot` — Get refs
9. `browser_type` — Type "notanemail" into the Email field
10. `browser_type` — Type "SomePassword1!" into the Password field
11. `browser_click` — Click "Login" button
12. `browser_snapshot` — Look for "Please enter a valid email address" error
   - If HTML5 validation catches it first (input type="email"), note that as the validation mechanism
13. `browser_take_screenshot` — filename: `qa-59-login-invalid-email.png`

**Test 3: Wrong credentials (valid format, wrong values)**
14. `browser_navigate` to `http://localhost:3000/auth/login` (fresh state)
15. `browser_snapshot` — Get refs
16. `browser_fill_form`:
   - Email: "wrong-user@nonexistent.com"
   - Password: "WrongPassword1!"
17. `browser_click` — Click "Login" button
18. `browser_wait_for` — Wait for error text (expect "Invalid login credentials")
19. `browser_snapshot` — Verify:
   - Error message is human-readable ("Invalid login credentials")
   - NOT a raw error object or stack trace
   - Error is visually styled (red/error color)
   - Error is dismissible (if the form supports click-to-dismiss)
20. `browser_take_screenshot` — filename: `qa-59-login-wrong-credentials.png`

**Test 4: Error dismissal (if supported)**
21. If an error is visible from Test 3, attempt to dismiss it:
   - `browser_snapshot` — Look for a dismiss button or clickable error element
   - If found, click it and verify error disappears
   - If not found, note "error is not dismissible" as a finding

**Test 5: Signup form validation**
22. `browser_navigate` to `http://localhost:3000/auth/sign-up`
23. `browser_snapshot` — Verify page structure:
   - Heading "Create an account" present
   - Full Name field (labeled "Full Name (optional)")
   - Email field
   - Password field with strength checklist
   - "Sign up" button
   - Google OAuth button
24. `browser_take_screenshot` — filename: `qa-59-signup-page-desktop.png`

25. **Password strength checklist test:** Type a weak password to verify checklist behavior
26. `browser_type` — Type "abc" into the Password field
27. `browser_snapshot` — Check the password strength checklist:
   - "At least 8 characters" should show X/red (unmet)
   - "One uppercase letter" should show X/red (unmet)
   - "One number" should show X/red (unmet)
   - "One special character" should show X/red (unmet)
28. `browser_take_screenshot` — filename: `qa-59-signup-weak-password.png`

29. Now type a strong password to see all checks turn green:
   - Clear the password field first, then type "AuditTest123!"
   - Or: `browser_navigate` to `/auth/sign-up` again, fill password field with "StrongPass1!"
30. `browser_snapshot` — Verify all 4 checklist items show green check marks
31. `browser_take_screenshot` — filename: `qa-59-signup-strong-password.png`

32. Record AUTH-05 result: PASS if all error scenarios show clear, human-readable messages without raw errors. FAIL if any scenario shows raw JSON, stack traces, blank errors, or no error at all.

**Part B: Signup flow (AUTH-02)**

33. `browser_navigate` to `http://localhost:3000/auth/sign-up` (fresh state)
34. `browser_snapshot` — Get refs
35. Generate a unique test email using current timestamp approach. Use: `audit-signup-test-{use current timestamp}@test.com`
   - The executor should generate the email string in advance, e.g., "audit-signup-test-1709049600@test.com"
36. `browser_fill_form`:
   - Full Name: "AUDIT_SignupTest"
   - Email: [generated email from step 35]
   - Password: "AuditTest123!"
37. Verify password checklist shows all green (from Part A testing, we know "AuditTest123!" passes all rules)
38. `browser_click` — Click "Sign up" button
39. `browser_wait_for` — Wait for either:
   - Text "Check your email" (expected: redirect to /verify-email)
   - Text "Create an account" still visible (form didn't submit — check for errors)
   - Text "Dashboard" (if auto-confirm is enabled in dev mode)
   - Max 15 seconds
40. `browser_snapshot` — Document what happened:
   - If redirected to `/verify-email`: AUTH-02 PASS (signup submitted successfully, email verification pending)
   - If redirected to `/dashboard` then `/onboarding`: AUTH-02 PASS (auto-confirm enabled, full flow works)
   - If error message appears: AUTH-02 FAIL (record the error)
   - If "User already registered" appears: Note this, try a different email
41. `browser_take_screenshot` — filename: `qa-59-signup-result.png`

42. **DB verification** (optional but valuable): Use `mcp__supabase__execute_sql` to verify the user was created:
```sql
SELECT id, email, created_at, email_confirmed_at
FROM auth.users
WHERE email LIKE 'audit-signup-test-%@test.com'
ORDER BY created_at DESC
LIMIT 1;
```
   - If row exists: confirms signup reached the database
   - If `email_confirmed_at` is NOT NULL: auto-confirm is enabled
   - If `email_confirmed_at` IS NULL: email verification is required

43. Record AUTH-02 result with the specific behavior observed.

44. **Cleanup note:** Record in the findings that the test user was created and may need cleanup. Do NOT delete it — it serves as evidence.

**Part C: Password reset flow (AUTH-03)**

45. `browser_navigate` to `http://localhost:3000/auth/forgot-password`
46. `browser_snapshot` — Verify page structure:
   - Heading "Reset Your Password" present
   - Email input field present
   - "Send reset email" button present
   - Link back to login present
47. `browser_take_screenshot` — filename: `qa-59-forgot-password-page.png`

48. `browser_type` — Type "audit-test@avisloop.com" into the Email field
49. `browser_click` — Click "Send reset email" button
50. `browser_wait_for` — Wait for text "Check Your Email" (success state heading)
51. `browser_snapshot` — Verify success state:
   - Heading changed to "Check Your Email"
   - Description mentions "Password reset instructions sent" or similar
   - No error messages
52. `browser_take_screenshot` — filename: `qa-59-forgot-password-success.png`

53. Record partial AUTH-03 result: PASS for form submission if success state shown. Note limitation: "Email delivery and actual password reset cannot be verified via Playwright."

54. **Test the update-password form rendering** (separate from the email flow):
55. `browser_navigate` to `http://localhost:3000/auth/update-password`
56. `browser_snapshot` — Verify page structure:
   - Heading present (likely "Reset Your Password" or similar)
   - New password field with strength checklist
   - Confirm password field
   - "Save new password" button
   - Note: This form may or may not work without a recovery session. Document what renders.
57. `browser_take_screenshot` — filename: `qa-59-update-password-page.png`

58. **Test update-password validation:**
59. Type mismatched passwords:
   - `browser_type` — "NewPassword1!" into new password field
   - `browser_type` — "DifferentPassword1!" into confirm password field
60. `browser_click` — Click "Save new password"
61. `browser_snapshot` — Look for "Passwords do not match" error
62. `browser_take_screenshot` — filename: `qa-59-update-password-mismatch.png`

63. Record AUTH-03 update-password form results. Final AUTH-03 status should note:
   - Forgot-password form: PASS/FAIL
   - Update-password form rendering: PASS/FAIL
   - Limitation: "Full end-to-end password reset (email click → new password saved) not testable — requires email delivery"

**Part D: Signup page responsive checks**

64. `browser_resize` to 768x1024 (tablet)
65. `browser_navigate` to `http://localhost:3000/auth/sign-up`
66. `browser_snapshot` — Verify single-column layout
67. `browser_take_screenshot` — filename: `qa-59-signup-tablet.png`

68. `browser_resize` to 390x844 (mobile)
69. `browser_snapshot` — Verify form is usable on mobile
70. `browser_take_screenshot` — filename: `qa-59-signup-mobile.png`

71. `browser_resize` to 1440x900 (reset to desktop)

**Part E: Google OAuth button presence check**

72. `browser_navigate` to `http://localhost:3000/auth/login`
73. `browser_snapshot` — Verify "Continue with Google" button is present
74. Record: "Google OAuth button present: YES/NO". Do NOT click it — Google consent screen is not automatable and clicking may redirect to an error page in dev.

**Part F: Finalize findings document**

75. Update `docs/qa-v3.1/59-auth-flows.md` — Append the AUTH-02, AUTH-03, and AUTH-05 sections:

```markdown
## AUTH-05: Invalid credentials error messages

**Status:** [PASS/FAIL]

### Test results
| Scenario | Expected Error | Actual Result | Status |
|----------|---------------|---------------|--------|
| Empty form submit | "Email is required" + "Password is required" | [actual] | [P/F] |
| Invalid email format | "Please enter a valid email address" | [actual] | [P/F] |
| Wrong credentials | "Invalid login credentials" | [actual] | [P/F] |
| Error dismissal | Error disappears on click/re-submit | [actual] | [P/F] |

### Signup validation
| Scenario | Expected | Actual | Status |
|----------|----------|--------|--------|
| Weak password checklist | 4 X marks for "abc" | [actual] | [P/F] |
| Strong password checklist | 4 green checks for "AuditTest123!" | [actual] | [P/F] |

### Evidence
- `qa-59-login-empty-errors.png`
- `qa-59-login-invalid-email.png`
- `qa-59-login-wrong-credentials.png`
- `qa-59-signup-weak-password.png`
- `qa-59-signup-strong-password.png`

### Bugs found
- [none or list]

---

## AUTH-02: Signup flow

**Status:** [PASS/FAIL — with qualification]

### Test steps
1. Navigated to /auth/sign-up
2. Filled: AUDIT_SignupTest / [email] / AuditTest123!
3. Submitted form
4. Result: [redirect to /verify-email | redirect to /onboarding | error]

### DB verification
- User created in auth.users: [YES/NO]
- Email confirmed: [YES/NO/not checked]
- Auto-confirm behavior: [enabled/disabled/unknown]

### Limitations
- Email delivery not testable via Playwright
- If email verification required, cannot test onboarding redirect

### Evidence
- `qa-59-signup-page-desktop.png`
- `qa-59-signup-result.png`

### Cleanup needed
- Test user created: [email] — may need manual deletion

### Bugs found
- [none or list]

---

## AUTH-03: Password reset flow

**Status:** [PARTIAL PASS/FAIL]

### Forgot password form
- Form renders correctly: [YES/NO]
- Submit with valid email: [success state shown / error]
- Success state shows "Check Your Email": [YES/NO]

### Update password form
- Form renders at /auth/update-password: [YES/NO]
- Password strength checklist present: [YES/NO]
- Confirm password field present: [YES/NO]
- Mismatch validation works: [YES/NO — "Passwords do not match" shown]

### Limitations
- Cannot test: email delivery, clicking reset link, actually saving new password
- Reason: Email is external to Playwright; recovery session requires email link

### Evidence
- `qa-59-forgot-password-page.png`
- `qa-59-forgot-password-success.png`
- `qa-59-update-password-page.png`
- `qa-59-update-password-mismatch.png`

### Bugs found
- [none or list]

---

## Responsive & Accessibility

### Signup page responsive
| Viewport | Status | Notes |
|----------|--------|-------|
| Desktop (1440x900) | [result] | |
| Tablet (768x1024) | [result] | |
| Mobile (390x844) | [result] | |

### Google OAuth
- Button present on login: [YES/NO]
- Button present on signup: [YES/NO]
- Not tested (Google consent screen not automatable)

---

## Overall Assessment

[Summary paragraph: how many requirements passed, any critical bugs, any limitations, readiness for Phase 60]
```

Fill in ALL placeholders with actual observed results. Update the Summary table at the top with final statuses for all 5 requirements.
  </action>
  <verify>
1. `docs/qa-v3.1/59-auth-flows.md` has all 5 requirement sections filled (AUTH-01 through AUTH-05)
2. Summary table at top shows status for all 5 requirements
3. Screenshots captured: qa-59-login-empty-errors.png, qa-59-login-invalid-email.png, qa-59-login-wrong-credentials.png, qa-59-signup-page-desktop.png, qa-59-signup-weak-password.png, qa-59-signup-strong-password.png, qa-59-signup-result.png, qa-59-forgot-password-page.png, qa-59-forgot-password-success.png, qa-59-update-password-page.png, qa-59-update-password-mismatch.png, qa-59-signup-tablet.png, qa-59-signup-mobile.png
4. No placeholder text remains — all [brackets] replaced with actual findings
5. Overall Assessment section provides a clear readiness statement for Phase 60
  </verify>
  <done>
- AUTH-02 marked PASS or FAIL with signup behavior documented (redirect target, DB verification)
- AUTH-03 marked PARTIAL PASS with both forms tested and limitations clearly documented
- AUTH-05 marked PASS or FAIL with all 4 error scenarios tested and results tabulated
- Responsive checks completed for signup page (tablet + mobile)
- Google OAuth button presence confirmed
- Overall Assessment written with readiness statement
- All screenshot evidence captured and referenced
- Findings document is complete with no remaining placeholders
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Read `docs/qa-v3.1/59-auth-flows.md` in full
2. Confirm the Summary table has all 5 requirements with PASS/FAIL/PARTIAL status
3. Confirm at least 15 screenshots were captured (login desktop, dashboard, session x3, login tablet, login mobile, login errors x3, signup desktop, signup passwords x2, signup result, forgot-password x2, update-password x2, signup responsive x2)
4. Confirm no placeholder text remains in the document
5. Confirm the Overall Assessment section exists and states readiness for Phase 60
6. If any requirement is FAIL, confirm the bug is clearly described with reproduction steps
</verification>

<success_criteria>
- All 5 auth requirements (AUTH-01 through AUTH-05) have been tested and have a documented status
- The findings document `docs/qa-v3.1/59-auth-flows.md` is complete with evidence
- At minimum: AUTH-01 (login) and AUTH-04 (session) must PASS for Phase 60 to proceed
- AUTH-02 and AUTH-03 may be PARTIAL PASS due to email delivery limitations — this is expected and acceptable
- AUTH-05 must show that errors are human-readable (not raw server errors)
- Any bugs found are recorded with clear reproduction steps
</success_criteria>

<output>
After completion, create `.planning/phases/59-auth-flows/59-01-SUMMARY.md`
</output>
