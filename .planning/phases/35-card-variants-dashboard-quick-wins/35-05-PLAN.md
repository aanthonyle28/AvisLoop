---
phase: 35-card-variants-dashboard-quick-wins
plan: 05
type: execute
wave: 2
depends_on: ["35-02"]
files_modified:
  - components/dashboard/kpi-widgets.tsx
  - components/dashboard/attention-alerts.tsx
  - components/dashboard/ready-to-send-queue.tsx
  - components/customers/customer-detail-drawer.tsx
  - components/onboarding/setup-progress-pill.tsx
  - components/onboarding/setup-progress-drawer.tsx
  - components/onboarding/steps/send-step.tsx
  - components/onboarding/steps/customer-import-step.tsx
  - components/onboarding/steps/sms-consent-step.tsx
  - components/onboarding/steps/software-used-step.tsx
  - components/review/thank-you-card.tsx
  - components/layout/notification-bell.tsx
  - components/customers/csv-import-dialog.tsx
  - components/customers/csv-preview-table.tsx
  - components/jobs/csv-job-import-dialog.tsx
  - components/jobs/job-columns.tsx
  - components/templates/template-list-item.tsx
  - app/(dashboard)/analytics/page.tsx
  - app/(dashboard)/jobs/page.tsx
  - app/(dashboard)/campaigns/page.tsx
  - app/(dashboard)/customers/page.tsx
  - app/(dashboard)/history/page.tsx
  - app/(dashboard)/feedback/page.tsx
  - app/(dashboard)/billing/page.tsx
  - app/(dashboard)/send/page.tsx
  - app/(dashboard)/campaigns/new/page.tsx
  - app/(dashboard)/billing/page.tsx
  - app/(dashboard)/campaigns/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Status badges/indicators use text-success and text-destructive instead of hardcoded green/red Tailwind classes"
    - "SMS consent status colors use text-success, text-destructive, text-warning instead of green/red/amber"
    - "Success callouts use bg-success-bg, border-success-border, text-success instead of hardcoded green"
    - "Info callouts use bg-info-bg, border-info-border, text-info instead of hardcoded blue"
    - "CSV import result colors use text-success, text-warning, text-destructive"
    - "All dashboard pages use consistent container padding and space-y-6 spacing"
  artifacts:
    - path: "components/dashboard/attention-alerts.tsx"
      provides: "Alert icons using text-destructive, text-warning, text-info, text-success"
    - path: "components/onboarding/steps/send-step.tsx"
      provides: "3-state banner using success/warning/destructive tokens"
  key_links:
    - from: "components/dashboard/kpi-widgets.tsx"
      to: "tailwind.config.ts"
      via: "text-success and text-destructive semantic classes"
      pattern: "text-success|text-destructive"
---

<objective>
Replace hardcoded Tailwind color-scale classes with semantic tokens across Categories C (status badges), D (SMS consent), F (success/info callouts), L (onboarding completion), N (CSV import), P (template badges), plus 3 app-level files. Also normalize page padding across all dashboard pages to consistent `container py-6 space-y-6`.

Purpose: Completes the 210-occurrence batch replacement and normalizes page spacing, making the design system fully token-driven and visually consistent.
Output: ~17 component files and ~10 page files updated.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/33-hardcoded-color-audit/TIER2-COLOR-AUDIT.md (canonical reference — Categories C, D, F, L, N, P)
@.planning/phases/35-card-variants-dashboard-quick-wins/35-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Categories C, D, F, L (status/consent/success/onboarding)</name>
  <files>
    components/dashboard/kpi-widgets.tsx,
    components/dashboard/attention-alerts.tsx,
    components/dashboard/ready-to-send-queue.tsx,
    components/customers/customer-detail-drawer.tsx,
    components/customers/edit-customer-sheet.tsx,
    components/onboarding/setup-progress-pill.tsx,
    components/onboarding/setup-progress-drawer.tsx,
    components/onboarding/steps/send-step.tsx,
    components/onboarding/steps/customer-import-step.tsx,
    components/onboarding/steps/review-destination-step.tsx,
    components/onboarding/steps/sms-consent-step.tsx,
    components/onboarding/steps/software-used-step.tsx,
    components/review/thank-you-card.tsx,
    components/layout/notification-bell.tsx,
    components/customers/add-customer-sheet.tsx
  </files>
  <action>
    Read each file and apply the following replacement mappings. Remove `dark:` overrides where semantic tokens handle dark mode automatically.

    **Category C: Status Badges / Trend Indicators — Replacement Map:**

    kpi-widgets.tsx (trend indicator ternary, lines 31-33):
    | Current | Replace With |
    |---------|-------------|
    | `text-green-600 dark:text-green-400` (positive trend) | `text-success` |
    | `text-red-600 dark:text-red-400` (negative trend) | `text-destructive` |

    attention-alerts.tsx (4-case switch, lines 27-31 — replace atomically):
    | Current | Replace With | Alert Type |
    |---------|-------------|------------|
    | `text-red-600 dark:text-red-400` | `text-destructive` | error |
    | `text-yellow-600 dark:text-yellow-400` | `text-warning` | warning |
    | `text-blue-600 dark:text-blue-400` | `text-info` | info |
    | `text-green-600 dark:text-green-400` | `text-success` | all-clear |

    ready-to-send-queue.tsx:
    | Current | Replace With |
    |---------|-------------|
    | `text-green-500` (checkmark) | `text-success` |
    | `text-yellow-500` (clock) | `text-warning` |

    job-columns.tsx:
    | Current | Replace With |
    |---------|-------------|
    | `bg-amber-100 text-amber-800` (scheduled badge) | `bg-warning-bg text-warning-foreground` |
    | `bg-emerald-100 text-emerald-800` (completed badge) | `bg-success-bg text-success-foreground` |
    | `text-emerald-600` (enrolled status) | `text-success` |
    | `text-amber-600` (not enrolled status) | `text-warning` |
    | Remove associated `dark:` overrides |

    **Category D: SMS Consent — Replacement Map (3-state, replace atomically):**

    customer-detail-drawer.tsx and edit-customer-sheet.tsx:
    | Current | Replace With | Status |
    |---------|-------------|--------|
    | `text-green-600 dark:text-green-400` | `text-success` | opted_in |
    | `text-red-600 dark:text-red-400` | `text-destructive` | opted_out |
    | `text-amber-600 dark:text-amber-400` | `text-warning` | unknown |

    Note: edit-customer-sheet.tsx may already have some Cat B replacements from Plan 04. Only replace the SMS consent colors here (they are in a different section of the file, around lines 139-163).

    **Category F: Success/Info Callouts — Replacement Map:**

    | Current | Replace With |
    |---------|-------------|
    | `bg-green-50` | `bg-success-bg` |
    | `border-green-200` | `border-success-border` |
    | `text-green-600` | `text-success` |
    | `text-green-700` | `text-success-foreground` |
    | `bg-green-100` (icon container) | `bg-success/10` |
    | `hover:bg-green-100` | `hover:bg-success/10` |
    | `bg-blue-50` | `bg-info-bg` |
    | `border-blue-200` | `border-info-border` |
    | `text-blue-600` | `text-info` |
    | `text-blue-900` | `text-info-foreground` |
    | `bg-blue-100` | `bg-info/10` |
    | Remove associated `dark:` overrides |

    Files: setup-progress-pill.tsx, setup-progress-drawer.tsx, thank-you-card.tsx, notification-bell.tsx (info/success items only — NOT the red badge which was handled in Phase 33), add-customer-sheet.tsx (campaign enrollment banner), review-destination-step.tsx (green checkmark), sms-consent-step.tsx (blue info box), software-used-step.tsx (blue info box).

    **Category L: Onboarding Completion — Replacement Map:**

    send-step.tsx (3-state: success/warning/error — replace ALL atomically):
    | Current | Replace With | State |
    |---------|-------------|-------|
    | `bg-green-100 text-green-600` | `bg-success/10 text-success` | success icon |
    | `bg-yellow-50 border-yellow-200` | `bg-warning-bg border-warning-border` | warning banner |
    | `text-yellow-600` / `text-yellow-700` / `text-yellow-800` | `text-warning` / `text-warning` / `text-warning-foreground` | warning text |
    | `bg-red-50 border-red-200 text-red-600` | `bg-destructive/10 border-destructive/20 text-destructive` | error banner |

    customer-import-step.tsx:
    | Current | Replace With |
    |---------|-------------|
    | `text-green-600` | `text-success` |
    | `bg-red-50` | `bg-destructive/10` |

    After all replacements, run `pnpm typecheck`.
  </action>
  <verify>
    `pnpm typecheck` passes.
    `grep -rn "text-green-[0-9]\|bg-green-[0-9]\|text-yellow-[0-9]\|bg-yellow-[0-9]" components/dashboard/kpi-widgets.tsx components/dashboard/attention-alerts.tsx components/onboarding/setup-progress-pill.tsx` returns 0 matches.
  </verify>
  <done>
    - KPI trend indicators use text-success / text-destructive
    - Attention alert icons use text-destructive / text-warning / text-info / text-success
    - SMS consent status uses text-success / text-destructive / text-warning
    - Success callouts use bg-success-bg, border-success-border, text-success
    - Info callouts use bg-info-bg, border-info-border, text-info
    - Onboarding completion states use semantic tokens
    - All dark: overrides removed where tokens handle dark mode
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Categories N, P, app-level files, and normalize page padding</name>
  <files>
    components/customers/csv-import-dialog.tsx,
    components/customers/csv-preview-table.tsx,
    components/jobs/csv-job-import-dialog.tsx,
    components/jobs/job-columns.tsx,
    components/templates/template-list-item.tsx,
    app/(dashboard)/analytics/page.tsx,
    app/(dashboard)/jobs/page.tsx,
    app/(dashboard)/campaigns/page.tsx,
    app/(dashboard)/customers/page.tsx,
    app/(dashboard)/history/page.tsx,
    app/(dashboard)/feedback/page.tsx,
    app/(dashboard)/billing/page.tsx,
    app/(dashboard)/send/page.tsx,
    app/(dashboard)/campaigns/new/page.tsx,
    app/(dashboard)/campaigns/[id]/page.tsx
  </files>
  <action>
    **PART 1: Category N (CSV Import Results) — Replacement Map:**

    csv-import-dialog.tsx:
    | Current | Replace With |
    |---------|-------------|
    | `text-green-600` (created count) | `text-success` |
    | `text-yellow-600` / `text-amber-600` (skipped/review) | `text-warning` |
    | `bg-amber-50 border-amber-200` (review callout) | `bg-warning-bg border-warning-border` |
    | `text-amber-700` / `text-amber-600` (callout text) | `text-warning-foreground` / `text-warning` |
    | Remove `dark:` overrides |

    csv-preview-table.tsx:
    | Current | Replace With |
    |---------|-------------|
    | `text-yellow-600` | `text-warning` |
    | `bg-yellow-50` | `bg-warning-bg` |
    | `text-green-600` | `text-success` |
    | `text-yellow-800` | `text-warning-foreground` |
    | `border-yellow-300` | `border-warning-border` |

    csv-job-import-dialog.tsx:
    | Current | Replace With |
    |---------|-------------|
    | `text-green-600` (success count) | `text-success` |
    | `text-red-600` (error count) | `text-destructive` |
    | `bg-red-50` (error details bg) | `bg-destructive/10` |
    | `text-blue-600` (new customers info) | `text-info` |
    | `text-amber-600` (warning/skipped) | `text-warning` |

    **PART 2: Category P (Template Channel Badge):**

    template-list-item.tsx:
    | Current | Replace With |
    |---------|-------------|
    | `bg-blue-100 text-blue-700 dark:bg-blue-950 dark:text-blue-300` (email) | `bg-info/10 text-info` |
    | `bg-green-100 text-green-700 dark:bg-green-950 dark:text-green-300` (SMS) | `bg-success/10 text-success` |

    **PART 3: App-Level Files (from TIER2-COLOR-AUDIT.md metadata section):**

    These 3 app-level files have inline hardcoded colors that should be tokenized:

    `app/(dashboard)/campaigns/new/page.tsx` (info tip box):
    | Current | Replace With |
    |---------|-------------|
    | `bg-blue-50 border-blue-200` | `bg-info-bg border-info-border` |
    | `text-blue-600` / `text-blue-700` | `text-info` |
    | `text-blue-900` | `text-info-foreground` |

    `app/(dashboard)/billing/page.tsx` (subscription success banner):
    | Current | Replace With |
    |---------|-------------|
    | `bg-green-50` | `bg-success-bg` |
    | `text-green-600` / `text-green-700` | `text-success` |
    | `text-green-800` | `text-success-foreground` |

    Note: `app/(dashboard)/settings/page.tsx` inline colors may have been handled in Plan 04 via settings-tabs.tsx. Check before replacing. If there are remaining `text-amber-600`, `border-red-200`, or `text-red-600` in the settings page, replace them here.

    **PART 4: Page Padding Normalization (DS-05, PG-02, PG-03, PG-04):**

    Normalize ALL dashboard page containers to consistent spacing. Read each page file and update the outermost `<div>` wrapper:

    **Standard pages** (full-width): `container py-6 space-y-6`
    | Page | Current | Target |
    |------|---------|--------|
    | `dashboard/page.tsx` | `container mx-auto py-6 px-4 space-y-6` | `container py-6 space-y-6` (remove redundant mx-auto and px-4) |
    | `analytics/page.tsx` | `container mx-auto py-6 px-4` | `container py-6 space-y-6` |
    | `jobs/page.tsx` | `container py-6` | `container py-6 space-y-6` |
    | `campaigns/page.tsx` | `container py-6 space-y-8` | `container py-6 space-y-6` |
    | `customers/page.tsx` | `container py-6` | `container py-6 space-y-6` |
    | `history/page.tsx` | `container py-6` | `container py-6 space-y-6` |
    | `send/page.tsx` | `container mx-auto py-6 px-4` | `container py-6 space-y-6` |

    **Constrained pages** (max-width): `container max-w-4xl py-6 space-y-6`
    | Page | Current | Target |
    |------|---------|--------|
    | `feedback/page.tsx` | `container py-8 max-w-4xl` | `container max-w-4xl py-6 space-y-6` |
    | `billing/page.tsx` | `container mx-auto py-6 px-4 max-w-4xl` | `container max-w-4xl py-6 space-y-6` |

    **Settings page:** LEAVE UNCHANGED — it has a unique sticky header layout that should not be normalized.

    **Sub-pages** (campaigns/new, campaigns/[id], campaigns/[id]/edit): Normalize to `container py-6 space-y-6` with their existing max-width constraints if any.

    IMPORTANT: The Tailwind `container` class already applies `mx-auto` and responsive padding, so explicit `mx-auto` and `px-4` are redundant with `container`. Remove them to avoid double padding.

    After all changes, run `pnpm typecheck && pnpm lint`.
  </action>
  <verify>
    `pnpm lint && pnpm typecheck` — both must pass.
    Verify CSV files are clean: `grep -rn "text-green-[0-9]\|text-yellow-[0-9]\|text-amber-[0-9]" components/customers/csv-import-dialog.tsx components/customers/csv-preview-table.tsx components/jobs/csv-job-import-dialog.tsx` returns 0 matches.
    Verify page padding: `grep -rn "space-y-6" app/(dashboard)/*/page.tsx` shows consistent space-y-6 across most pages.
  </verify>
  <done>
    - CSV import result colors use text-success, text-warning, text-destructive, text-info
    - Template channel badges use bg-info/10 text-info (email) and bg-success/10 text-success (SMS)
    - App-level files (campaigns/new, billing) use semantic tokens
    - All dashboard pages use consistent container py-6 space-y-6
    - Settings page left unchanged (intentional layout deviation)
    - Build passes
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. Verify no remaining hardcoded colors in tokenized files (excluding data-viz, stars, marketing):
   - `grep -rn "bg-amber-\|border-amber-\|text-amber-" components/ --include="*.tsx" | grep -v "marketing/" | grep -v "card.tsx"` — should only show card.tsx variants and marketing files
   - `grep -rn "bg-green-[0-9]\|border-green-[0-9]\|text-green-[0-9]" components/ --include="*.tsx" | grep -v "marketing/" | grep -v "card.tsx"` — should only show data-viz (campaign-stats.tsx, bulk-send-columns.tsx) and card.tsx
   - `grep -rn "text-red-[0-9]\|bg-red-[0-9]\|border-red-[0-9]" components/ --include="*.tsx" | grep -v "marketing/" | grep -v "card.tsx"` — should only show data-viz
   - `grep -rn "text-yellow-[0-9]" components/ --include="*.tsx" | grep -v "marketing/"` — should only show star ratings (satisfaction-rating.tsx, feedback-card.tsx, stat-strip.tsx) and data-viz (campaign-stats.tsx, bulk-send-columns.tsx, bulk-send-confirm-dialog.tsx)
4. Verify page padding is normalized: `grep "space-y-6" app/(dashboard)/*/page.tsx` shows consistent spacing across pages (except settings)
</verification>

<success_criteria>
- Categories C, D, F, L, N, P fully tokenized (~75 occurrences across 17 files)
- App-level hardcoded colors in campaigns/new and billing pages replaced with tokens
- All dashboard pages use consistent container py-6 space-y-6 spacing
- Only data-viz dots, star ratings, and marketing/decorative colors remain inline (intentional)
- Build passes (lint + typecheck)
</success_criteria>

<output>
After completion, create `.planning/phases/35-card-variants-dashboard-quick-wins/35-05-SUMMARY.md`
</output>
