---
phase: 28-onboarding-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/settings/email-auth-checklist.tsx
  - app/(dashboard)/settings/page.tsx
autonomous: true

must_haves:
  truths:
    - "Settings page displays SPF/DKIM/DMARC setup guidance checklist"
    - "Checklist links to Resend documentation for each DNS record type"
    - "Checklist is informational only -- does not block onboarding or any functionality"
  artifacts:
    - path: "components/settings/email-auth-checklist.tsx"
      provides: "Email authentication guidance component with 3 DNS record types"
      min_lines: 40
    - path: "app/(dashboard)/settings/page.tsx"
      provides: "Settings page with EmailAuthChecklist section"
      contains: "EmailAuthChecklist"
  key_links:
    - from: "app/(dashboard)/settings/page.tsx"
      to: "components/settings/email-auth-checklist.tsx"
      via: "import and render in settings page"
      pattern: "EmailAuthChecklist"
---

<objective>
Add email authentication (SPF/DKIM/DMARC) setup guidance checklist to settings page.

Purpose: DLVR-01 requires SPF/DKIM/DMARC guidance. Per research, this is display-only (Resend handles verification via their dashboard). Placing in settings as static guidance with documentation links.

Output: New EmailAuthChecklist component rendered in settings page.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@app/(dashboard)/settings/page.tsx
@.planning/phases/28-onboarding-redesign/28-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EmailAuthChecklist component</name>
  <files>components/settings/email-auth-checklist.tsx</files>
  <action>
Create a Client Component that displays a guidance checklist for email authentication DNS records. This is informational only -- no verification API calls.

Component structure:
- Info banner explaining that Resend handles email authentication automatically
- Three checklist items: SPF, DKIM, DMARC
- Each item shows: label, brief description, setup instructions, and link to Resend docs
- Use existing design patterns: border rounded-lg p-4 bg-card, Badge component for status
- All items show "Setup in Resend" status badge (not pending/verified since we can't verify)
- External links open in new tab with rel="noopener noreferrer"

DNS record items:
1. SPF Record -- "Authorizes Resend to send email on your behalf" -- link: https://resend.com/docs/dashboard/domains/introduction
2. DKIM Signature -- "Cryptographically signs emails to prevent spoofing" -- link: https://resend.com/docs/dashboard/domains/introduction
3. DMARC Policy -- "Sets policy for unauthenticated email handling" -- link: https://resend.com/docs/dashboard/domains/introduction

Use Phosphor icons (ShieldCheck for the section, ArrowSquareOut for external links) to match the existing icon set.

Add a "Open Resend Dashboard" button/link at the bottom pointing to https://resend.com/domains

Style to match other settings sections (border border-border rounded-lg p-6 bg-card shadow-sm pattern).
  </action>
  <verify>File exists at components/settings/email-auth-checklist.tsx with correct exports.</verify>
  <done>EmailAuthChecklist component renders 3 DNS record items with Resend documentation links.</done>
</task>

<task type="auto">
  <name>Task 2: Add EmailAuthChecklist to settings page</name>
  <files>app/(dashboard)/settings/page.tsx</files>
  <action>
Import EmailAuthChecklist at top of settings page:
```typescript
import { EmailAuthChecklist } from '@/components/settings/email-auth-checklist'
```

Add a new section between "AI Personalization" and "Integrations" sections:
```tsx
{/* Section 4.5: Email Authentication */}
<section className="border border-border rounded-lg p-6 bg-card shadow-sm">
  <h2 className="text-xl font-semibold mb-4">Email Authentication</h2>
  <p className="text-muted-foreground mb-4">
    Improve email deliverability by setting up DNS authentication records.
    These are configured through your Resend dashboard.
  </p>
  <EmailAuthChecklist />
</section>
```

This is a static component -- no data fetching required. No changes to SettingsContent async function.
  </action>
  <verify>Run `pnpm typecheck` -- no errors. Settings page renders without errors.</verify>
  <done>Settings page includes Email Authentication section with EmailAuthChecklist component between AI Personalization and Integrations.</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. EmailAuthChecklist component file exists with proper export
4. Settings page imports and renders EmailAuthChecklist
</verification>

<success_criteria>
- Settings page displays "Email Authentication" section with SPF, DKIM, DMARC checklist items
- Each item links to Resend documentation
- Component is informational only -- no blocking behavior, no API calls
- Matches existing settings page design patterns
</success_criteria>

<output>
After completion, create `.planning/phases/28-onboarding-redesign/28-02-SUMMARY.md`
</output>
