---
phase: 64-history-analytics-feedback
plan: 03
type: execute
wave: 2
depends_on: [64-01]
files_modified: [docs/qa-v3.1/64-feedback.md]
autonomous: true

must_haves:
  truths:
    - "Customer feedback rows are seeded in the database for the test business with varied ratings (1-star and 3-star), one with feedback_text and one without"
    - "Feedback page at /feedback displays all submitted feedback entries with star ratings visible (filled yellow stars for the rating value, empty gray stars for the remainder)"
    - "Feedback stats bar shows correct Total, Unresolved, Resolved, and Avg Rating values matching the seeded data"
    - "Marking a feedback item as resolved via the 'Mark Resolved' dialog with internal notes persists after page refresh — resolved_at and internal_notes are visible in the UI after reload"
    - "Reopening a resolved feedback item via the 'Reopen' button clears resolved_at and returns the card to unresolved styling"
    - "Feedback empty state renders correctly — shows ChatCircle icon, 'No feedback yet' heading, and custom empty message"
    - "Findings document docs/qa-v3.1/64-feedback.md exists with PASS/FAIL per requirement for FDBK-01 through FDBK-03"
  artifacts:
    - path: "docs/qa-v3.1/64-feedback.md"
      provides: "Feedback page QA findings with pass/fail, screenshots, DB verification, resolution workflow evidence, and bugs"
      min_lines: 100
  key_links: []
---

<objective>
Execute the Phase 64 QA audit of the Feedback page: feedback list display with ratings, resolution workflow (mark resolved with notes, reopen), and empty state behavior.

Purpose: The Feedback page captures private feedback from customers who rate 1-3 stars in the review funnel. This audit verifies the display, the resolution workflow (mark resolved + reopen), and persistence across page refreshes.

Output: `docs/qa-v3.1/64-feedback.md` — findings document with PASS/FAIL per requirement (FDBK-01 through FDBK-03), DB verification evidence, screenshot references, and bugs.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-history-analytics-feedback/64-RESEARCH.md
@docs/PROJECT_STATE.md

Test account: audit-test@avisloop.com / AuditTest123!
App URL: http://localhost:3000
Output file: docs/qa-v3.1/64-feedback.md
Business name: "Audit Test HVAC" (id: 6ed94b54-6f35-4ede-8dcb-28f562052042)

Existing data context:
- 3 AUDIT_ customers:
  - AUDIT_Marcus Rodriguez (id: d2ae4cd9-0a6f-422f-abb7-f0da335dab49, email: audit-marcus@example.com)
  - AUDIT_Patricia Johnson (id: 887b3e79-a707-45f4-be6a-2dc21232d534, email: audit-patricia@example.com)
  - AUDIT_Sarah Chen (id: 54009be7-8404-4a1a-93c9-1ce0b205329d, email: audit-sarah@example.com)
- **0 customer_feedback rows** — must seed before UI testing

Feedback page implementation:
- `FeedbackList` component: renders cards or empty state
- `FeedbackCard` component (client): star rating display, "Mark Resolved" button (opens ResolveFeedbackDialog), "Reopen" button, "Email" mailto link
- `ResolveFeedbackDialog`: form with hidden `id` field + `internal_notes` textarea + "Mark Resolved" submit
- `resolveFeedbackAction`: server action that sets resolved_at, resolved_by, internal_notes + revalidatePath('/feedback')
- `unresolveFeedbackAction`: server action that clears resolved_at, resolved_by + revalidatePath('/feedback')
- Stats bar: Total, Unresolved, Resolved (total - unresolved), Avg Rating

Important: `FeedbackCard` derives `isResolved` from prop (`!!feedback.resolved_at`). After `resolveFeedbackAction` calls `revalidatePath('/feedback')`, the server component re-fetches and passes updated data. To verify persistence, refresh the page manually and confirm the card still shows as resolved.

The customer_feedback table requires: business_id, customer_id, rating (1-5), feedback_text (optional), submitted_at (defaults NOW()). The enrollment_id is optional.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed feedback, verify list display, resolution workflow, and empty state (FDBK-01 through FDBK-03)</name>
  <files>docs/qa-v3.1/64-feedback.md</files>
  <action>
This task seeds customer_feedback data via Supabase MCP and then tests all 3 FDBK requirements using Playwright, including the full resolve/reopen lifecycle.

**IMPORTANT:** The dev server must be running (`pnpm dev`). Before starting Playwright steps, verify with `browser_navigate` to `http://localhost:3000`. If the server is not running, start it in the background first.

**Part A: Verify empty state FIRST (before seeding data) — FDBK-03**

1. Navigate to `http://localhost:3000/feedback`
2. Take a snapshot — the page should show the empty state since there are 0 customer_feedback rows
3. Verify the empty state renders:
   - ChatCircle icon (in a rounded muted circle)
   - "No feedback yet" heading (text-2xl font-semibold)
   - "When customers share feedback through your review funnel, it will appear here." message
4. Verify the stats section does NOT appear (it's gated behind `stats.total > 0`)
5. Take screenshot: `qa-64-feedback-empty-state.png`

**Part B: Seed customer_feedback via Supabase MCP**

Insert 3 feedback rows for the test business with varied ratings:

```sql
INSERT INTO customer_feedback (business_id, customer_id, rating, feedback_text, submitted_at) VALUES
-- 1-star from Marcus, with feedback text, submitted 2 hours ago
('6ed94b54-6f35-4ede-8dcb-28f562052042', 'd2ae4cd9-0a6f-422f-abb7-f0da335dab49', 1, 'The technician was 30 minutes late and left a mess in the hallway. Very disappointed with the service quality.', NOW() - INTERVAL '2 hours'),
-- 3-star from Patricia, with feedback text, submitted 1 day ago
('6ed94b54-6f35-4ede-8dcb-28f562052042', '887b3e79-a707-45f4-be6a-2dc21232d534', 3, 'Service was okay but pricing was higher than quoted. Would appreciate more transparent pricing.', NOW() - INTERVAL '1 day'),
-- 2-star from Sarah, no feedback text, submitted 3 days ago
('6ed94b54-6f35-4ede-8dcb-28f562052042', '54009be7-8404-4a1a-93c9-1ce0b205329d', 2, NULL, NOW() - INTERVAL '3 days');
```

After insertion, verify: `SELECT id, rating, feedback_text IS NOT NULL as has_text, resolved_at FROM customer_feedback WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042' ORDER BY submitted_at DESC;`

Expected: 3 rows, all resolved_at = NULL, ratings 1, 3, 2.

Also verify stats: `SELECT COUNT(*) as total, COUNT(*) FILTER (WHERE resolved_at IS NULL) as unresolved, ROUND(AVG(rating)::numeric, 1) as avg_rating FROM customer_feedback WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042';`

Expected: total=3, unresolved=3, avg_rating=2.0

**Part C: FDBK-01 — Feedback page lists submitted feedback with ratings**

1. Navigate to `http://localhost:3000/feedback` (or refresh if already there)
2. Take a screenshot: `qa-64-feedback-list-desktop.png`
3. Take a snapshot to read content
4. Verify page header: "Customer Feedback" title + subtitle showing "3 total"
5. Verify stats bar (4 cards):
   - Total = 3
   - Unresolved = 3 (amber colored)
   - Resolved = 0 (green colored)
   - Avg Rating = 2.0
6. Verify 3 feedback cards are displayed, each showing:
   - Customer name and email
   - Star rating (filled yellow stars for the value, gray empty stars for the rest)
   - For the Marcus card (1 star): 1 filled yellow star + 4 gray stars
   - For the Patricia card (3 stars): 3 filled yellow stars + 2 gray stars
   - For the Sarah card (2 stars): 2 filled yellow stars + 3 gray stars
7. Verify feedback_text content is visible on Marcus and Patricia cards
8. Verify Sarah's card does NOT show feedback text (it's NULL)
9. Verify each card has "Email" button and "Mark Resolved" button
10. Verify cards are ordered by submitted_at DESC (Marcus first/most recent, Sarah last)

**Part D: FDBK-02 — Feedback resolution workflow**

This is the most important test — verifying the full resolve/reopen lifecycle with persistence.

**Step 1: Mark Marcus's feedback as resolved**

1. On the Marcus card (1-star, first card), click "Mark Resolved" button
2. Take a snapshot — the ResolveFeedbackDialog should open
3. Verify dialog shows: "Resolve Feedback" title, "Mark feedback from AUDIT_Marcus Rodriguez as resolved." text
4. Type internal notes in the textarea: "Called customer, offered $50 credit. Customer satisfied with resolution."
5. Click "Mark Resolved" button in the dialog
6. Wait for dialog to close and page to revalidate
7. Take a snapshot — Marcus's card should now show resolved styling:
   - Muted background (bg-muted/30)
   - Muted border
   - "Reopen" button instead of "Mark Resolved"
   - Internal notes section visible: "Internal notes:" label + the typed text
   - "Resolved X ago" footer text
8. Take screenshot: `qa-64-feedback-resolved.png`

**Step 2: Verify persistence after page refresh**

1. Navigate away (e.g., to /dashboard) and then back to /feedback — OR do a hard refresh
2. Take a snapshot
3. Verify Marcus's card is STILL showing as resolved (resolved_at persisted in DB)
4. Verify internal notes are still visible: "Called customer, offered $50 credit. Customer satisfied with resolution."
5. Verify stats bar updated: Total=3, Unresolved=2, Resolved=1, Avg Rating=2.0
6. Take screenshot: `qa-64-feedback-after-refresh.png`

**Step 3: Verify DB state**

Query via Supabase MCP: `SELECT id, rating, resolved_at IS NOT NULL as is_resolved, internal_notes FROM customer_feedback WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042' ORDER BY submitted_at DESC;`

Verify Marcus's row has resolved_at set (not NULL) and internal_notes matches.

**Step 4: Reopen Marcus's feedback**

1. On the resolved Marcus card, click "Reopen" button
2. Wait for page to revalidate
3. Take a snapshot — Marcus's card should return to unresolved styling:
   - Normal border (not muted)
   - "Mark Resolved" button returns (not "Reopen")
   - Internal notes section should disappear (resolved_at is null now)
4. Verify stats bar: Total=3, Unresolved=3, Resolved=0
5. Take screenshot: `qa-64-feedback-reopened.png`

**Step 5: Verify reopen persistence in DB**

Query: `SELECT id, rating, resolved_at, internal_notes FROM customer_feedback WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042' AND customer_id = 'd2ae4cd9-0a6f-422f-abb7-f0da335dab49';`

Verify resolved_at is NULL and resolved_by is NULL.

**Part E: Re-resolve for clean state**

After testing the full lifecycle, re-resolve Marcus's feedback so the data state is "1 resolved, 2 unresolved" for any downstream testing:

1. Click "Mark Resolved" on Marcus's card again
2. Enter internal notes: "Resolved after QA testing"
3. Submit — verify it resolves again
4. Take final snapshot of feedback page

**Part F: Write findings document**

Create `docs/qa-v3.1/64-feedback.md` with:
- Header (date, tester, test account, business info)
- Summary table with PASS/FAIL for FDBK-01 through FDBK-03
- DB seeding verification section (feedback counts, ratings)
- FDBK-03 (empty state): tested BEFORE seeding data — document what was seen
- FDBK-01 (list display): document star ratings, feedback text visibility, stats bar values
- FDBK-02 (resolution workflow): document the full lifecycle — resolve with notes → refresh → verify persistence → reopen → verify persistence → re-resolve
- DB verification evidence for resolve/unresolve operations
- Per-step screenshot references
- Any bugs or findings discovered during testing
  </action>
  <verify>
- `docs/qa-v3.1/64-feedback.md` exists and has PASS/FAIL for all 3 requirements
- DB has 3 customer_feedback rows for the test business
- Marcus's feedback is in resolved state (resolved_at is not null)
- Screenshot files referenced in the document exist
  </verify>
  <done>
All 3 FDBK requirements have PASS/FAIL verdicts documented in `docs/qa-v3.1/64-feedback.md`. Feedback list displays with correct star ratings, resolution workflow persists across page refresh, reopen clears resolved state, and empty state was verified before data seeding.
  </done>
</task>

</tasks>

<verification>
- `docs/qa-v3.1/64-feedback.md` has at least 100 lines with all FDBK-01 through FDBK-03 verdicts
- Each requirement has explicit PASS or FAIL with evidence
- Resolution workflow tested with full lifecycle: resolve → refresh → verify → reopen → verify → re-resolve
- Screenshot files are referenced and exist in the project root
- DB state confirmed via Supabase MCP queries after each mutation
</verification>

<success_criteria>
Feedback page audit complete with all 3 requirements verified. Feedback list displays ratings correctly, resolution workflow persists across page refresh (resolve and reopen both work), and empty state renders when no feedback exists.
</success_criteria>

<output>
After completion, create `.planning/phases/64-history-analytics-feedback/64-03-SUMMARY.md`
</output>
