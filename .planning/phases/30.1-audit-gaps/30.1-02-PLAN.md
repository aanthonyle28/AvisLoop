---
phase: 30.1-audit-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/layout/sidebar.tsx
  - components/layout/bottom-nav.tsx
  - app/(dashboard)/send/page.tsx
  - components/send/send-page-client.tsx
autonomous: true

must_haves:
  truths:
    - "Send page renamed to Manual Request in sidebar navigation"
    - "Send page renamed to Manual Request in mobile bottom navigation"
    - "Manual Request moved below Activity in sidebar nav order"
    - "Send page shows friction warning banner explaining campaigns handle this automatically"
  artifacts:
    - path: "components/layout/sidebar.tsx"
      provides: "Updated navigation with Manual Request label and position"
      contains: "Manual Request"
    - path: "components/layout/bottom-nav.tsx"
      provides: "Updated mobile nav with Manual Request label"
      contains: "Manual Request"
    - path: "components/send/send-page-client.tsx"
      provides: "Friction warning banner component"
      contains: "friction-warning"
  key_links:
    - from: "components/layout/sidebar.tsx"
      to: "/send"
      via: "href prop"
      pattern: "label.*Manual.*Request.*href.*send"
---

<objective>
Rename Send page to "Manual Request" in all navigation, move it below Activity, and add friction warning banner.

Purpose: V2 philosophy states manual sending is an escape hatch, not primary workflow. This de-emphasizes the Send page per UX-AUDIT High #8 and closes Phase 30 gap.

Output: Updated navigation labels, reordered nav items, friction warning on Send page.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/V1-TO-V2-PHILOSOPHY.md
@.planning/phases/30-v2-alignment/30-VERIFICATION.md (gap: Send page de-emphasis)

Current nav order (sidebar.tsx lines 33-42):
1. Dashboard
2. Jobs
3. Campaigns
4. Analytics
5. Customers
6. Send ← rename + move down
7. Activity
8. Feedback

Target nav order:
1. Dashboard
2. Jobs
3. Campaigns
4. Analytics
5. Customers
6. Activity
7. Manual Request ← moved below Activity
8. Feedback
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename and reorder Send in sidebar</name>
  <files>components/layout/sidebar.tsx</files>
  <action>
Update the mainNav array in sidebar.tsx:

1. Find the Send nav item (currently line ~39)
2. Change label from "Send" to "Manual Request"
3. Reorder the array so Manual Request comes AFTER Activity (position 7)

Current order:
```tsx
const mainNav: NavItem[] = [
  { icon: House, label: 'Dashboard', href: '/dashboard' },
  { icon: Briefcase, label: 'Jobs', href: '/jobs' },
  { icon: Megaphone, label: 'Campaigns', href: '/campaigns' },
  { icon: ChartBar, label: 'Analytics', href: '/analytics' },
  { icon: AddressBook, label: 'Customers', href: '/customers' },
  { icon: PaperPlaneTilt, label: 'Send', href: '/send' },           // ← change
  { icon: ClockCounterClockwise, label: 'Activity', href: '/history' },
  { icon: ChatCircleText, label: 'Feedback', href: '/feedback' },
]
```

New order:
```tsx
const mainNav: NavItem[] = [
  { icon: House, label: 'Dashboard', href: '/dashboard' },
  { icon: Briefcase, label: 'Jobs', href: '/jobs' },
  { icon: Megaphone, label: 'Campaigns', href: '/campaigns' },
  { icon: ChartBar, label: 'Analytics', href: '/analytics' },
  { icon: AddressBook, label: 'Customers', href: '/customers' },
  { icon: ClockCounterClockwise, label: 'Activity', href: '/history' },  // ← moved up
  { icon: PaperPlaneTilt, label: 'Manual Request', href: '/send' },      // ← renamed, moved down
  { icon: ChatCircleText, label: 'Feedback', href: '/feedback' },
]
```
  </action>
  <verify>grep "Manual Request" components/layout/sidebar.tsx returns the nav item</verify>
  <done>Sidebar shows "Manual Request" in position 7 (after Activity)</done>
</task>

<task type="auto">
  <name>Task 2: Rename Send in bottom nav</name>
  <files>components/layout/bottom-nav.tsx</files>
  <action>
Update the items array in bottom-nav.tsx:

1. Find the Send nav item (currently line ~12)
2. Change label from "Send" to "Manual" (shorter for mobile)
3. Reorder so it comes after Activity (last position or near last)

Current items (5 items):
```tsx
const items = [
  { icon: House, label: 'Dashboard', href: '/dashboard' },
  { icon: PaperPlaneTilt, label: 'Send', href: '/send' },      // ← change
  { icon: Briefcase, label: 'Jobs', href: '/jobs' },
  { icon: Megaphone, label: 'Campaigns', href: '/campaigns' },
  { icon: ClockCounterClockwise, label: 'Activity', href: '/history' },
]
```

New order (prioritize V2 actions, de-emphasize manual):
```tsx
const items = [
  { icon: House, label: 'Dashboard', href: '/dashboard' },
  { icon: Briefcase, label: 'Jobs', href: '/jobs' },
  { icon: Megaphone, label: 'Campaigns', href: '/campaigns' },
  { icon: ClockCounterClockwise, label: 'Activity', href: '/history' },
  { icon: PaperPlaneTilt, label: 'Manual', href: '/send' },    // ← renamed, moved to last
]
```

Use "Manual" instead of "Manual Request" for space (mobile bottom nav has limited label width).
  </action>
  <verify>grep "Manual" components/layout/bottom-nav.tsx returns the nav item</verify>
  <done>Mobile bottom nav shows "Manual" in last position</done>
</task>

<task type="auto">
  <name>Task 3: Add friction warning banner to Send page</name>
  <files>components/send/send-page-client.tsx, app/(dashboard)/send/page.tsx</files>
  <action>
Add a friction warning banner at the top of the Send page to reinforce V2 philosophy.

**In send-page-client.tsx:**

1. Import Warning icon from @phosphor-icons/react
2. Add a warning banner component at the top of the rendered content (before tabs)

```tsx
import { Warning } from '@phosphor-icons/react'

// Inside the component return, at the very top:
<div className="mb-6 flex items-start gap-3 rounded-lg border border-amber-200 bg-amber-50 p-4 dark:border-amber-900/50 dark:bg-amber-950/20" data-testid="friction-warning">
  <Warning className="h-5 w-5 text-amber-600 dark:text-amber-400 shrink-0 mt-0.5" weight="fill" />
  <div className="text-sm">
    <p className="font-medium text-amber-900 dark:text-amber-200">
      Campaigns handle review requests automatically
    </p>
    <p className="mt-1 text-amber-700 dark:text-amber-300">
      Manual sending is for one-off situations. For ongoing follow-up, set up a{' '}
      <Link href="/campaigns" className="underline font-medium hover:text-amber-900 dark:hover:text-amber-100">
        campaign
      </Link>
      {' '}instead.
    </p>
  </div>
</div>
```

**In app/(dashboard)/send/page.tsx:**

Update the metadata title from "Send" to "Manual Request":
```tsx
export const metadata = {
  title: 'Manual Request',
}
```
  </action>
  <verify>
- grep "friction-warning" components/send/send-page-client.tsx
- grep "Campaigns handle" components/send/send-page-client.tsx
- grep "Manual Request" app/(dashboard)/send/page.tsx
  </verify>
  <done>Send page shows friction warning banner explaining campaigns are the primary method</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. Sidebar shows "Manual Request" in position 7 (after Activity)
4. Bottom nav shows "Manual" in position 5 (last)
5. Send page has amber friction warning banner at top
6. Page title is "Manual Request"
</verification>

<success_criteria>
- "Send" renamed to "Manual Request" in sidebar
- "Send" renamed to "Manual" in bottom nav
- Nav order updated: Activity before Manual Request
- Friction warning banner visible on /send page
- No TypeScript or lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/30.1-audit-gaps/30.1-02-SUMMARY.md`
</output>
