---
phase: 30.1-audit-gaps
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - app/(dashboard)/campaigns/new/page.tsx
autonomous: true

must_haves:
  truths:
    - "New campaign page shows guidance callout recommending presets"
    - "Callout links to campaign presets for one-click setup"
    - "Users understand presets are the recommended starting point"
  artifacts:
    - path: "app/(dashboard)/campaigns/new/page.tsx"
      provides: "Preset guidance callout"
      contains: "preset"
  key_links:
    - from: "app/(dashboard)/campaigns/new/page.tsx"
      to: "/campaigns"
      via: "Link to presets"
      pattern: "href.*campaigns.*preset"
---

<objective>
Add preset guidance callout to the new campaign page.

Purpose: Guide users toward using presets (one-click setup) rather than building from scratch (QA-AUDIT M04-02).

Output: New campaign page with prominent guidance callout recommending presets.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/QA-AUDIT.md (M04-02: Missing preset guidance)

New campaign page: @app/(dashboard)/campaigns/new/page.tsx
Current state: Shows form directly with no guidance about presets
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add preset guidance callout</name>
  <files>app/(dashboard)/campaigns/new/page.tsx</files>
  <action>
Add a guidance callout between the header and the form that recommends using presets:

```tsx
import Link from 'next/link'
import { Lightbulb, ArrowRight } from '@phosphor-icons/react/dist/ssr'
import { getAvailableTemplates } from '@/lib/data/message-template'
import { CampaignForm } from '@/components/campaigns/campaign-form'
import { ArrowLeft } from '@phosphor-icons/react/dist/ssr'

export const metadata = {
  title: 'New Campaign',
}

export default async function NewCampaignPage() {
  const templates = await getAvailableTemplates()

  return (
    <div className="container py-6 max-w-3xl">
      <Link
        href="/campaigns"
        className="text-sm text-muted-foreground hover:text-foreground flex items-center gap-1 mb-4"
      >
        <ArrowLeft className="h-4 w-4" />
        Back to campaigns
      </Link>

      <div className="mb-6">
        <h1 className="text-2xl font-semibold">New Campaign</h1>
        <p className="text-muted-foreground">
          Create an automated review request sequence for completed jobs
        </p>
      </div>

      {/* Preset guidance callout */}
      <div className="mb-8 rounded-lg border border-blue-200 bg-blue-50 p-4 dark:border-blue-900/50 dark:bg-blue-950/20">
        <div className="flex items-start gap-3">
          <Lightbulb className="h-5 w-5 text-blue-600 dark:text-blue-400 shrink-0 mt-0.5" weight="fill" />
          <div className="flex-1">
            <p className="font-medium text-blue-900 dark:text-blue-200">
              New to campaigns? Start with a preset
            </p>
            <p className="mt-1 text-sm text-blue-700 dark:text-blue-300">
              Campaign presets are pre-configured sequences optimized for different follow-up styles.
              Choose Conservative, Standard, or Aggressive based on your preference.
            </p>
            <Link
              href="/campaigns#presets"
              className="mt-3 inline-flex items-center gap-1.5 text-sm font-medium text-blue-700 dark:text-blue-300 hover:text-blue-900 dark:hover:text-blue-100"
            >
              View presets
              <ArrowRight className="h-4 w-4" />
            </Link>
          </div>
        </div>
      </div>

      <CampaignForm templates={templates} />
    </div>
  )
}
```

The callout:
- Uses blue (informational) color scheme
- Has a lightbulb icon for "tip" visual
- Briefly explains what presets are
- Links back to /campaigns with #presets anchor (preset picker is on that page)
- Is prominent but not blocking (user can scroll past to use form)
  </action>
  <verify>
- grep "preset" app/(dashboard)/campaigns/new/page.tsx
- grep "Lightbulb" app/(dashboard)/campaigns/new/page.tsx
- grep "Start with a preset" app/(dashboard)/campaigns/new/page.tsx
  </verify>
  <done>New campaign page shows preset guidance callout</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. New campaign page shows blue callout above form
4. Callout explains presets and links to /campaigns#presets
5. Form still accessible below callout
</verification>

<success_criteria>
- Preset guidance callout visible on /campaigns/new page
- Callout recommends starting with a preset
- Link to /campaigns#presets works
- No TypeScript or lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/30.1-audit-gaps/30.1-05-SUMMARY.md`
</output>
