---
phase: 54-business-switcher-ui
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - components/layout/page-header.tsx
autonomous: true

must_haves:
  truths:
    - "On mobile, the business switcher is visible in the page header — agency owners can switch businesses without a desktop sidebar"
    - "When the page has no title prop, the business name appears alongside the AvisLoop logo in the mobile header"
    - "When the page has a title prop, the business name still appears below or alongside the title"
    - "The mobile switcher uses the same BusinessSwitcher component as the desktop sidebar — no code duplication"
    - "Single-business users see only the business name as plain text in the mobile header — no dropdown chrome"
  artifacts:
    - path: "components/layout/page-header.tsx"
      provides: "Mobile header with business switcher integration"
      contains: "BusinessSwitcher"
  key_links:
    - from: "components/layout/page-header.tsx"
      to: "components/layout/business-switcher.tsx"
      via: "import and render in mobile header"
      pattern: "BusinessSwitcher"
---

<objective>
Integrate the BusinessSwitcher component into the mobile page header so agency owners can switch businesses on mobile devices without needing the desktop sidebar.

Purpose: Satisfies SWITCH-04. Without this, mobile users (common for field technicians and on-the-go agency owners) cannot switch businesses at all.
Output: Updated `components/layout/page-header.tsx`
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-business-switcher-ui/54-RESEARCH.md
@.planning/phases/54-business-switcher-ui/54-01-SUMMARY.md

@components/layout/page-header.tsx
@components/layout/business-switcher.tsx
@components/layout/app-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BusinessSwitcher to mobile page header</name>
  <files>components/layout/page-header.tsx</files>
  <action>
Modify `components/layout/page-header.tsx` to include the BusinessSwitcher:

1. **Import:** Add `import { BusinessSwitcher } from './business-switcher'` at the top.

2. **Layout restructure:** The current header has two sections: left (logo or title) and right (AccountMenu). Restructure to include the business switcher. The recommended layout:

   **When no `title` prop (default — shows logo):**
   - Left: AvisLoop logo (existing)
   - Center: Business switcher (new) — takes remaining space with `flex-1 min-w-0` for truncation
   - Right: AccountMenu (existing)

   **When `title` prop is provided:**
   - Left: Page title (existing)
   - Center: Business switcher (new) — takes remaining space with `flex-1 min-w-0` for truncation
   - Right: AccountMenu (existing)

3. **Implementation approach:** Replace the current two-column flex layout with a three-section layout:

```tsx
<header className="md:hidden bg-card border-b border-border">
  <div className="h-16 px-4 flex items-center gap-3">
    {/* Left side: Page title or logo */}
    <div className="flex items-center gap-2 shrink-0">
      {title ? (
        <h1 className="font-semibold text-lg">{title}</h1>
      ) : (
        <Link href="/dashboard" className="flex items-center gap-2">
          <ArrowsClockwise size={24} weight="regular" className="text-accent" />
          <span className="font-bold text-lg">AvisLoop</span>
        </Link>
      )}
    </div>

    {/* Center: Business context — takes remaining space, truncates gracefully */}
    <div className="flex-1 min-w-0 flex justify-end">
      <BusinessSwitcher />
    </div>

    {/* Right side: Account */}
    <div className="flex items-center shrink-0">
      <AccountMenu
        side="bottom"
        align="end"
        trigger={
          <Button variant="ghost" size="icon-sm" aria-label="Account menu">
            <UserCircle size={24} weight="regular" />
          </Button>
        }
      />
    </div>
  </div>
</header>
```

4. **Key layout considerations:**
   - The business switcher section uses `flex-1 min-w-0` so it takes remaining space and allows its content to truncate via the `truncate` class inside `BusinessSwitcher`
   - `justify-end` on the center section right-aligns the business name when there's extra space (keeps it close to the account menu, away from the logo)
   - `shrink-0` on left and right sections prevents the logo and account button from being squeezed
   - Gap of `gap-3` provides comfortable spacing between the three sections

5. **Do NOT:**
   - Pass any props to `BusinessSwitcher` — it reads from context
   - Add any new state or hooks to `page-header.tsx`
   - Change the `md:hidden` class — the header is mobile-only
   - Remove or change the AccountMenu integration
  </action>
  <verify>
Run `pnpm typecheck` and `pnpm lint` — both must pass. Grep for `BusinessSwitcher` in `page-header.tsx` to confirm integration. Verify the component renders with `md:hidden` (mobile only).
  </verify>
  <done>
`page-header.tsx` imports and renders `BusinessSwitcher` in the mobile header. The business name appears between the logo/title and the account menu. Typecheck and lint pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final verification — typecheck, lint, visual audit</name>
  <files>components/layout/business-switcher.tsx, components/layout/sidebar.tsx, components/layout/page-header.tsx</files>
  <action>
Run the full verification suite to confirm everything works end-to-end:

1. Run `pnpm typecheck` — must pass with zero errors
2. Run `pnpm lint` — must pass with zero errors or warnings
3. Grep to verify all key wiring:
   - `BusinessSwitcher` imported in `sidebar.tsx` and `page-header.tsx`
   - `useBusinessSettings` called in `business-switcher.tsx`
   - `switchBusiness` called in `business-switcher.tsx`
   - `businesses.length` guard exists in `business-switcher.tsx`
   - `!collapsed` guard wraps the switcher in `sidebar.tsx`
4. Verify no unused imports were introduced in any modified file
5. Verify the `business-switcher.tsx` file has `'use client'` directive at the top

If any lint or typecheck errors exist, fix them before marking done.
  </action>
  <verify>
`pnpm typecheck` passes. `pnpm lint` passes. All grep checks confirm correct wiring.
  </verify>
  <done>
All three files pass typecheck and lint. BusinessSwitcher is correctly wired in both desktop sidebar and mobile header. Single-business guard prevents unnecessary dropdown chrome. Collapsed sidebar hides the switcher.
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. `page-header.tsx` imports and renders `BusinessSwitcher`
4. The mobile header shows business name between logo and account menu
5. `BusinessSwitcher` component is used in both `sidebar.tsx` and `page-header.tsx` (same component, no duplication)
6. No new packages installed — only existing dependencies used
7. All files modified are client components with `'use client'` directive
</verification>

<success_criteria>
- Mobile page header includes BusinessSwitcher between logo and account menu
- Agency owners can switch businesses on mobile via the dropdown
- Single-business users see plain text business name (no dropdown)
- Same BusinessSwitcher component used in both desktop and mobile contexts
- Typecheck and lint pass with zero errors across all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/54-business-switcher-ui/54-02-SUMMARY.md`
</output>
