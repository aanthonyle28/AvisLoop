---
phase: 60-onboarding-wizard
plan: 01
type: execute
wave: 1
depends_on: [59-01]
files_modified: [docs/qa-v3.1/60-onboarding-wizard.md]
autonomous: true

must_haves:
  truths:
    - "Additional business wizard completes 2 steps (business setup + campaign preset) and creates a new business row + campaign in the database"
    - "Original audit-test business (6ed94b54-6f35-4ede-8dcb-28f562052042) data is completely unchanged after additional business creation"
    - "First-business onboarding wizard completes all 4 steps (business setup, campaign preset, CRM platform, SMS consent) and redirects to /dashboard"
    - "After first-business wizard completion, businesses table shows onboarding_completed_at IS NOT NULL, sms_consent_acknowledged = true, and service_types_enabled is non-empty"
    - "A non-preset campaign with campaign_touches exists in the database for the audit-test business after wizard completion"
    - "Step 1 data entered and saved via Continue is pre-filled when navigating back to ?step=1 after visiting another page (DB-backed draft persistence)"
    - "CRM Platform step (step 3) software_used column missing bug is documented with evidence"
    - "Findings document docs/qa-v3.1/60-onboarding-wizard.md exists with pass/fail per requirement (ONB-01, ONB-02, ONB-03)"
  artifacts:
    - path: "docs/qa-v3.1/60-onboarding-wizard.md"
      provides: "Onboarding wizard QA findings with pass/fail, screenshots, and bugs"
      min_lines: 100
  key_links: []
---

<objective>
Execute the Phase 60 QA E2E audit of both onboarding wizard flows (first-business 4-step wizard and additional-business 2-step wizard) plus draft persistence verification.

Purpose: Confirm that onboarding is fully functional for both new users and agency users adding additional businesses. Verify that data persists correctly and that the known software_used bug is documented.

Output: `docs/qa-v3.1/60-onboarding-wizard.md` -- a findings document with pass/fail per requirement (ONB-01, ONB-02, ONB-03), screenshot evidence references, and any bugs discovered.

CRITICAL TEST ORDER: ONB-02 first, then ONB-03, then ONB-01 last. Completing ONB-01 marks onboarding as done and changes routing behavior, so it must run last. After ONB-01, reset the DB to pre-onboarding state for future test runs.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-onboarding-wizard/60-RESEARCH.md
@docs/PROJECT_STATE.md

Test account: audit-test@avisloop.com / AuditTest123!
App URL: http://localhost:3000
Output file: docs/qa-v3.1/60-onboarding-wizard.md
User ID: ac6f9407-7e88-4204-9f0f-8d213c58ab67
Business ID: 6ed94b54-6f35-4ede-8dcb-28f562052042
</context>

<tasks>

<task type="auto">
  <name>Task 1: ONB-02 -- Additional business creation wizard (2 steps)</name>
  <files>docs/qa-v3.1/60-onboarding-wizard.md</files>
  <action>
This task tests the CreateBusinessWizard -- the additional business creation flow accessed via `/businesses` page "Add Business" button. This must run FIRST because it does not affect the primary business's onboarding state.

**IMPORTANT:** The dev server must be running (`pnpm dev`). Before starting Playwright steps, verify with a quick `browser_navigate` to `http://localhost:3000`. If the server is not running, start it in the background first.

**IMPORTANT:** You must be logged in as audit-test@avisloop.com from Phase 59. If not logged in, log in first via `/auth/login`.

**Step-by-step Playwright MCP walkthrough:**

**Part A: Baseline DB snapshot**

1. `mcp__supabase__execute_sql` -- Capture baseline business count and data:
```sql
SELECT id, name, onboarding_completed_at, service_types_enabled, created_at
FROM businesses
WHERE user_id = 'ac6f9407-7e88-4204-9f0f-8d213c58ab67'
ORDER BY created_at;
```
Record the result. Expected: 1 row (the existing "Audit Test HVAC" business with `onboarding_completed_at = null`).

**Part B: Navigate to businesses page and start wizard**

2. `browser_navigate` to `http://localhost:3000/businesses`
3. `browser_snapshot` -- Verify:
   - Page loads with business card(s) visible
   - "Add Business" button/link is visible
   - Record what elements are on the page
4. `browser_take_screenshot` -- filename: `qa-60-businesses-page.png`

5. `browser_click` -- Click the "Add Business" button/link
6. `browser_wait_for` -- Wait for text "Set up your new business" (CreateBusinessWizard step 1 heading)
7. `browser_snapshot` -- Verify CreateBusinessWizard step 1 loaded:
   - Heading: "Set up your new business"
   - Business name input present
   - Service type chips present (HVAC, Plumbing, Electrical, Cleaning, Roofing, Painting, Handyman, Other)
   - Progress bar shows "1/2" (NOT "1/3")
   - Continue button present (should be disabled -- no service type selected)
8. `browser_take_screenshot` -- filename: `qa-60-add-biz-step1-empty.png`
9. `browser_console_messages` (level: error) -- Check for any JS errors on load

**Part C: Step 1 -- Fill business details and services**

10. `browser_snapshot` -- Get current refs for form fields
11. Find the business name input (labeled "Business name *" or id `business-name`) and type: "Phase 60 Test Business"
    Use `browser_type` with the ref from the snapshot.
12. Find the phone number input (labeled "Phone number" or id `phone-number`) and type: "555-060-0001"
    Use `browser_type` with the ref from the snapshot.

13. `browser_click` -- Click the "HVAC" service type chip
14. `browser_snapshot` -- Verify HVAC chip shows selected state (check icon, primary-colored background)

15. `browser_click` -- Click the "Cleaning" service type chip (select a second service)
16. `browser_snapshot` -- Verify both HVAC and Cleaning are selected

17. `browser_take_screenshot` -- filename: `qa-60-add-biz-step1-filled.png`

**Part C.1: Validation test -- try Continue without business name**

18. Clear the business name field: `browser_click` on business name input ref, then select all and delete (triple-click or Ctrl+A then Delete), OR navigate to a fresh wizard. Actually, the simpler approach: just verify the Continue button is enabled now (since services are selected and name is filled). Instead test the OTHER validation: if you de-select all service types, Continue should be disabled.

    Actually, the validation rules are: (1) empty business name shows error on submit, (2) no service selected disables the button. Since we already have both filled, let's test the validation path first before filling everything:

    REVISED APPROACH: Before filling anything, test that Continue is disabled when no services are selected. We already passed this point (services are now selected). Skip explicit empty validation testing for the additional business wizard -- it uses the same component as the first-business wizard which will be tested in ONB-01. Move on.

19. `browser_click` -- Click "Continue" button
20. `browser_wait_for` -- Wait for text "Choose your follow-up approach" (step 2 heading)
21. `browser_snapshot` -- Verify:
   - Step 2 (Campaign Preset) loaded
   - Heading: "Choose your follow-up approach"
   - Three preset cards visible: "Gentle Follow-Up", "Standard Follow-Up", "Aggressive Follow-Up"
   - Progress bar shows "2/2"
   - Continue button present (should be disabled until preset selected)
22. `browser_take_screenshot` -- filename: `qa-60-add-biz-step2-presets.png`
23. `browser_console_messages` (level: error) -- Check for JS errors

**Part D: Step 2 -- Select campaign preset and complete**

24. `browser_click` -- Click the "Standard Follow-Up" card
25. `browser_snapshot` -- Verify:
   - "Standard Follow-Up" card is selected (ring-2 styling, primary border)
   - Continue button is now enabled
26. `browser_take_screenshot` -- filename: `qa-60-add-biz-step2-selected.png`

27. `browser_click` -- Click "Continue" button
28. `browser_wait_for` -- Wait for dashboard content to appear. The wizard should redirect to `/dashboard` (NOT `/dashboard?onboarding=complete`). Wait for any dashboard text like "Dashboard" or KPI-related content. Max 15 seconds.
29. `browser_snapshot` -- Verify:
   - On dashboard page (URL contains `/dashboard`)
   - No error messages
   - Business name in sidebar/header may now show the new business (cookie was switched)
30. `browser_take_screenshot` -- filename: `qa-60-add-biz-complete-dashboard.png`
31. `browser_console_messages` (level: error) -- Check for JS errors during completion

**Part E: DB verification -- new business created, original unchanged**

32. `mcp__supabase__execute_sql` -- Verify two businesses exist:
```sql
SELECT id, name, onboarding_completed_at, service_types_enabled,
       sms_consent_acknowledged, created_at
FROM businesses
WHERE user_id = 'ac6f9407-7e88-4204-9f0f-8d213c58ab67'
ORDER BY created_at;
```
Expected: 2 rows. First row is original "Audit Test HVAC" with `onboarding_completed_at = null`. Second row is "Phase 60 Test Business" with `onboarding_completed_at IS NOT NULL`.

33. `mcp__supabase__execute_sql` -- Verify original business is UNCHANGED:
```sql
SELECT id, name, onboarding_completed_at, service_types_enabled
FROM businesses
WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
```
Expected: name = 'Audit Test HVAC', onboarding_completed_at = null, service_types_enabled = '{}' (empty).

34. `mcp__supabase__execute_sql` -- Verify new business has a non-preset campaign:
```sql
SELECT c.id, c.name, c.status, c.is_preset, c.business_id, c.service_type
FROM campaigns c
JOIN businesses b ON b.id = c.business_id
WHERE b.user_id = 'ac6f9407-7e88-4204-9f0f-8d213c58ab67'
AND c.is_preset = false
AND c.business_id != '6ed94b54-6f35-4ede-8dcb-28f562052042'
ORDER BY c.created_at DESC
LIMIT 5;
```
Expected: At least 1 campaign row for the new business, `is_preset = false`.

35. `mcp__supabase__execute_sql` -- Verify campaign touches exist for new campaign:
```sql
SELECT ct.touch_number, ct.channel, ct.delay_hours
FROM campaign_touches ct
JOIN campaigns c ON c.id = ct.campaign_id
JOIN businesses b ON b.id = c.business_id
WHERE b.user_id = 'ac6f9407-7e88-4204-9f0f-8d213c58ab67'
AND c.is_preset = false
AND c.business_id != '6ed94b54-6f35-4ede-8dcb-28f562052042'
ORDER BY ct.touch_number;
```
Expected: 3 touches (Standard preset has 3 touches: email, email, sms).

**Part F: Cleanup -- delete additional business**

36. `mcp__supabase__execute_sql` -- Delete the test business (CASCADE removes campaigns + touches):
```sql
DELETE FROM businesses
WHERE user_id = 'ac6f9407-7e88-4204-9f0f-8d213c58ab67'
AND id != '6ed94b54-6f35-4ede-8dcb-28f562052042';
```

37. `mcp__supabase__execute_sql` -- Verify only original business remains:
```sql
SELECT id, name FROM businesses
WHERE user_id = 'ac6f9407-7e88-4204-9f0f-8d213c58ab67';
```
Expected: 1 row, id = '6ed94b54-6f35-4ede-8dcb-28f562052042'.

38. Switch back to original business context: `browser_navigate` to `http://localhost:3000/dashboard` (the active_business_id cookie may point to the deleted business now -- navigating to dashboard should fall back to the remaining business).

**Part G: Initialize findings document**

39. Create `docs/qa-v3.1/60-onboarding-wizard.md` with the initial structure and ONB-02 results filled in:

```markdown
# Phase 60: Onboarding Wizard -- QA Findings

**Tested:** 2026-02-27
**Tester:** Claude (Playwright MCP)
**App URL:** http://localhost:3000
**Test account:** audit-test@avisloop.com

---

## Summary

| Requirement | Status | Notes |
|-------------|--------|-------|
| ONB-01: First-business wizard (4 steps) | [pending] | |
| ONB-02: Additional business creation (2 steps) | [PASS/FAIL] | [notes from test] |
| ONB-03: Draft persistence | [pending] | |

---

## ONB-02: Additional Business Creation

**Status:** [PASS/FAIL]

### Pre-conditions
- Logged in as audit-test@avisloop.com
- Baseline: 1 business ("Audit Test HVAC"), onboarding_completed_at = null

### Test steps

#### Entry point
1. Navigated to /businesses
2. "Add Business" button/link found: [YES/NO, describe element]
3. Clicked "Add Business" -- redirected to /onboarding?mode=new
4. CreateBusinessWizard rendered with heading "Set up your new business"

#### Step 1: Business Setup
5. Progress bar: [1/2 or other]
6. Filled business name: "Phase 60 Test Business"
7. Filled phone: "555-060-0001"
8. Selected service types: HVAC, Cleaning
9. Continue button state: [enabled/disabled]
10. Clicked Continue -- advanced to step 2

#### Step 2: Campaign Preset
11. Progress bar: [2/2 or other]
12. Three preset cards visible: [Gentle Follow-Up, Standard Follow-Up, Aggressive Follow-Up]
13. Selected "Standard Follow-Up"
14. Clicked Continue -- wizard completed

#### Completion
15. Redirected to: [/dashboard or /dashboard?onboarding=complete or other]
16. Dashboard loaded correctly: [YES/NO]

### DB verification

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| Business count after creation | 2 | [actual] | [P/F] |
| New business name | "Phase 60 Test Business" | [actual] | [P/F] |
| New business onboarding_completed_at | NOT NULL | [actual] | [P/F] |
| New business service_types_enabled | includes hvac, cleaning | [actual] | [P/F] |
| Original business unchanged | onboarding_completed_at = null | [actual] | [P/F] |
| Campaign created for new business | is_preset = false | [actual] | [P/F] |
| Campaign touches exist | 3 touches (Standard preset) | [actual] | [P/F] |

### Cleanup
- Test business deleted via SQL CASCADE
- Verified 1 business remains

### Evidence
- `qa-60-businesses-page.png` -- Businesses page with Add Business button
- `qa-60-add-biz-step1-empty.png` -- CreateBusinessWizard step 1 empty state
- `qa-60-add-biz-step1-filled.png` -- Step 1 with name + services filled
- `qa-60-add-biz-step2-presets.png` -- Step 2 preset cards
- `qa-60-add-biz-step2-selected.png` -- Step 2 with Standard Follow-Up selected
- `qa-60-add-biz-complete-dashboard.png` -- Dashboard after wizard completion

### Console errors
- [list any JS errors from browser_console_messages, or "None"]

### Bugs found
- [none or list]

---
```

Fill in all [placeholder] values with actual observed results. Leave ONB-01 and ONB-03 sections as stubs (filled by Tasks 2 and 3).
  </action>
  <verify>
1. File `docs/qa-v3.1/60-onboarding-wizard.md` exists with ONB-02 section filled in
2. Screenshots captured: qa-60-businesses-page.png, qa-60-add-biz-step1-empty.png, qa-60-add-biz-step1-filled.png, qa-60-add-biz-step2-presets.png, qa-60-add-biz-step2-selected.png, qa-60-add-biz-complete-dashboard.png
3. ONB-02 has a clear PASS or FAIL with DB verification table showing all checks
4. Test business has been cleaned up (only original business remains)
5. Active business cookie has been reset (navigated to dashboard, should fallback to original)
  </verify>
  <done>
- ONB-02 is marked PASS or FAIL with all test steps documented
- DB verification confirms new business created with correct data and original untouched
- Test business deleted, only original remains
- All 6 screenshots captured and referenced in findings document
- Console errors (if any) documented
  </done>
</task>

<task type="auto">
  <name>Task 2: ONB-03 -- Draft persistence verification</name>
  <files>docs/qa-v3.1/60-onboarding-wizard.md</files>
  <action>
This task tests that data entered and saved in step 1 of the first-business wizard persists when navigating away and back. This must run SECOND because it requires the business to have `onboarding_completed_at = null` (the original state), which ONB-01 would change.

**IMPORTANT:** Draft persistence in the OnboardingWizard is DB-backed, NOT localStorage field-level. When a user completes step 1 (clicks Continue), the data is written to the database. When the user returns to `/onboarding?step=1`, the server component fetches the business data from DB and passes it as `defaultValues` to the step component. The `onboarding-draft-v3` localStorage key stores wizard-level state, not individual form field values.

**IMPORTANT:** You must be logged in as audit-test@avisloop.com. The business must still have `onboarding_completed_at = null`.

**Step-by-step Playwright MCP walkthrough:**

**Part A: Verify pre-condition**

1. `mcp__supabase__execute_sql` -- Confirm business is still in pre-onboarding state:
```sql
SELECT id, name, onboarding_completed_at, service_types_enabled
FROM businesses
WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
```
Expected: `onboarding_completed_at = null`, `service_types_enabled = '{}'`.

**Part B: Navigate to onboarding wizard**

2. `browser_navigate` to `http://localhost:3000/onboarding`
   (Since onboarding is incomplete, this should render OnboardingWizard)
3. `browser_wait_for` -- Wait for text "Let's get your business set up" (step 1 heading)
4. `browser_snapshot` -- Verify OnboardingWizard step 1 is rendered:
   - Heading: "Let's get your business set up"
   - Business name input present
   - Service type chips present
   - Progress bar shows step 1 of 4
5. `browser_take_screenshot` -- filename: `qa-60-draft-step1-initial.png`

**Part C: Clear any existing localStorage draft**

6. `browser_evaluate` -- Clear existing draft to start fresh:
   ```
   () => { localStorage.removeItem('onboarding-draft-v3'); return 'cleared'; }
   ```

**Part D: Fill step 1 and complete it (writes to DB)**

7. `browser_snapshot` -- Get current refs for form fields
8. Find the business name input and type: "Draft Persistence Test"
   Use `browser_type` with the correct ref.
9. Find the phone number input and type: "555-999-8888"
   Use `browser_type` with the correct ref.
10. `browser_click` -- Click the "Electrical" service type chip
11. `browser_snapshot` -- Verify Electrical is selected (check icon, primary-colored)
12. `browser_take_screenshot` -- filename: `qa-60-draft-step1-filled.png`

13. `browser_click` -- Click "Continue" button
14. `browser_wait_for` -- Wait for text "Choose your follow-up approach" (step 2 heading)
15. `browser_snapshot` -- Verify step 2 loaded:
    - Heading: "Choose your follow-up approach"
    - URL should contain `?step=2`
16. `browser_take_screenshot` -- filename: `qa-60-draft-step2-reached.png`

**Part E: Navigate away from onboarding**

17. `browser_navigate` to `http://localhost:3000/jobs`
18. `browser_snapshot` -- Verify we are on the jobs page (or wherever the redirect goes -- since onboarding is incomplete, dashboard may redirect back. If so, navigate to a non-dashboard route like `/auth/login` temporarily, or just note the behavior).

    FALLBACK: If navigation to /jobs redirects back to /onboarding, use:
    `browser_evaluate` -- `() => { window.location.href = 'about:blank'; return 'navigated away'; }`
    Then navigate back.

**Part F: Return to onboarding and verify DB-backed persistence**

19. `browser_navigate` to `http://localhost:3000/onboarding?step=1`
20. `browser_wait_for` -- Wait for text "Let's get your business set up"
21. `browser_snapshot` -- Verify step 1 is rendered AND pre-filled:
    - Business name input should contain "Draft Persistence Test" (populated from DB via defaultValues)
    - Phone input should contain "555-999-8888" or formatted version
    - Electrical chip should be in selected state (pre-selected from DB)
    - This data was saved to DB when Continue was clicked in Part D
22. `browser_take_screenshot` -- filename: `qa-60-draft-step1-restored.png`

23. Record persistence result:
    - If business name, phone, and service type are pre-filled: PASS
    - If any field is empty/reset: FAIL (note which fields lost data)

**Part G: Test URL-based step navigation**

24. `browser_navigate` to `http://localhost:3000/onboarding?step=2`
25. `browser_wait_for` -- Wait for text "Choose your follow-up approach"
26. `browser_snapshot` -- Verify step 2 renders correctly via direct URL navigation
27. `browser_take_screenshot` -- filename: `qa-60-draft-step2-direct-url.png`

28. `browser_navigate` to `http://localhost:3000/onboarding?step=1`
29. `browser_snapshot` -- Verify step 1 again shows pre-filled data

**Part H: Check localStorage draft key**

30. `browser_evaluate` -- Check if localStorage draft exists:
    ```
    () => { const val = localStorage.getItem('onboarding-draft-v3'); return val ? JSON.parse(val) : 'not found'; }
    ```
    Record the value. The draft may or may not contain form field data (research indicates wizard-level state only, not per-field values). Document what is stored.

**Part I: Cleanup -- reset business to pre-onboarding state**

31. `mcp__supabase__execute_sql` -- Reset business data written during this test:
```sql
UPDATE businesses
SET name = 'Audit Test HVAC',
    phone = NULL,
    google_review_link = NULL,
    onboarding_completed_at = NULL,
    sms_consent_acknowledged = false,
    sms_consent_acknowledged_at = NULL,
    service_types_enabled = '{}',
    service_type_timing = '{"hvac": 24, "other": 24, "roofing": 72, "cleaning": 4, "handyman": 24, "painting": 48, "plumbing": 48, "electrical": 24}',
    custom_service_names = '{}'
WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
```

32. `mcp__supabase__execute_sql` -- Delete any campaigns created during this test:
```sql
DELETE FROM campaigns
WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042'
AND is_preset = false;
```

33. `mcp__supabase__execute_sql` -- Verify reset succeeded:
```sql
SELECT name, onboarding_completed_at, service_types_enabled
FROM businesses
WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
```
Expected: name = 'Audit Test HVAC', onboarding_completed_at = null, service_types_enabled = '{}'.

**Part J: Update findings document**

34. Append the ONB-03 section to `docs/qa-v3.1/60-onboarding-wizard.md`:

```markdown
## ONB-03: Draft Persistence

**Status:** [PASS/FAIL]

### Pre-conditions
- Business onboarding_completed_at = null (first-run wizard accessible)
- localStorage draft cleared before test

### Persistence mechanism
- **Type:** DB-backed (step completion writes to DB, server pre-fills on return)
- **NOT:** localStorage field-level persistence
- localStorage key `onboarding-draft-v3` stores: [describe what was found]

### Test steps

#### Fill and complete step 1
1. Navigated to /onboarding -- step 1 rendered
2. Filled business name: "Draft Persistence Test"
3. Filled phone: "555-999-8888"
4. Selected service type: Electrical
5. Clicked Continue -- step 2 loaded

#### Navigate away and return
6. Navigated away to [page]
7. Returned to /onboarding?step=1
8. Business name field value: [observed value]
9. Phone field value: [observed value]
10. Electrical chip state: [selected/not selected]
11. Persistence result: [data was/was not pre-filled from DB]

#### URL-based step navigation
12. Direct navigation to /onboarding?step=2: [rendered step 2 / failed]
13. Direct navigation to /onboarding?step=1: [rendered step 1 with pre-fill / failed]

#### localStorage inspection
14. localStorage.getItem('onboarding-draft-v3'): [value or "not found"]

### Cleanup
- Business reset to pre-onboarding state via SQL
- Test campaigns deleted
- Verified reset: name = "Audit Test HVAC", onboarding_completed_at = null

### Evidence
- `qa-60-draft-step1-initial.png` -- Step 1 before filling
- `qa-60-draft-step1-filled.png` -- Step 1 with data entered
- `qa-60-draft-step2-reached.png` -- Step 2 after Continue
- `qa-60-draft-step1-restored.png` -- Step 1 after return (persistence check)
- `qa-60-draft-step2-direct-url.png` -- Step 2 via direct URL navigation

### Bugs found
- [none or list]

---
```

Fill in all placeholders with actual results.
  </action>
  <verify>
1. ONB-03 section appended to `docs/qa-v3.1/60-onboarding-wizard.md` with all placeholders filled
2. Screenshots captured: qa-60-draft-step1-initial.png, qa-60-draft-step1-filled.png, qa-60-draft-step2-reached.png, qa-60-draft-step1-restored.png, qa-60-draft-step2-direct-url.png
3. ONB-03 has a clear PASS or FAIL with evidence of whether pre-fill worked
4. Business has been reset to pre-onboarding state (verified via SQL)
5. Test campaigns deleted
  </verify>
  <done>
- ONB-03 marked PASS or FAIL with clear evidence of persistence behavior
- DB-backed vs localStorage persistence mechanism documented
- URL-based step navigation tested and documented
- Business fully reset to pre-onboarding state for ONB-01
- All 5 screenshots captured and referenced
  </done>
</task>

<task type="auto">
  <name>Task 3: ONB-01 -- First-business onboarding wizard (4 steps) + final report</name>
  <files>docs/qa-v3.1/60-onboarding-wizard.md</files>
  <action>
This task tests the full first-business onboarding wizard (4 steps: Business Setup, Campaign Preset, CRM Platform, SMS Consent). This must run LAST because completing the wizard marks `onboarding_completed_at` on the business, changing routing behavior.

**IMPORTANT:** You must be logged in as audit-test@avisloop.com. The business must have `onboarding_completed_at = null` (reset by Task 2 cleanup).

**Step-by-step Playwright MCP walkthrough:**

**Part A: Verify pre-condition**

1. `mcp__supabase__execute_sql` -- Confirm pre-condition:
```sql
SELECT id, name, onboarding_completed_at, service_types_enabled
FROM businesses
WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
```
Expected: onboarding_completed_at = null, service_types_enabled = '{}'.

**Part B: Verify dashboard redirects to onboarding**

2. `browser_navigate` to `http://localhost:3000/dashboard`
3. `browser_wait_for` -- Wait for text "Let's get your business set up" (should redirect to /onboarding when onboarding incomplete)
   NOTE: The redirect may go directly to /onboarding, or it may show dashboard briefly then redirect. Wait for the onboarding heading.
   If the redirect does not happen (dashboard renders), manually navigate to `http://localhost:3000/onboarding`.
4. `browser_snapshot` -- Verify OnboardingWizard step 1:
   - Heading: "Let's get your business set up"
   - Subheading: "This info appears in your review request messages."
   - Business name input (may be pre-filled with "Audit Test HVAC" from DB)
   - Phone number input
   - Google review link input
   - Service type chips (all 8: HVAC through Other)
   - Progress bar: step 1 of 4
   - Continue button
5. `browser_take_screenshot` -- filename: `qa-60-onb01-step1-empty.png`
6. `browser_console_messages` (level: error) -- Check for JS errors

**Part C: Step 1 -- Business Setup**

7. `browser_snapshot` -- Get refs for all form fields

**C.1: Validation test -- empty business name**
8. First, clear the business name field if it has a default value. Click the business name input ref, select all (Ctrl+A), then delete.
   Use `browser_click` on the input ref, then `browser_press_key` with "Control+a", then `browser_press_key` with "Backspace".
9. `browser_snapshot` -- Verify business name is now empty.
10. Attempt to click Continue WITHOUT selecting any services.
    `browser_click` -- The Continue button should be `disabled` when no services are selected. If it IS disabled, verify via snapshot that it has disabled styling. If it is NOT disabled, click it and look for validation error "Select at least one service type".
11. `browser_take_screenshot` -- filename: `qa-60-onb01-step1-validation.png`

**C.2: Fill form correctly**
12. `browser_snapshot` -- Get fresh refs
13. Type "Audit HVAC Complete" into the business name input.
    Use `browser_type` with the ref.
14. Type "555-111-2222" into the phone number input.
    Use `browser_type` with the ref.
15. Type "https://g.page/r/test-audit" into the Google review link input.
    Use `browser_type` with the ref.

16. `browser_click` -- Click "HVAC" chip
17. `browser_click` -- Click "Plumbing" chip
18. `browser_snapshot` -- Verify both HVAC and Plumbing are selected (check icons, primary-colored backgrounds)
19. `browser_take_screenshot` -- filename: `qa-60-onb01-step1-filled.png`

**C.3: Test "Other" service with custom name (optional but valuable)**
20. `browser_click` -- Click "Other" chip
21. `browser_snapshot` -- Verify "Other" is selected AND custom service input appears (labeled "What type of service?" or id `custom-service`)
22. Find the custom service input and type: "Pool Cleaning"
    Use `browser_type` with the ref.
23. `browser_click` -- Click "Add" button (or `browser_press_key` with "Enter" while focused on custom service input)
24. `browser_snapshot` -- Verify "Pool Cleaning" tag appears as a pill/badge
25. `browser_take_screenshot` -- filename: `qa-60-onb01-step1-custom-service.png`

26. `browser_click` -- Click "Continue"
27. `browser_wait_for` -- Wait for text "Choose your follow-up approach" (step 2)
28. `browser_snapshot` -- Verify step 2 loaded:
    - Heading: "Choose your follow-up approach"
    - URL should contain `?step=2`
    - Progress bar: step 2 of 4
29. `browser_take_screenshot` -- filename: `qa-60-onb01-step2.png`
30. `browser_console_messages` (level: error) -- Check for JS errors

**Part D: Step 2 -- Campaign Preset**

31. `browser_snapshot` -- Get refs for preset cards
32. Verify three preset cards are visible:
    - "Gentle Follow-Up" -- "Two emails over 3 days..."
    - "Standard Follow-Up" -- "Two emails and a text message over 7 days..."
    - "Aggressive Follow-Up" -- "A text within hours, then email and SMS reminders..."
33. Verify Continue button is disabled (no preset selected yet).

34. `browser_click` -- Click "Gentle Follow-Up" card
35. `browser_snapshot` -- Verify card is selected (ring-2 styling)
36. `browser_take_screenshot` -- filename: `qa-60-onb01-step2-selected.png`

37. `browser_click` -- Click "Continue"
38. `browser_wait_for` -- Wait for text "What software do you use to manage jobs?" (step 3 heading)
39. `browser_snapshot` -- Verify step 3 loaded:
    - Heading: "What software do you use to manage jobs?"
    - Subheading: "This helps us plan integrations. Skip if you're unsure."
    - Info banner: "This is for our roadmap planning only. No integration will be set up now."
    - CRM platform cards visible (Jobber, Housecall Pro, ServiceTitan, GorillaDesk, FieldPulse, None, Other)
    - "Skip without saving" link visible
    - Progress bar: step 3 of 4
40. `browser_take_screenshot` -- filename: `qa-60-onb01-step3.png`
41. `browser_console_messages` (level: error) -- Check for JS errors

**Part E: Step 3 -- CRM Platform (document the software_used bug)**

**E.1: Test "Skip without saving" path first (go back to step 3 after)**
42. `browser_click` -- Click "Skip without saving" link
43. `browser_wait_for` -- Wait for text "SMS consent requirements" (step 4 heading)
44. `browser_snapshot` -- Verify step 4 loaded after skipping step 3
45. `browser_take_screenshot` -- filename: `qa-60-onb01-step3-skip-result.png`

**E.2: Go back to step 3 to test Continue path**
46. Look for "Back" link/button on step 4. `browser_snapshot` to find it.
47. `browser_click` -- Click "Back" to return to step 3
48. `browser_wait_for` -- Wait for text "What software do you use to manage jobs?"
49. `browser_snapshot` -- Verify back on step 3

**E.3: Test Continue with CRM selection (known bug: software_used column missing)**
50. `browser_click` -- Click "Jobber" card
51. `browser_snapshot` -- Verify Jobber is selected
52. `browser_take_screenshot` -- filename: `qa-60-onb01-step3-jobber-selected.png`

53. `browser_click` -- Click "Continue"
54. `browser_console_messages` (level: error) -- IMPORTANT: Capture any errors. The `saveSoftwareUsed()` action will fail because `software_used` column doesn't exist in DB. Check if any error appears in console.
55. `browser_wait_for` -- Wait for text "SMS consent requirements" (step 4 should still load despite the silent failure)
56. `browser_snapshot` -- Verify step 4 loaded even though software_used save failed
57. `browser_take_screenshot` -- filename: `qa-60-onb01-step3-continue-result.png`

58. Record BUG-ONB-01 findings:
    - Step 3 Continue advances to step 4 even when DB write fails
    - Console errors (if any) from the failed save
    - No user-visible error message (silent failure)

**Part F: Step 4 -- SMS Consent**

59. `browser_snapshot` -- Verify step 4:
    - Heading: "SMS consent requirements"
    - Subheading about sending text messages
    - Requirements card with "Key requirements:" heading
    - SMS consent checkbox (unchecked by default)
    - "Complete Setup" button (should be disabled when checkbox unchecked)
    - Progress bar: step 4 of 4
60. `browser_take_screenshot` -- filename: `qa-60-onb01-step4.png`

**F.1: Validation test -- try to complete without checkbox**
61. Verify "Complete Setup" button is disabled. `browser_snapshot` -- check if button has disabled attribute or disabled styling.
62. If the button is clickable despite checkbox being unchecked:
    `browser_click` -- Click "Complete Setup"
    `browser_wait_for` -- Wait for text "You must acknowledge SMS consent requirements"
    `browser_snapshot` -- Verify toast error appears
    `browser_take_screenshot` -- filename: `qa-60-onb01-step4-validation.png`
    If button is properly disabled and not clickable, note "button correctly disabled when unchecked" and skip the error check.

**F.2: Check the checkbox and complete**
63. `browser_snapshot` -- Get ref for the SMS consent checkbox
64. `browser_click` -- Click the checkbox (ref should be for `sms-consent-acknowledgment` or the label text)
65. `browser_snapshot` -- Verify:
    - Checkbox is now checked
    - "Complete Setup" button is now enabled
66. `browser_take_screenshot` -- filename: `qa-60-onb01-step4-checked.png`

67. `browser_click` -- Click "Complete Setup"
68. `browser_wait_for` -- Wait for dashboard content. The wizard should redirect to `/dashboard?onboarding=complete`. Wait for any dashboard text like "Dashboard" or KPI content. Max 15 seconds.
69. `browser_snapshot` -- Verify:
    - URL contains `/dashboard`
    - Dashboard content is visible (KPIs, welcome card, etc.)
    - No error messages
70. `browser_take_screenshot` -- filename: `qa-60-onb01-complete-dashboard.png`
71. `browser_console_messages` (level: error) -- Check for JS errors during completion

**Part G: DB verification -- wizard completion**

72. `mcp__supabase__execute_sql` -- Verify business is now onboarding-complete:
```sql
SELECT name, onboarding_completed_at, service_types_enabled, service_type_timing,
       sms_consent_acknowledged, sms_consent_acknowledged_at, custom_service_names,
       phone, google_review_link
FROM businesses
WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
```
Expected:
- name = 'Audit HVAC Complete'
- onboarding_completed_at IS NOT NULL
- service_types_enabled includes 'hvac', 'plumbing', 'other'
- sms_consent_acknowledged = true
- sms_consent_acknowledged_at IS NOT NULL
- custom_service_names includes 'Pool Cleaning'
- phone = '555-111-2222' or formatted version
- google_review_link = 'https://g.page/r/test-audit'

73. `mcp__supabase__execute_sql` -- Verify campaign created:
```sql
SELECT id, name, service_type, status, is_preset, created_at
FROM campaigns
WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042'
AND is_preset = false
ORDER BY created_at DESC;
```
Expected: At least 1 row with is_preset = false (the Gentle Follow-Up campaign).

74. `mcp__supabase__execute_sql` -- Verify campaign touches:
```sql
SELECT ct.touch_number, ct.channel, ct.delay_hours
FROM campaign_touches ct
JOIN campaigns c ON c.id = ct.campaign_id
WHERE c.business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042'
AND c.is_preset = false
ORDER BY c.created_at DESC, ct.touch_number;
```
Expected: 2 touches (Gentle/Conservative preset has 2 touches).

75. `mcp__supabase__execute_sql` -- Verify software_used column does NOT exist (confirms bug):
```sql
SELECT column_name
FROM information_schema.columns
WHERE table_name = 'businesses' AND column_name = 'software_used';
```
Expected: 0 rows (column does not exist).

**Part H: Responsive checks**

76. `browser_navigate` to `http://localhost:3000/onboarding`
    NOTE: Since onboarding is now complete, this may redirect to dashboard. If it does, use the `?mode=new` path to check wizard rendering at different viewports, OR skip responsive checks for the first-business wizard and note that OnboardingWizard cannot be accessed after completion.

    FALLBACK: If redirected, test responsive on the additional business wizard path instead:
    `browser_navigate` to `http://localhost:3000/onboarding?mode=new`

77. `browser_resize` to 768x1024 (tablet)
78. `browser_snapshot` -- Verify wizard renders correctly in tablet viewport
79. `browser_take_screenshot` -- filename: `qa-60-onboarding-tablet.png`

80. `browser_resize` to 390x844 (mobile)
81. `browser_snapshot` -- Verify wizard is usable on mobile:
    - Form fields are full-width
    - Service chips wrap correctly
    - Progress bar visible
    - Buttons are reachable
82. `browser_take_screenshot` -- filename: `qa-60-onboarding-mobile.png`

83. `browser_resize` to 1440x900 (reset to desktop)

**Part I: Cleanup -- reset business for future test runs**

84. `mcp__supabase__execute_sql` -- Reset business to pre-onboarding state:
```sql
UPDATE businesses
SET name = 'Audit Test HVAC',
    phone = NULL,
    google_review_link = NULL,
    onboarding_completed_at = NULL,
    sms_consent_acknowledged = false,
    sms_consent_acknowledged_at = NULL,
    service_types_enabled = '{}',
    service_type_timing = '{"hvac": 24, "other": 24, "roofing": 72, "cleaning": 4, "handyman": 24, "painting": 48, "plumbing": 48, "electrical": 24}',
    custom_service_names = '{}'
WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
```

85. `mcp__supabase__execute_sql` -- Delete test campaigns:
```sql
DELETE FROM campaigns
WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042'
AND is_preset = false;
```

86. `mcp__supabase__execute_sql` -- Verify reset:
```sql
SELECT name, onboarding_completed_at, service_types_enabled
FROM businesses
WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
```
Expected: name = 'Audit Test HVAC', onboarding_completed_at = null, service_types_enabled = '{}'.

**Part J: Finalize findings document**

87. Update `docs/qa-v3.1/60-onboarding-wizard.md` -- Append the ONB-01 section and finalize:

```markdown
## ONB-01: First-Business Onboarding Wizard (4 Steps)

**Status:** [PASS/FAIL]

### Pre-conditions
- Business onboarding_completed_at = null
- No existing non-preset campaigns for this business

### Test steps

#### Dashboard redirect
1. Navigated to /dashboard
2. Redirect to /onboarding: [YES (automatic) / NO (manual navigation needed)]

#### Step 1: Business Setup
3. Heading: [observed heading]
4. Progress bar: [1/4 or other]
5. Form fields present: [business name, phone, google review link, service chips]
6. Validation (empty name): [disabled button / error message / describe]
7. Filled: "Audit HVAC Complete", "555-111-2222", "https://g.page/r/test-audit"
8. Selected services: HVAC, Plumbing, Other
9. Custom service: "Pool Cleaning" tag added
10. Continue clicked -- advanced to step 2

#### Step 2: Campaign Preset
11. Heading: "Choose your follow-up approach"
12. Progress bar: [2/4 or other]
13. Preset cards visible: [list cards seen]
14. Selected: "Gentle Follow-Up"
15. Continue clicked -- advanced to step 3

#### Step 3: CRM Platform
16. Heading: "What software do you use to manage jobs?"
17. Progress bar: [3/4 or other]
18. Info banner present: [YES/NO]
19. Skip path tested: "Skip without saving" -- [advanced to step 4 / failed]
20. Back navigation: returned to step 3 -- [YES/NO]
21. Selected: "Jobber"
22. Continue clicked -- [advanced to step 4 / failed]
23. **BUG-ONB-01:** software_used column missing -- see Bugs section

#### Step 4: SMS Consent
24. Heading: "SMS consent requirements"
25. Progress bar: [4/4 or other]
26. Checkbox unchecked validation: [button disabled / toast error / describe]
27. Checked checkbox
28. "Complete Setup" clicked -- [redirected to dashboard / failed]

#### Completion
29. Dashboard URL: [/dashboard?onboarding=complete or other]
30. Dashboard rendered: [YES/NO, describe what's visible]

### DB verification

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| onboarding_completed_at | NOT NULL | [actual] | [P/F] |
| name | "Audit HVAC Complete" | [actual] | [P/F] |
| service_types_enabled | {hvac, plumbing, other} | [actual] | [P/F] |
| custom_service_names | {Pool Cleaning} | [actual] | [P/F] |
| sms_consent_acknowledged | true | [actual] | [P/F] |
| phone | 555-111-2222 | [actual] | [P/F] |
| google_review_link | https://g.page/r/test-audit | [actual] | [P/F] |
| Campaign created (is_preset=false) | 1 row | [actual] | [P/F] |
| Campaign touches | 2 (Gentle preset) | [actual] | [P/F] |
| software_used column exists | NO (bug) | [actual] | [P/F] |

### Responsive

| Viewport | Status | Notes |
|----------|--------|-------|
| Desktop (1440x900) | [result] | |
| Tablet (768x1024) | [result] | |
| Mobile (390x844) | [result] | |

### Cleanup
- Business reset to pre-onboarding state
- Test campaigns deleted
- Verified: name = "Audit Test HVAC", onboarding_completed_at = null

### Console errors
- [list any JS errors, or "None"]
- Step 3 Continue console errors: [list any errors from software_used failure]

### Evidence
- `qa-60-onb01-step1-empty.png` -- Step 1 initial state
- `qa-60-onb01-step1-validation.png` -- Step 1 validation (disabled/error)
- `qa-60-onb01-step1-filled.png` -- Step 1 fully filled
- `qa-60-onb01-step1-custom-service.png` -- Custom service name added
- `qa-60-onb01-step2.png` -- Step 2 Campaign Preset
- `qa-60-onb01-step2-selected.png` -- Preset selected
- `qa-60-onb01-step3.png` -- Step 3 CRM Platform
- `qa-60-onb01-step3-skip-result.png` -- After skip
- `qa-60-onb01-step3-jobber-selected.png` -- Jobber card selected
- `qa-60-onb01-step3-continue-result.png` -- After Continue (bug test)
- `qa-60-onb01-step4.png` -- Step 4 SMS Consent
- `qa-60-onb01-step4-validation.png` -- Validation (disabled/error)
- `qa-60-onb01-step4-checked.png` -- Checkbox checked
- `qa-60-onb01-complete-dashboard.png` -- Dashboard after completion
- `qa-60-onboarding-tablet.png` -- Tablet viewport
- `qa-60-onboarding-mobile.png` -- Mobile viewport

---

## Bugs Found

### BUG-ONB-01: software_used column missing from businesses table

**Severity:** Medium
**Component:** CRM Platform step (step 3 of first-business wizard)
**File:** `lib/actions/onboarding.ts` -- `saveSoftwareUsed()`

**Description:**
The CRM Platform step in the first-business onboarding wizard calls `saveSoftwareUsed()` which tries to UPDATE the `software_used` column on the `businesses` table. However, this column does not exist in the database (verified via `information_schema.columns` query). The update fails silently -- the wizard advances to step 4 with no error visible to the user.

**Impact:**
- User's CRM platform choice is never saved
- PRODUCT-003 decision (CRM Platform Step in Onboarding) cannot fulfill its stated purpose ("Know which integrations to build first")
- No UX impact (wizard functions correctly, just doesn't persist the data)

**Reproduction:**
1. Start first-business onboarding wizard
2. Reach step 3 (CRM Platform)
3. Select any CRM platform (e.g., Jobber)
4. Click Continue
5. Wizard advances to step 4 -- no error shown
6. Query DB: `SELECT column_name FROM information_schema.columns WHERE table_name = 'businesses' AND column_name = 'software_used';` -- returns 0 rows

**Console errors:** [insert actual console errors captured during test]

**Fix suggestion:** Add migration: `ALTER TABLE businesses ADD COLUMN software_used TEXT;`

---

[List any other bugs found during testing]

---

## Responsive & Accessibility

### Wizard responsive behavior
| Viewport | Status | Notes |
|----------|--------|-------|
| Desktop (1440x900) | [result] | |
| Tablet (768x1024) | [result] | |
| Mobile (390x844) | [result] | |

### Accessibility observations
- Service type chips: [keyboard navigable? role attributes?]
- Campaign preset cards: [role="radio", aria-checked observed?]
- SMS consent checkbox: [proper label association?]
- Progress bar: [role="progressbar" with aria-valuenow?]

---

## Overall Assessment

[Summary paragraph covering:
- How many requirements passed (ONB-01, ONB-02, ONB-03)
- Critical bugs found (BUG-ONB-01 and any others)
- Wizard usability observations
- Responsive behavior summary
- Readiness for next QA phase]
```

Fill in ALL [placeholder] values with actual observed results. Update the Summary table at the top of the document with final statuses for all 3 requirements. Ensure no placeholder text remains.
  </action>
  <verify>
1. `docs/qa-v3.1/60-onboarding-wizard.md` has all 3 requirement sections filled (ONB-01, ONB-02, ONB-03)
2. Summary table at top shows status for all 3 requirements
3. Screenshots captured: at least 16 screenshots (qa-60-onb01-step1-empty, step1-validation, step1-filled, step1-custom-service, step2, step2-selected, step3, step3-skip-result, step3-jobber-selected, step3-continue-result, step4, step4-validation/checked, complete-dashboard, tablet, mobile)
4. BUG-ONB-01 (software_used) documented with reproduction steps and DB evidence
5. DB verification tables filled with actual values for all checks
6. Business has been reset to pre-onboarding state
7. No placeholder text remains in the document
8. Overall Assessment section exists with readiness statement
  </verify>
  <done>
- ONB-01 marked PASS or FAIL with all 4 steps tested and all DB verifications completed
- BUG-ONB-01 documented with severity, reproduction steps, console errors, and fix suggestion
- Responsive checks completed (tablet + mobile)
- Business fully reset to pre-onboarding state for future test runs
- All screenshots captured and referenced
- Summary table updated with final statuses for ONB-01, ONB-02, ONB-03
- Overall Assessment written with readiness for next QA phase
- Findings document is complete with no remaining placeholders
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. Read `docs/qa-v3.1/60-onboarding-wizard.md` in full
2. Confirm the Summary table has all 3 requirements with PASS/FAIL status
3. Confirm at least 17 screenshots were captured across all tasks
4. Confirm no placeholder text remains in the document
5. Confirm BUG-ONB-01 (software_used) is documented with clear reproduction steps
6. Confirm DB verification tables are filled with actual values (not placeholders)
7. Confirm the business has been reset to pre-onboarding state (onboarding_completed_at = null)
8. Confirm the Overall Assessment section exists and provides a readiness statement for the next QA phase
9. If any requirement is FAIL, confirm the issue is clearly described with evidence
</verification>

<success_criteria>
- All 3 onboarding requirements (ONB-01, ONB-02, ONB-03) have been tested and have a documented status
- The findings document `docs/qa-v3.1/60-onboarding-wizard.md` is complete with evidence
- ONB-01: All 4 wizard steps exercised, business + campaign created in DB, dashboard redirect confirmed
- ONB-02: 2-step wizard completed, new business created without affecting original, cleanup performed
- ONB-03: DB-backed persistence verified (step 1 data pre-filled after navigation round-trip)
- BUG-ONB-01 (software_used column missing) documented with severity, reproduction, and fix suggestion
- Business reset to pre-onboarding state after all tests
- Any additional bugs found are recorded with clear reproduction steps
</success_criteria>

<output>
After completion, create `.planning/phases/60-onboarding-wizard/60-01-SUMMARY.md`
</output>
