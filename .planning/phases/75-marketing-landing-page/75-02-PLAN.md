---
phase: 75-marketing-landing-page
plan: 02
type: execute
wave: 2
depends_on:
  - 75-01
files_modified:
  - app/(marketing)/page.tsx
  - app/(marketing)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage at / shows hero, services overview, pricing cards (Basic $199/mo, Advanced $299/mo, Review add-on $99/mo), and a CTA — positioned as web design agency"
    - "Existing homepage content is accessible at /reputation with no broken links"
    - "Landing page renders correctly at 375px mobile — no horizontal overflow, no truncated hero or pricing text"
    - "Nav links in layout.tsx resolve to correct anchors on the new homepage"
  artifacts:
    - path: "app/(marketing)/page.tsx"
      provides: "New web design agency homepage"
      contains: "HeroWebDesign"
    - path: "app/(marketing)/layout.tsx"
      provides: "Updated nav with Reputation link and correct anchor targets"
      contains: "reputation"
  key_links:
    - from: "app/(marketing)/layout.tsx"
      to: "/#pricing"
      via: "nav Pricing link — must point to section id=pricing in new page"
      pattern: "/#pricing"
    - from: "app/(marketing)/page.tsx"
      to: "components/marketing/v3/hero-webdesign.tsx"
      via: "import HeroWebDesign"
      pattern: "HeroWebDesign"
    - from: "app/(marketing)/layout.tsx"
      to: "/reputation"
      via: "Reputation nav link added so users can reach the existing review service page"
      pattern: "/reputation"
---

<objective>
Assemble the new web design agency homepage by replacing app/(marketing)/page.tsx with v3 components, and update the marketing layout nav to reflect the new site structure.

Purpose: Plan 01 created all v3 components and the /reputation route. This plan wires them together: new page.tsx imports and renders the v3 components in the correct order; layout.tsx nav is updated so existing anchors (/#pricing, /#faq) still resolve and the /reputation page is discoverable.

Output: The homepage at / is now a web design agency landing page. The marketing nav is updated. All success criteria for Phase 75 are met.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-marketing-landing-page/75-01-SUMMARY.md
@app/(marketing)/page.tsx
@app/(marketing)/layout.tsx
@components/marketing/v3/hero-webdesign.tsx
@components/marketing/v3/pricing-webdesign.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace page.tsx with web design agency homepage</name>
  <files>app/(marketing)/page.tsx</files>
  <action>
    Rewrite app/(marketing)/page.tsx entirely. The new page imports and assembles the v3 components from Plan 01 plus the existing shared components (Testimonials, CTASection) that are content-agnostic. Do NOT import any v2/ components (HeroV2, ServicesSection, HowItWorksSection, etc.) — those are now only used in /reputation.

    Metadata block:
    - title: "AvisLoop — Web Design for Home Service Businesses | HVAC, Plumbing, Electrical"
    - description: "Professional websites for HVAC, plumbing, electrical, and home service businesses. Starting at $199/month. No upfront cost, no contracts. Includes ongoing maintenance and a client portal for revision requests."
    - keywords: ["web design for home services", "HVAC website design", "plumbing company website", "electrician website", "home service website", "managed web design", "Austin web design agency"]
    - openGraph: title and description matching above, url: baseUrl, siteName: "AvisLoop", type: "website"
    - twitter: card: "summary_large_image", title and description matching

    Add a new webDesignFaqSchema JSON-LD block for the web design FAQs (reuse the same 8 Q&A pairs from the faq-webdesign.tsx component — FAQ schema helps SEO). Include it via a script tag with type="application/ld+json".

    JSX structure (component order):
    1. HeroWebDesign — from components/marketing/v3/hero-webdesign
    2. ServicesWebDesign — from components/marketing/v3/services-webdesign
    3. ProcessSection — from components/marketing/v3/process-section
    4. Testimonials — from components/marketing/testimonials (reuse existing — testimonials are generic enough; do not modify the Testimonials component)
    5. PricingWebDesign — from components/marketing/v3/pricing-webdesign
    6. FAQWebDesign — from components/marketing/v3/faq-webdesign
    7. CTASection — from components/marketing/cta-section (reuse existing — the CTA copy "Your next customer is reading your reviews right now" is still valid for a web design agency context)

    The page is a Server Component (no 'use client'). baseUrl logic for metadata is identical to the old page.tsx.

    Important: Do NOT add SocialProofStrip, WhyAvisLoop, AnimatedStatsSection, or any other v2-only component to this page.
  </action>
  <verify>
    Check app/(marketing)/page.tsx imports:
    - grep "HeroWebDesign" app/(marketing)/page.tsx — must appear
    - grep "PricingWebDesign" app/(marketing)/page.tsx — must appear
    - grep "FAQWebDesign" app/(marketing)/page.tsx — must appear
    - grep "v2/" app/(marketing)/page.tsx — must NOT appear
    - grep "HeroV2" app/(marketing)/page.tsx — must NOT appear

    Run pnpm typecheck — must pass.
  </verify>
  <done>app/(marketing)/page.tsx imports only v3 components (plus Testimonials, CTASection), has updated metadata for web design agency positioning, no v2 component imports. Typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update marketing layout nav for new site structure</name>
  <files>app/(marketing)/layout.tsx</files>
  <action>
    Update app/(marketing)/layout.tsx nav links to match the new homepage structure. The existing layout has these nav links: Services (/#features), How It Works (/#how-it-works), Pricing (/#pricing), FAQ (/#faq).

    Required changes:
    1. Update "Services" link: change href from /#features to /#services (the new ServicesWebDesign section should have id="services" — if Plan 01's services-webdesign.tsx does not have this id, update services-webdesign.tsx to add id="services" to the outer section element)
    2. Keep "How It Works" link: change href from /#how-it-works to /#process (the new ProcessSection should have id="process" — if Plan 01's process-section.tsx does not have this id, update it to add id="process")
    3. Keep "Pricing" link at /#pricing — unchanged (pricing-webdesign.tsx already has id="pricing" per Plan 01)
    4. Keep "FAQ" link at /#faq — the new faq-webdesign.tsx should have id="faq" on its section — if not, update faq-webdesign.tsx to add id="faq"
    5. Add new nav link "Reputation" → /reputation — position it after FAQ, before the Book a Call CTA button. Use Button variant="ghost" size="sm" wrapping a Link, matching the pattern of other nav links. Label: "Reputation" or "Review Service" — choose whichever fits the nav spacing.

    Footer changes:
    - Update the footer "What We Do" link from /#features to /#services
    - Update the footer "How It Works" link from /#how-it-works to /#process
    - Add a footer link to /reputation under the Service column (label: "Review Service")
    - Update the footer description text from "Managed Google review service for HVAC..." to "Professional websites and reputation management for HVAC, plumbing, electrical, and home service businesses."
    - Update the footer copyright line bottom text from "Google review management for home service businesses" to "Web design and reputation management for home service businesses"

    Mobile nav: The MobileNav component is in components/marketing/mobile-nav.tsx. Read that file first to understand its structure, then determine if it hardcodes nav items or receives them as props. If it hardcodes items, update it to match the same nav structure (Services → /#services, How It Works → /#process, Pricing → /#pricing, FAQ → /#faq, Reputation → /reputation). If it reads from a shared config, update the config.

    The "Book a Call" CTA button in the navbar header stays as-is (href="/#pricing").

    Run pnpm lint after all changes.
  </action>
  <verify>
    - grep "/reputation" app/(marketing)/layout.tsx — must appear (at least once for the nav link)
    - grep "/#pricing" app/(marketing)/layout.tsx — must still appear (CTA button stays)
    - grep "/#features" app/(marketing)/layout.tsx — must NOT appear (replaced by /#services)
    - pnpm lint — must pass
    - pnpm typecheck — must pass
  </verify>
  <done>layout.tsx nav has a Reputation link to /reputation, anchor links updated to /#services and /#process, footer updated with web design copy. Lint and typecheck pass.</done>
</task>

</tasks>

<verification>
Final checks after both tasks:

1. pnpm typecheck — must pass with no errors
2. pnpm lint — must pass with no errors
3. grep "HeroV2" app/(marketing)/page.tsx — must return nothing (v2 not in new page)
4. grep "HeroWebDesign" app/(marketing)/page.tsx — must return a match
5. grep "/reputation" app/(marketing)/layout.tsx — must return a match
6. grep "\$199" components/marketing/v3/pricing-webdesign.tsx — must return a match
7. cat app/(marketing)/reputation/page.tsx | grep "HeroV2" — must return a match (reputation page still has v2 content)

Smoke test mentally: navigating / renders web design content. Navigating /reputation renders the old homepage content. The /reputation route is reachable from the nav.
</verification>

<success_criteria>
- pnpm typecheck and pnpm lint both pass
- app/(marketing)/page.tsx positions AvisLoop as a web design agency (HeroWebDesign, ServicesWebDesign, PricingWebDesign, FAQWebDesign)
- app/(marketing)/reputation/page.tsx preserves all existing review/reputation content (HeroV2, SocialProofStrip, etc.) — identical to the old homepage
- Marketing nav includes a link to /reputation
- All anchor hrefs (/#pricing, /#services, /#process, /#faq) resolve to sections in the new homepage
- No v2 components imported in the new page.tsx
- No lucide-react imports in any v3 component or updated layout
</success_criteria>

<output>
After completion, create .planning/phases/75-marketing-landing-page/75-02-SUMMARY.md
</output>
