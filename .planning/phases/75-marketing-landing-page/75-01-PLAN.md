---
phase: 75-marketing-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(marketing)/reputation/page.tsx
  - components/marketing/v3/hero-webdesign.tsx
  - components/marketing/v3/services-webdesign.tsx
  - components/marketing/v3/pricing-webdesign.tsx
  - components/marketing/v3/process-section.tsx
  - components/marketing/v3/faq-webdesign.tsx
autonomous: true

must_haves:
  truths:
    - "Existing homepage content (reputation/review copy) is accessible at /reputation with no broken links or missing sections"
    - "Homepage at / positions AvisLoop as a web design agency, not a review request tool"
    - "Pricing section shows all three offerings: Basic $199/mo (1-4 pages, 2 revisions/mo), Advanced $299/mo (4-10 pages, 4 revisions/mo), and Review add-on $99/mo"
    - "Landing page renders without horizontal overflow at 375px mobile viewport"
    - "All components use Kumbh Sans, warm palette (--accent token), and Phosphor icons — no new fonts, color libraries, or icon sets"
  artifacts:
    - path: "app/(marketing)/reputation/page.tsx"
      provides: "Route that renders existing homepage content intact"
      contains: "HeroV2"
    - path: "components/marketing/v3/hero-webdesign.tsx"
      provides: "Web design agency hero section"
      min_lines: 40
    - path: "components/marketing/v3/services-webdesign.tsx"
      provides: "Services overview for web design offering"
      min_lines: 30
    - path: "components/marketing/v3/pricing-webdesign.tsx"
      provides: "Three-card pricing section (Basic, Advanced, Review add-on)"
      min_lines: 60
    - path: "components/marketing/v3/process-section.tsx"
      provides: "How it works section for web design"
      min_lines: 30
    - path: "components/marketing/v3/faq-webdesign.tsx"
      provides: "FAQ section addressing web design objections"
      min_lines: 40
  key_links:
    - from: "app/(marketing)/reputation/page.tsx"
      to: "components/marketing/v2/hero-v2.tsx"
      via: "import HeroV2 — same components as old page.tsx"
      pattern: "HeroV2"
    - from: "components/marketing/v3/pricing-webdesign.tsx"
      to: "https://calendly.com"
      via: "CTA links pointing to existing Calendly URL pattern"
      pattern: "calendly\\.com"
---

<objective>
Create the /reputation route (preserving existing homepage content) and build all v3 marketing components for the web design agency homepage.

Purpose: Phase 74 is complete — all web design agency features exist and can be accurately described in copy. This plan creates the content foundation: the /reputation preservation route and the six v3 components that will compose the new homepage. Plan 02 assembles these into the new page.tsx.

Output: app/(marketing)/reputation/page.tsx (existing content, new URL), and six components/marketing/v3/ files (hero, services, pricing, process, FAQ — all web-design-agency themed, all using the existing brand design system).
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<!-- Key source files to understand before writing new components -->
@app/(marketing)/page.tsx
@app/(marketing)/layout.tsx
@components/marketing/v2/hero-v2.tsx
@components/marketing/v2/pricing-section.tsx
@components/marketing/faq-section.tsx
@components/marketing/cta-section.tsx
@components/marketing/testimonials.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /reputation route preserving existing homepage content</name>
  <files>app/(marketing)/reputation/page.tsx</files>
  <action>
    Create app/(marketing)/reputation/page.tsx as a copy of the current app/(marketing)/page.tsx, with three adjustments only:

    1. Update metadata: title → "AvisLoop — Managed Google Review Service for Home Service Businesses | Reputation Management", add canonical URL pointing to /reputation, keep all existing keywords and OG/twitter fields unchanged.
    2. Keep the faqSchema JSON-LD unchanged (it is reputation-focused and belongs here).
    3. Keep all component imports identical to the current page.tsx — import HeroV2, SocialProofStrip, ServicesSection, HowItWorksSection, WhyAvisLoop, AnimatedStatsSection, Testimonials, PricingSection, FAQSection, CTASection from their current paths under components/marketing/v2/ and components/marketing/. Do not modify those v2 components.

    The page body (JSX) must be identical to the current page.tsx — same component order, same script tag with faqSchema. Do NOT add a "See our web design services" banner or any new UI — the reputation page should be the existing page verbatim except for the metadata changes described above.

    Result: visiting /reputation renders exactly what / used to render.
  </action>
  <verify>Check that app/(marketing)/reputation/page.tsx exists and contains HeroV2, SocialProofStrip, PricingSection in its JSX. Check that it does NOT import anything from components/marketing/v3/.</verify>
  <done>app/(marketing)/reputation/page.tsx exists, imports all v2 components, and the metadata title references "Reputation Management".</done>
</task>

<task type="auto">
  <name>Task 2: Build v3 hero, services, and process components</name>
  <files>
    components/marketing/v3/hero-webdesign.tsx
    components/marketing/v3/services-webdesign.tsx
    components/marketing/v3/process-section.tsx
  </files>
  <action>
    Create components/marketing/v3/ directory and build three components. All must use 'use client' only if they need client-side state (prefer Server Components). All must follow the existing v2 component patterns: FadeIn from @/components/ui/fade-in, Phosphor icons from @phosphor-icons/react, Button from @/components/ui/button, Tailwind with the existing design tokens (--accent, --foreground, --muted-foreground, --card, --border).

    --- hero-webdesign.tsx ---
    Hero section positioning AvisLoop as a web design agency for home service businesses. Structure:
    - Section label (small caps, text-accent): "Web Design for Home Service Businesses"
    - H1: Something like "Your Website Should Be Working While You're On The Job" — bold, 4xl-6xl, tight leading
    - Subtitle (text-muted-foreground, lg): 2-3 sentences. Position against generalist agencies: "We build and maintain professional websites for HVAC, plumbing, electrical, and roofing companies. Starting at $199/month — no upfront cost, no contracts, no technical knowledge needed."
    - Two CTAs: Primary "Book a Free Call" → Calendly URL (https://calendly.com/anthony-le-avisloop/avisloop-demo-onboarding-call), Secondary "See Pricing" → /#pricing anchor. Use Button with size="lg".
    - Trust line below CTAs: small text showing e.g. "✓ No upfront cost  ✓ Cancel anytime  ✓ Includes ongoing maintenance"
    - Layout: single column on mobile, two-column grid on lg (text left, visual right). The right column can be a styled placeholder card showing a mock website card (simple div with rounded-2xl border bg-card p-6 showing fake site stats — "Live for 127 days", "4 revisions completed", "Next billing Apr 15") — this shows the product's value at a glance without needing real screenshots.
    - Wrap all in FadeIn direction="up"

    --- services-webdesign.tsx ---
    Services overview section. Structure:
    - Section label (text-accent, small caps): "What's Included"
    - H2: "Everything Your Website Needs, Managed For You"
    - 3-column feature grid (1-col mobile, 3-col lg). Each card: Phosphor icon (Globe, Wrench, Star, etc.), bold title, 2-sentence description. Services to highlight:
      1. "Professional Website" — custom-designed, mobile-optimized site built for your service area
      2. "Monthly Revision Requests" — submit change requests through your client portal; Basic gets 2/mo, Advanced gets 4/mo
      3. "Ongoing Maintenance" — hosting, updates, and technical management included in monthly fee
      4. "Review Automation Add-On" — optional $99/mo add-on: automated Google review campaigns, AI responses, review funnel protection (links conceptually to /reputation page)
      5. "Client Portal" — your own bookmarkable URL to submit requests, track status, and see what's been done
      6. "No Upfront Cost" — no one-time design fees; subscription covers everything
    - Cards use rounded-xl border border-border/40 bg-card p-6 pattern (matching existing card style)

    --- process-section.tsx ---
    "How It Works" section. 4-step numbered list. Structure:
    - Section label + H2: "Up and Running in Days, Not Months"
    - Numbered steps in a vertical list (mobile) or 2x2 grid (md+):
      1. "Book a Free Strategy Call" — we learn about your business, service area, and what your current website (or lack of one) is costing you
      2. "We Build Your Site" — professional design configured for your service types and local market; you review and approve
      3. "Go Live" — we deploy to your domain and handle all the technical setup; you get your client portal link
      4. "Submit Revisions Anytime" — need to update hours, add a service, or change pricing? Submit a request through your portal; we handle it within 48 hours
    - Each step: number badge (rounded-full bg-accent/10 text-accent font-bold), title, description
    - Bottom CTA: "Book a Free Strategy Call" button → Calendly URL
  </action>
  <verify>
    Check files exist:
    - components/marketing/v3/hero-webdesign.tsx
    - components/marketing/v3/services-webdesign.tsx
    - components/marketing/v3/process-section.tsx

    Grep for Phosphor icon imports in each file. Grep for "calendly.com" in hero-webdesign.tsx. Confirm none of the three files import from lucide-react.
  </verify>
  <done>Three v3 components exist, each imports from @phosphor-icons/react (not lucide-react), hero has dual CTAs with Calendly URL, services lists 6 feature cards, process lists 4 numbered steps.</done>
</task>

<task type="auto">
  <name>Task 3: Build v3 pricing and FAQ components</name>
  <files>
    components/marketing/v3/pricing-webdesign.tsx
    components/marketing/v3/faq-webdesign.tsx
  </files>
  <action>
    --- pricing-webdesign.tsx ---
    Three-card pricing section. This is the most critical component for MKT-03 compliance.

    Structure:
    - id="pricing" on the section element (nav anchor links here)
    - Section label + H2: "Simple Monthly Pricing"
    - Subtitle: "No setup fees. No contracts. Cancel anytime."
    - Three cards in a responsive grid: 1-col mobile, 3-col lg. Cards at lg are equal height (use grid, not flex).

    Card 1 — Basic ($199/mo):
    - Badge: "Basic"
    - Price: $199 /month (large, bold)
    - Tagline: "For small service businesses"
    - Feature list with Check icons:
      - 1–4 page professional website
      - Mobile-optimized design
      - 2 revision requests per month
      - Client portal access
      - Ongoing hosting and maintenance
      - Domain setup included
    - CTA button: "Book a Call" → Calendly URL
    - Card styling: rounded-2xl border bg-card p-8

    Card 2 — Advanced ($299/mo):
    - Badge: "Advanced" with a "Most Popular" or "Best Value" pill on top (use text-accent, bg-accent/10)
    - Price: $299 /month
    - Tagline: "For growing home service companies"
    - Feature list:
      - 4–10 page professional website
      - Mobile-optimized design
      - 4 revision requests per month
      - Client portal access
      - Ongoing hosting and maintenance
      - Domain setup included
      - Priority support
    - CTA button: "Book a Call" → Calendly URL
    - Card styling: rounded-2xl border-2 border-accent bg-card p-8 (accent border to highlight recommended)

    Card 3 — Review Add-On ($99/mo):
    - Badge: "Add-On"
    - Price: $99 /month (with "add to any plan" sub-text)
    - Tagline: "Add Google review automation to your plan"
    - Feature list:
      - Automated multi-touch review campaigns
      - AI-personalized messages (email + SMS)
      - Smart review funnel (4-5 stars → Google, 1-3 stars → private)
      - AI-written responses posted to Google
      - Monthly performance reports
      - Competitor tracking
    - CTA: "Learn More" → /reputation (links to the reputation page). Secondary: "Book a Call" → Calendly
    - Card styling: rounded-2xl border bg-card p-8 (matches Basic, this is an add-on not standalone)

    All pricing amounts must be exact: Basic = $199, Advanced = $299, Review add-on = $99. Do NOT use different numbers.

    All cards must use Check icon from @phosphor-icons/react for the feature list checkmarks. Use FadeIn wrappers for animation.

    --- faq-webdesign.tsx ---
    FAQ accordion for web design objections. Use the same pattern as components/marketing/faq-section.tsx (useState accordion with CaretDown icon). Create 6-8 Q&A pairs focused on web design concerns:

    1. Q: "Do I need to manage the website myself?" — A: No, we handle everything. You submit revision requests via your client portal and we implement them. You never touch hosting, code, or settings.
    2. Q: "What if I need to make changes?" — A: You get a dedicated client portal at a permanent URL. Submit your revision request (title, description, optional screenshot), and we complete it within 48 hours. Basic plan includes 2 revisions/month, Advanced includes 4.
    3. Q: "How is this different from paying a web designer once?" — A: One-time web design typically costs $3,000–$15,000 upfront. Our subscription model spreads the cost into manageable monthly payments and includes ongoing maintenance — so your site stays current without any additional invoices.
    4. Q: "What if I need more changes than my plan allows?" — A: Additional revision requests beyond your monthly limit are available at $50 each, confirmed before submission. You can also upgrade to Advanced any time for 4 revisions/month.
    5. Q: "Do I own my website?" — A: Yes. Your domain, your content. We handle the technical side, but you own the assets. If you ever cancel, we provide an export of your site files.
    6. Q: "What types of home service businesses do you work with?" — A: HVAC, plumbing, electrical, roofing, painting, cleaning, handyman, and general contracting. Our templates and copy frameworks are built specifically for home service businesses — not generic small business.
    7. Q: "What is the Review Add-On?" — A: An optional $99/month add-on that plugs in AvisLoop's automated review management service. You get multi-touch email and SMS campaigns after each job, a smart review funnel (routes 4–5 star ratings to Google, keeps 1–3 star ratings private), and AI-generated responses posted to your Google profile.
    8. Q: "Can I cancel anytime?" — A: Yes. No contracts, no cancellation fees. We keep it simple because we'd rather earn your business every month with results than lock you into anything.

    Component must be a client component ('use client') with useState for accordion expand/collapse, matching the pattern in components/marketing/faq-section.tsx exactly.
  </action>
  <verify>
    Check files exist:
    - components/marketing/v3/pricing-webdesign.tsx
    - components/marketing/v3/faq-webdesign.tsx

    Grep pricing-webdesign.tsx for "$199", "$299", "$99" — all three must appear.
    Grep pricing-webdesign.tsx for "id=\"pricing\"" — must exist on section element.
    Grep faq-webdesign.tsx for "use client" — must be present.
    Grep both files for lucide-react — must not appear.
  </verify>
  <done>pricing-webdesign.tsx contains three cards with exact prices $199, $299, $99 and a section id="pricing". faq-webdesign.tsx is a client component with 6+ Q&A pairs addressing web design objections. Neither file imports from lucide-react.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. ls components/marketing/v3/ — should show: hero-webdesign.tsx, services-webdesign.tsx, process-section.tsx, pricing-webdesign.tsx, faq-webdesign.tsx
2. ls app/(marketing)/reputation/ — should show: page.tsx
3. grep -r "lucide-react" components/marketing/v3/ — should return nothing
4. grep "\$199\|\$299\|\$99" components/marketing/v3/pricing-webdesign.tsx — all three must appear
5. grep 'id="pricing"' components/marketing/v3/pricing-webdesign.tsx — must appear
6. grep "HeroV2" app/(marketing)/reputation/page.tsx — must appear
</verification>

<success_criteria>
- app/(marketing)/reputation/page.tsx exists and imports v2 components (HeroV2, SocialProofStrip, etc.) without modification
- Five v3 components exist in components/marketing/v3/
- Pricing component has exactly three cards: Basic $199, Advanced $299, Review add-on $99 with correct tier details per MKT-03
- No lucide-react imports in any v3 component
- All three Calendly CTAs use https://calendly.com/anthony-le-avisloop/avisloop-demo-onboarding-call
</success_criteria>

<output>
After completion, create .planning/phases/75-marketing-landing-page/75-01-SUMMARY.md
</output>
