---
phase: 15-design-system-dashboard-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - tailwind.config.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "Primary color is #1B44BF across the app"
    - "Kumbh Sans is the only font family used"
    - "Phosphor icons package is installed and importable"
    - "No box-shadows on cards or containers (border-only design)"
    - "Semantic status colors are defined as CSS variables"
  artifacts:
    - path: "app/globals.css"
      provides: "Updated CSS custom properties with new color tokens, semantic status palette, border-only design"
      contains: "--primary: 224 75% 43%"
    - path: "app/layout.tsx"
      provides: "Kumbh Sans font loaded via next/font/google"
      contains: "Kumbh_Sans"
    - path: "tailwind.config.ts"
      provides: "Semantic status color mappings in theme extend"
      contains: "status"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "font class applied to body"
      pattern: "kumbhSans"
---

<objective>
Update the design system foundation: CSS custom properties (primary #1B44BF, semantic status palette, border-only design), swap font from Geist to Kumbh Sans, install Phosphor icons, and update Tailwind config with semantic status colors.

Purpose: All subsequent plans depend on these tokens, font, and icon library being in place.
Output: Updated globals.css, layout.tsx, tailwind.config.ts, and Phosphor icons installed.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-design-system-dashboard-redesign/15-CONTEXT.md
@app/globals.css
@app/layout.tsx
@tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phosphor icons and update font</name>
  <files>package.json, app/layout.tsx</files>
  <action>
1. Run `pnpm add @phosphor-icons/react` to install Phosphor icons.
2. In `app/layout.tsx`:
   - Replace `import { Geist } from "next/font/google"` with `import { Kumbh_Sans } from "next/font/google"`
   - Configure: `const kumbhSans = Kumbh_Sans({ subsets: ["latin"], weight: ["400", "500", "600"], variable: "--font-kumbh-sans", display: "swap" })`
   - Replace `geistSans.className` with `kumbhSans.className` in the body tag
   - Remove the old Geist font import and variable entirely
  </action>
  <verify>
    Run `pnpm typecheck` -- no errors about font imports.
    Run `node -e "require('@phosphor-icons/react')"` to confirm package installed.
  </verify>
  <done>Kumbh Sans loads as primary font, Phosphor icons importable</done>
</task>

<task type="auto">
  <name>Task 2: Update CSS tokens and Tailwind config</name>
  <files>app/globals.css, tailwind.config.ts</files>
  <action>
1. In `app/globals.css`, update `:root` CSS custom properties:
   - `--primary`: change from `217 91% 60%` to `224 75% 43%` (this is #1B44BF in HSL)
   - `--primary-foreground`: keep `0 0% 98%` (white text on blue)
   - `--background`: change to `0 0% 97.6%` (#F9F9F9 for main content area)
   - `--card`: keep `0 0% 100%` (white cards)
   - `--border`: change to `0 0% 89%` (#E3E3E3 card borders)
   - `--ring`: update to match new primary `224 75% 43%`
   - `--accent`: update to match new primary `224 75% 43%`
   - `--radius`: change from `0.75rem` to `0.5rem` (8px border-radius)
   - Add semantic status variables:
     ```
     --status-success: 142 71% 45%;
     --status-warning: 38 92% 50%;
     --status-error: 0 84% 60%;
     --status-info: 217 91% 60%;
     --status-reviewed: 270 50% 55%;
     ```
   - Remove `--accent-lime` and `--accent-coral` (Phase 10 landing page tokens, not needed for dashboard -- only remove if they are not used elsewhere; if they ARE used in marketing pages, keep them)

2. Update `.dark` theme to match:
   - `--primary`: `224 75% 55%` (slightly brighter for dark mode)
   - `--ring`: match new primary
   - `--accent`: match new primary
   - Add the same semantic status variables with slightly adjusted brightness for dark mode:
     ```
     --status-success: 142 71% 55%;
     --status-warning: 38 92% 55%;
     --status-error: 0 84% 65%;
     --status-info: 217 91% 65%;
     --status-reviewed: 270 50% 65%;
     ```

3. In `tailwind.config.ts`, add under `theme.extend.colors`:
   ```typescript
   status: {
     success: "hsl(var(--status-success))",
     warning: "hsl(var(--status-warning))",
     error: "hsl(var(--status-error))",
     info: "hsl(var(--status-info))",
     reviewed: "hsl(var(--status-reviewed))",
   },
   ```
   Keep existing `lime` and `coral` colors if marketing pages reference them.

4. In `components/ui/card.tsx`, remove `shadow` from the Card base class and InteractiveCard. Replace with border-only. Specifically:
   - Card: change `"rounded-xl border bg-card text-card-foreground shadow"` to `"rounded-lg border bg-card text-card-foreground"`
   - InteractiveCard: remove `shadow` and `motion-safe:hover:shadow-lg`, keep `motion-safe:hover:-translate-y-1`

5. In `components/ui/button.tsx`, update the base cva string:
   - Remove `motion-safe:hover:shadow-md` and `motion-safe:active:shadow-sm` from the base string (border-only design, no shadows)
   - Keep the hover translate and active scale effects
  </action>
  <verify>
    Run `pnpm typecheck` and `pnpm lint` -- both pass.
    Grep for `shadow` in card.tsx and button.tsx to confirm removal.
  </verify>
  <done>
    CSS tokens use #1B44BF primary, 8px radius, no shadows, semantic status palette defined.
    Tailwind config exposes status colors. Card and Button components updated.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint` passes
- `@phosphor-icons/react` in package.json dependencies
- `Kumbh_Sans` referenced in app/layout.tsx
- `--primary: 224 75% 43%` in globals.css
- `--status-success` through `--status-reviewed` in globals.css
- `status` key in tailwind.config.ts colors
- No `shadow` class in card.tsx Card component
</verification>

<success_criteria>
- Primary color is #1B44BF (HSL 224 75% 43%)
- Kumbh Sans is the app font (Regular 400, Medium 500, Semibold 600)
- Phosphor icons installed and importable
- Semantic status colors available via Tailwind (bg-status-success, text-status-error, etc.)
- Cards have borders but no shadows
- Buttons have no shadow on hover
- Both light and dark mode tokens updated
</success_criteria>

<output>
After completion, create `.planning/phases/15-design-system-dashboard-redesign/15-01-SUMMARY.md`
</output>
