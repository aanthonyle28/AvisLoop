---
phase: 33-hardcoded-color-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/33-hardcoded-color-audit/TIER2-COLOR-AUDIT.md
autonomous: true

must_haves:
  truths:
    - "Every inline Tailwind color-scale class in components/ is catalogued with file path, line count, and category"
    - "Categories distinguish status colors, form validation, chart/data visualization, and marketing-only from structural layout colors"
    - "Phase 35 has a clear input list of which files and patterns need new semantic tokens"
  artifacts:
    - path: ".planning/phases/33-hardcoded-color-audit/TIER2-COLOR-AUDIT.md"
      provides: "Complete inventory of inline color classes for Phase 35 cleanup"
      contains: "Category"
  key_links:
    - from: ".planning/phases/33-hardcoded-color-audit/TIER2-COLOR-AUDIT.md"
      to: ".planning/ROADMAP.md Phase 35"
      via: "Phase 35 success criteria reference"
      pattern: "Phase 35"
---

<objective>
Create a comprehensive Tier 2 color audit document cataloguing every inline Tailwind color-scale class (bg-amber-50, text-red-600, border-green-200, etc.) across all component files. Categorize each occurrence so Phase 35 can create semantic tokens for the right patterns.

Purpose: The success criteria for Phase 33 states inline semantic colors must be "replaced with token-based equivalents or documented for Phase 35 cleanup." Since no --warning, --success, --info tokens exist yet, creating tokens is Phase 35's job. This plan provides the input for that work.

Output: A TIER2-COLOR-AUDIT.md file with per-file inventory, categorization, and Phase 35 recommendations.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-hardcoded-color-audit/33-RESEARCH.md
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all inline color-scale classes in components/</name>
  <files>
    .planning/phases/33-hardcoded-color-audit/TIER2-COLOR-AUDIT.md
  </files>
  <action>
    1. Run grep to find ALL inline Tailwind color-scale classes in components/ (excluding already-tokenized classes like bg-primary, bg-destructive, bg-muted, etc.):
       ```
       grep -rn "bg-\(red\|green\|blue\|amber\|yellow\|orange\|emerald\|lime\|cyan\|indigo\|violet\|purple\|pink\|rose\|teal\|sky\|slate\|gray\|zinc\|neutral\|stone\)-\|text-\(red\|green\|blue\|amber\|yellow\|orange\|emerald\|lime\|cyan\|indigo\|violet\|purple\|pink\|rose\|teal\|sky\|slate\|gray\|zinc\|neutral\|stone\)-\|border-\(red\|green\|blue\|amber\|yellow\|orange\|emerald\|lime\|cyan\|indigo\|violet\|purple\|pink\|rose\|teal\|sky\|slate\|gray\|zinc\|neutral\|stone\)-" components/
       ```

    2. For each file, categorize occurrences into one of these categories:
       - **form-validation**: Error text (text-red-600 for form errors) — candidate for `text-destructive`
       - **status-badge**: Status indicators (green for success, amber for warning, red for error) — needs new `--success`, `--warning` tokens
       - **data-viz**: Chart dots, progress bars, rating stars — needs specialized tokens or stays inline
       - **notification**: Bell badges, alert icons — candidate for `bg-destructive`
       - **marketing**: Landing page, hero, animated demo — lower priority, may stay inline
       - **billing**: Usage warnings, subscription status — needs `--warning` token
       - **onboarding**: Step indicators, progress — needs review with Phase 38 onboarding changes

    3. Create TIER2-COLOR-AUDIT.md with:
       - Summary table: category -> file count -> occurrence count
       - Per-category file list with specific patterns found
       - Token recommendations for Phase 35 (which new tokens to create)
       - Files that can use existing tokens (text-destructive instead of text-red-600)
       - Files that should stay inline (marketing, data-viz edge cases)

    **Important:** Do NOT modify any component files. This is documentation only.

    **Scope note:** Include components/ only. Exclude app/(marketing)/ page files and any non-component files.
  </action>
  <verify>
    - TIER2-COLOR-AUDIT.md exists at `.planning/phases/33-hardcoded-color-audit/TIER2-COLOR-AUDIT.md`
    - Document contains at least 40 files (research found 43 files with color-scale classes)
    - Document has category summary table
    - Document has Phase 35 token recommendations section
  </verify>
  <done>
    Complete Tier 2 color audit document exists with per-file inventory categorized by usage pattern, plus Phase 35 token creation recommendations.
  </done>
</task>

</tasks>

<verification>
1. TIER2-COLOR-AUDIT.md exists and is non-empty
2. All 43+ files with inline color classes are catalogued
3. Each file is assigned a category
4. Phase 35 token recommendations are actionable (specific token names and values)
5. Quick-win replacements identified (text-red-600 -> text-destructive where semantically correct)
</verification>

<success_criteria>
- Comprehensive audit document covers all inline Tailwind color-scale usage in components/
- Categories distinguish different use cases (form errors vs. status badges vs. charts)
- Phase 35 has clear input: which tokens to create, which files to update, which to leave
- No component files modified by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/33-hardcoded-color-audit/33-02-SUMMARY.md`
</output>
