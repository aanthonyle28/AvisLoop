---
phase: 08-public-pages
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/(marketing)/pricing/page.tsx
  - components/marketing/pricing-table.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor can see pricing tiers at /pricing"
    - "Pricing page shows Free Trial, Basic, and Pro tiers"
    - "Each tier shows price, features, and CTA button"
    - "Recommended tier (Basic) is visually highlighted"
    - "All CTA buttons link to sign up"
  artifacts:
    - path: "app/(marketing)/pricing/page.tsx"
      provides: "Pricing page with metadata"
      min_lines: 30
    - path: "components/marketing/pricing-table.tsx"
      provides: "Pricing tiers comparison component"
      min_lines: 60
  key_links:
    - from: "app/(marketing)/pricing/page.tsx"
      to: "components/marketing/pricing-table.tsx"
      via: "import and render"
      pattern: "import.*PricingTable"
    - from: "components/marketing/pricing-table.tsx"
      to: "/auth/sign-up"
      via: "CTA button links"
      pattern: "href.*auth/sign-up"
---

<objective>
Create the pricing page showing tier comparison with Free Trial, Basic, and Pro plans.

Purpose: Visitors need to understand pricing options before signing up. A clear pricing page reduces friction in the conversion funnel and sets expectations about what each tier includes.

Output: Pricing page at /pricing with 3-tier comparison table showing features, prices, and CTAs.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-public-pages/08-RESEARCH.md
@.planning/phases/08-public-pages/08-01-SUMMARY.md

# Existing files for reference
@lib/constants/billing.ts
@app/(marketing)/layout.tsx
@components/ui/card.tsx
@components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pricing table component</name>
  <files>
    components/marketing/pricing-table.tsx
  </files>
  <action>
Create the pricing table component that displays tier comparison:

1. Create `components/marketing/pricing-table.tsx`:
   - Define tier data array matching existing TIER_LIMITS (but with marketing-friendly copy):
     ```typescript
     const tiers = [
       {
         name: 'Free Trial',
         price: '$0',
         period: '25 sends',
         description: 'Try before you commit',
         features: [
           '25 free review requests',
           'All features included',
           'Contact management',
           'Email delivery tracking',
           'No credit card required',
         ],
         cta: 'Start Free Trial',
         href: '/auth/sign-up',
       },
       {
         name: 'Basic',
         price: '$49',
         period: '/month',
         description: 'For small businesses',
         features: [
           '200 sends per month',
           '200 contacts',
           'Custom email templates',
           'Send history & analytics',
           'Email support',
         ],
         cta: 'Get Started',
         href: '/auth/sign-up',
         recommended: true,
       },
       {
         name: 'Pro',
         price: '$99',
         period: '/month',
         description: 'For growing businesses',
         features: [
           '500 sends per month',
           'Unlimited contacts',
           'Everything in Basic',
           'Priority support',
           'Advanced analytics',
         ],
         cta: 'Upgrade to Pro',
         href: '/auth/sign-up',
       },
     ]
     ```

   - Render as 3-column grid (stacks on mobile: grid-cols-1 md:grid-cols-3)
   - Use shadcn Card component with CardHeader, CardTitle, CardDescription, CardContent, CardFooter
   - Highlight recommended tier with border-primary and shadow-lg classes
   - Show "RECOMMENDED" badge on recommended tier
   - Feature list with Check icon from lucide-react
   - CTA button: default variant for recommended, outline for others
   - Use Button with asChild and Link for navigation

   - Server Component (no "use client")

Note: Keep tier data in the component file (not imported from billing.ts) because marketing copy differs from technical limits.
  </action>
  <verify>
Run `pnpm typecheck` - no errors
  </verify>
  <done>
PricingTable component exists with 3 tiers, recommended highlighting, and CTA buttons
  </done>
</task>

<task type="auto">
  <name>Task 2: Create pricing page</name>
  <files>
    app/(marketing)/pricing/page.tsx
  </files>
  <action>
Create the pricing page at /pricing:

1. Create `app/(marketing)/pricing/page.tsx`:
   - Add static metadata export for SEO:
     ```typescript
     export const metadata: Metadata = {
       title: 'Pricing - AvisLoop',
       description: 'Simple, transparent pricing. Start with 25 free sends, upgrade as you grow. No hidden fees.',
       openGraph: {
         title: 'Pricing - AvisLoop',
         description: 'Simple, transparent pricing. Start with 25 free sends, upgrade as you grow.',
       },
     }
     ```

   - Page header section:
     - Headline: "Simple, Transparent Pricing"
     - Subheadline: "Start free, upgrade as you grow. No hidden fees."
     - Centered text

   - Import and render PricingTable component

   - Optional FAQ section (if time permits):
     - "Can I switch plans?" - Yes, upgrade or downgrade anytime
     - "What happens when my trial ends?" - Your account remains, you just can't send more requests until you subscribe
     - Keep simple - just text, no accordion

   - Server Component

2. Page structure:
   ```
   <div className="container mx-auto px-4 py-16">
     <header> ... </header>
     <PricingTable />
     <FAQ (optional)> ... </FAQ>
   </div>
   ```
  </action>
  <verify>
1. Run `pnpm typecheck` - no errors
2. Run `pnpm lint` - no errors
3. Run `pnpm dev` and visit http://localhost:3000/pricing - pricing page renders
  </verify>
  <done>
Pricing page at /pricing shows:
- Header with "Simple, Transparent Pricing"
- 3 pricing tiers in comparison layout
- Recommended tier visually highlighted
- All CTAs link to /auth/sign-up
- Metadata configured for SEO
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. **Pricing page renders at /pricing**
   - Run `pnpm dev`
   - Visit http://localhost:3000/pricing
   - See "Simple, Transparent Pricing" header
   - See 3 pricing cards (Free Trial, Basic, Pro)

2. **Marketing layout applied**
   - Navbar shows (inherited from marketing layout)
   - Footer shows (inherited from marketing layout)

3. **Pricing content is correct**
   - Free Trial: $0, 25 sends, "Start Free Trial" CTA
   - Basic: $49/month, 200 sends, "Get Started" CTA, RECOMMENDED badge
   - Pro: $99/month, 500 sends, "Upgrade to Pro" CTA

4. **CTAs work**
   - All tier CTAs link to /auth/sign-up

5. **Navigation works**
   - Click "Pricing" in navbar from landing page - navigates to /pricing
   - Click "AvisLoop" logo - navigates back to /

6. **Build passes**
   - Run `pnpm build` - no errors
</verification>

<success_criteria>
- Pricing page at /pricing shows 3-tier comparison
- Tiers match product offering (trial: 25 free, basic: $49/200, pro: $99/500)
- Recommended tier (Basic) is visually highlighted
- All CTAs link to sign up page
- Page uses marketing layout (navbar, footer)
- Metadata configured for SEO
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-public-pages/08-02-SUMMARY.md`
</output>
