---
phase: 65-settings-billing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [docs/qa-v3.1/65-settings-services-customers.md]
autonomous: true

must_haves:
  truths:
    - "Services tab displays 8 service type cards (HVAC, Plumbing, Electrical, Cleaning, Roofing, Painting, Handyman, Other) each with a toggle and timing input"
    - "Toggling a service type on highlights the card and shows the timing input; toggling off removes the highlight and hides the timing input"
    - "Saving service type changes persists them — verified by page refresh showing the same enabled services and DB query confirming service_types_enabled array"
    - "When 'Other' service type is enabled, the custom service names section appears with the ability to add and remove tag pills"
    - "Review cooldown input (7-90 days) is editable and its own Save button persists the change — verified by DB query confirming review_cooldown_days"
    - "Customers tab displays customer list with working search (typing a name filters rows) and tag filter"
    - "Adding a new customer via the Customers tab creates a customer record — verified by DB query"
    - "Editing a customer (changing notes or name) saves correctly — verified by DB query"
    - "Archiving a customer changes their status to 'archived' — verified by DB query"
    - "All Services tab changes persist after full page reload (SETT-09 partial)"
    - "Findings document docs/qa-v3.1/65-settings-services-customers.md exists with PASS/FAIL per requirement for SETT-05, SETT-06, SETT-07, SETT-08, and partial SETT-09"
  artifacts:
    - path: "docs/qa-v3.1/65-settings-services-customers.md"
      provides: "Settings Services + Customers tab QA findings with pass/fail, screenshots, DB verification"
      min_lines: 100
  key_links: []
---

<objective>
Execute the Phase 65 QA audit of the Settings page Services tab (SETT-05, SETT-06) and Customers tab (SETT-07, SETT-08), plus persistence verification for Services tab (partial SETT-09).

Purpose: The Services tab controls which service types are enabled and their campaign timing, directly affecting job creation and campaign enrollment. The Customers tab provides full CRUD for customer management (de-emphasized in V2 but still functional). This audit verifies toggle/save flows, custom service names, and customer CRUD operations.

Output: `docs/qa-v3.1/65-settings-services-customers.md` — findings document with PASS/FAIL per requirement, DB verification evidence, and screenshot references.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/PROJECT_STATE.md
@docs/DATA_MODEL.md

Test account: audit-test@avisloop.com / AuditTest123!
App URL: http://localhost:3000
Output file: docs/qa-v3.1/65-settings-services-customers.md
Business name: "Audit Test HVAC" (id: 6ed94b54-6f35-4ede-8dcb-28f562052042)

**Services tab structure (ServiceTypesSection component):**
- 8 service type cards in a 2-column grid: hvac, plumbing, electrical, cleaning, roofing, painting, handyman, other
- Each card: service name label + toggle button (custom checkbox-like UI, not native checkbox)
- When toggled ON: card gets `border-primary bg-primary/5`, shows timing input (number, min=1, max=168, hours)
- When "Other" is enabled: "Custom service names" section appears with text input + Add button + TagBadge pills (max 10, removable)
- "Save Changes" button appears only when `hasChanges=true`
- "Reset" button to revert unsaved changes
- Review cooldown section: number input (min=7, max=90 days) with separate Save button

**Customers tab structure (CustomersClient component):**
- Reuses the full CustomersClient component from the standalone customers page
- Customer table with search, tag filters
- "Add Customer" button (sheet-based form)
- Row click opens customer detail drawer with edit capability
- Bulk actions on selection

**Important context:**
- The business currently has `service_types_enabled = []` (empty array) — from Phase 62 research
- Existing customers from prior phases: Test Technician, Bob Wilson, Jane Doe, John Smith, AUDIT_Patricia Johnson, AUDIT_Marcus Rodriguez, AUDIT_Sarah Chen
- When testing customer operations, prefix all test data with AUDIT_ for cleanup

**Toggle button interaction:** The service type toggle is a `<button>` element that triggers `toggleService(type)`. Clicking the button toggles the service on/off. The checkmark icon appears/disappears. This is NOT a native checkbox — use `browser_click` on the button element, not a checkbox interaction.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Services tab audit — toggles, timing, custom service names, review cooldown, persistence</name>
  <files>docs/qa-v3.1/65-settings-services-customers.md</files>
  <action>
This task tests SETT-05 (service type toggles), SETT-06 (custom service names), and SETT-09 (persistence) for the Services tab.

**IMPORTANT:** The dev server must be running. If not logged in, re-login with audit-test@avisloop.com / AuditTest123!

**Step-by-step Playwright MCP walkthrough:**

**Part A: Prerequisite — verify current service type state**

1. Run DB query to confirm current service type settings:
    ```sql
    SELECT service_types_enabled, service_type_timing, review_cooldown_days, custom_service_names
    FROM businesses
    WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
    ```
    Record: current enabled list (likely empty []), timing object, cooldown days, custom names.

**Part B: Navigate to Services tab**

2. `browser_navigate` to `http://localhost:3000/settings`
3. `browser_wait_for` — Wait for Settings heading
4. `browser_snapshot` — Find the "Services" tab trigger
5. `browser_click` — Click the "Services" tab
6. `browser_wait_for` — Wait for Services tab content to load
7. `browser_snapshot` — Record the full Services tab structure:
    - 8 service type cards visible
    - Each card has: label (e.g., "HVAC", "Plumbing") + toggle button
    - Which services are currently toggled ON (have checkmark + highlighted border)
    - Review cooldown section at the bottom
8. `browser_take_screenshot` — filename: `qa-65-settings-services-initial.png`

**Part C: SETT-05 — Service type toggles**

9. **Count all 8 service type cards:** Verify these exact 8 labels appear:
    - HVAC, Plumbing, Electrical, Cleaning, Roofing, Painting, Handyman, Other
    Record which are currently enabled (if any).

10. **Toggle HVAC on:**
    - `browser_snapshot` — Find the HVAC card's toggle button
    - `browser_click` — Click the HVAC toggle button
    - `browser_snapshot` — Verify:
      - HVAC card now has highlighted border (border-primary)
      - Timing input appeared showing hours (default 24)
      - Checkmark icon visible
    - `browser_take_screenshot` — filename: `qa-65-settings-services-hvac-on.png`

11. **Toggle Plumbing on:**
    - `browser_click` — Click the Plumbing toggle button
    - `browser_snapshot` — Verify:
      - Plumbing card highlighted with timing input (default 48)
      - "Save Changes" button now visible (hasChanges=true)

12. **Change timing for HVAC:**
    - Find the HVAC timing input
    - Clear it and type "12" (change from default 24 to 12)
    - `browser_snapshot` — Verify the value changed to 12

13. **Save service type changes:**
    - `browser_click` — Click "Save Changes" button
    - `browser_wait_for` — Wait for success toast ("Service type settings saved")
    - `browser_snapshot` — Verify:
      - Success feedback received
      - "Save Changes" button is no longer visible (hasChanges back to false)
    - `browser_take_screenshot` — filename: `qa-65-settings-services-saved.png`

14. **DB verification — service types saved:**
    ```sql
    SELECT service_types_enabled, service_type_timing
    FROM businesses
    WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
    ```
    Verify:
    - service_types_enabled contains 'hvac' and 'plumbing'
    - service_type_timing has hvac: 12 (changed from default)
    - service_type_timing has plumbing: 48 (default)

15. **Toggle HVAC off (test de-toggle):**
    - `browser_click` — Click the HVAC toggle button (should toggle OFF)
    - `browser_snapshot` — Verify:
      - HVAC card loses highlighted border
      - Timing input disappears
      - "Save Changes" button reappears
    - Save the change (click Save Changes)
    - `browser_wait_for` — Wait for success toast

16. **SETT-09 partial — Persistence after refresh:**
    - `browser_navigate` to `http://localhost:3000/settings` (full page reload)
    - `browser_wait_for` — Wait for Settings
    - Click the "Services" tab
    - `browser_snapshot` — Verify:
      - HVAC is OFF (not highlighted)
      - Plumbing is ON (highlighted, timing shows 48)
    - `browser_take_screenshot` — filename: `qa-65-settings-services-persisted.png`

17. Record SETT-05: PASS if all 8 service types displayed, toggles work correctly (on/off), timing editable, and saves persist (DB verified). FAIL if any service type missing, toggle doesn't work, or save fails.

**Part D: SETT-06 — Custom service names**

18. **Enable "Other" service type:**
    - `browser_snapshot` — Find the "Other" card toggle
    - `browser_click` — Click the "Other" toggle button
    - `browser_snapshot` — Verify:
      - "Other" card is highlighted
      - Timing input appears for "Other"
      - "Custom service names" section appears below the grid with:
        - Text input with placeholder (e.g., "e.g. Pest Control, Pool Cleaning...")
        - "Add" button
        - Heading "Custom service names"
    - `browser_take_screenshot` — filename: `qa-65-settings-services-other-enabled.png`

19. **Add a custom service name:**
    - `browser_snapshot` — Get ref for the custom service name input (aria-label="Custom service name")
    - Type "AUDIT Pest Control" into the input
    - `browser_click` — Click the "Add" button
    - `browser_snapshot` — Verify:
      - "AUDIT Pest Control" appears as a TagBadge pill
      - Input is cleared
      - The pill has a remove (X) button

20. **Add a second custom service name:**
    - Type "AUDIT Pool Cleaning" into the input
    - `browser_click` — Click "Add" (or press Enter)
    - `browser_snapshot` — Verify two pills: "AUDIT Pest Control" and "AUDIT Pool Cleaning"

21. **Remove one custom service name:**
    - `browser_snapshot` — Find the remove button (X) on "AUDIT Pool Cleaning" pill
    - `browser_click` — Click the remove button
    - `browser_snapshot` — Verify only "AUDIT Pest Control" remains
    - `browser_take_screenshot` — filename: `qa-65-settings-services-custom-names.png`

22. **Save with custom service names:**
    - `browser_click` — Click "Save Changes"
    - `browser_wait_for` — Wait for success toast

23. **DB verification — custom service names:**
    ```sql
    SELECT custom_service_names, service_types_enabled
    FROM businesses
    WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
    ```
    Verify: custom_service_names contains 'AUDIT Pest Control', service_types_enabled contains 'other'.

24. Record SETT-06: PASS if custom service names section appears when Other is enabled, add/remove works, and saves persist. FAIL if section doesn't appear or add/remove broken.

**Part E: Review cooldown**

25. `browser_snapshot` — Find the "Review cooldown" section with its number input
    - Record current cooldown value (likely 30)

26. **Change cooldown:**
    - Clear the input and type "14"
    - `browser_snapshot` — Verify the cooldown-specific "Save" button appears (inline, next to the input)
    - `browser_click` — Click the cooldown Save button
    - `browser_wait_for` — Wait for success toast ("Review cooldown updated")

27. **DB verification — cooldown saved:**
    ```sql
    SELECT review_cooldown_days
    FROM businesses
    WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
    ```
    Verify: review_cooldown_days = 14.

28. **Restore cooldown to original value (30):**
    - Clear input, type "30"
    - Click Save
    - Verify via DB

**Part F: Cleanup — restore original service type state**

29. The business originally had `service_types_enabled = []`. Restore it:
    - Toggle OFF Plumbing
    - Toggle OFF Other
    - Click "Save Changes"
    - `browser_wait_for` — Wait for success toast

30. **DB verification — restored:**
    ```sql
    SELECT service_types_enabled, custom_service_names, review_cooldown_days
    FROM businesses
    WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
    ```
    Verify: service_types_enabled is empty or contains only what was originally there.

**Part G: Initialize findings document**

31. Create `docs/qa-v3.1/65-settings-services-customers.md` with findings:

```markdown
# Phase 65: Settings & Billing — QA Findings (Part 2: Services + Customers)

**Tested:** [current date]
**Tester:** Claude (Playwright MCP + Supabase MCP)
**App URL:** http://localhost:3000
**Test account:** audit-test@avisloop.com
**Business:** Audit Test HVAC (6ed94b54-6f35-4ede-8dcb-28f562052042)

---

## Summary

| Requirement | Status | Notes |
|-------------|--------|-------|
| SETT-05: Service type toggles | [PASS/FAIL] | [notes] |
| SETT-06: Custom service names | [PASS/FAIL] | [notes] |
| SETT-07: Customers tab search/filters | [pending] | |
| SETT-08: Customer add/edit/archive | [pending] | |
| SETT-09: Persistence (Services) | [PASS/FAIL] | [notes] |

---

## SETT-05: Service type toggles

**Status:** [PASS/FAIL]

### Service types displayed
| # | Service Type | Label | Present? |
|---|-------------|-------|----------|
| 1 | hvac | HVAC | [YES/NO] |
| 2 | plumbing | Plumbing | [YES/NO] |
| 3 | electrical | Electrical | [YES/NO] |
| 4 | cleaning | Cleaning | [YES/NO] |
| 5 | roofing | Roofing | [YES/NO] |
| 6 | painting | Painting | [YES/NO] |
| 7 | handyman | Handyman | [YES/NO] |
| 8 | other | Other | [YES/NO] |

### Toggle test
| Action | Result |
|--------|--------|
| Toggle HVAC on | [card highlighted, timing input appeared] |
| Toggle Plumbing on | [card highlighted, timing input appeared] |
| Change HVAC timing to 12h | [input accepted new value] |
| Save Changes | [success toast, button hidden] |
| Toggle HVAC off | [card de-highlighted, timing hidden] |

### DB verification
```
[paste SQL results showing service_types_enabled and service_type_timing]
```

### Persistence (SETT-09)
- Page refreshed: [YES]
- HVAC state after refresh: [OFF as expected]
- Plumbing state after refresh: [ON with 48h timing]
- Result: [PASS/FAIL]

### Evidence
- `qa-65-settings-services-initial.png`
- `qa-65-settings-services-hvac-on.png`
- `qa-65-settings-services-saved.png`
- `qa-65-settings-services-persisted.png`

### Bugs found
- [none or list]

---

## SETT-06: Custom service names

**Status:** [PASS/FAIL]

### Custom names section
- Appeared when "Other" toggled on: [YES/NO]
- Input field with placeholder: [YES/NO]
- "Add" button: [YES/NO]

### Add/remove test
| Action | Result |
|--------|--------|
| Add "AUDIT Pest Control" | [pill appeared, input cleared] |
| Add "AUDIT Pool Cleaning" | [second pill appeared] |
| Remove "AUDIT Pool Cleaning" | [pill removed, only first remains] |
| Save | [success toast] |

### DB verification
```
[paste SQL result showing custom_service_names array]
```

### Evidence
- `qa-65-settings-services-other-enabled.png`
- `qa-65-settings-services-custom-names.png`

### Bugs found
- [none or list]

---

## Review Cooldown

### Test
- Changed from [original] to 14 days
- Save button appeared inline: [YES/NO]
- Success toast: [text]
- DB verified: review_cooldown_days = 14

### Restored to original: [value]

---
```

Fill in ALL [placeholder] values.
  </action>
  <verify>
1. `docs/qa-v3.1/65-settings-services-customers.md` exists with SETT-05, SETT-06, SETT-09 (Services) sections filled
2. Screenshots captured: services-initial, hvac-on, services-saved, services-persisted, other-enabled, custom-names
3. DB verifications confirm toggle/save/persistence for service types
4. DB verifications confirm custom service names were saved
5. DB verifications confirm cooldown was changed and restored
6. Original service type state restored (service_types_enabled back to original, cooldown to original)
  </verify>
  <done>
- SETT-05 marked PASS or FAIL with all 8 service types listed, toggle on/off tested, timing edit tested, DB verified
- SETT-06 marked PASS or FAIL with custom names add/remove tested, DB verified
- SETT-09 partial marked PASS or FAIL for Services tab persistence
- Review cooldown tested, changed, and restored
- Original service type state restored (no persistent test mutations)
  </done>
</task>

<task type="auto">
  <name>Task 2: Customers tab audit — search, filters, add/edit/archive customer</name>
  <files>docs/qa-v3.1/65-settings-services-customers.md</files>
  <action>
This task tests SETT-07 (customer list with search/filters) and SETT-08 (add/edit/archive customer). Must run AFTER Task 1 because it depends on the findings document existing.

**IMPORTANT:** If still logged in, proceed directly. If session expired, re-login with audit-test@avisloop.com / AuditTest123!

**Step-by-step Playwright MCP walkthrough:**

**Part A: SETT-07 — Customer list, search, and filters**

1. `browser_navigate` to `http://localhost:3000/settings`
2. `browser_wait_for` — Wait for Settings heading
3. `browser_snapshot` — Find the "Customers" tab trigger
4. `browser_click` — Click the "Customers" tab
5. `browser_wait_for` — Wait for customers content to load (table or empty state)
6. `browser_snapshot` — Record the Customers tab structure:
    - Customer table with columns
    - Search input
    - Tag filter
    - "Add Customer" button
    - Row count
7. `browser_take_screenshot` — filename: `qa-65-settings-customers-list.png`

8. **Count customers:**
    Expected customers from prior phases: Test Technician, Bob Wilson, Jane Doe, John Smith, AUDIT_Patricia Johnson, AUDIT_Marcus Rodriguez, AUDIT_Sarah Chen (7 total, though some may be archived).
    Record actual row count.

9. **Test search:**
    - `browser_snapshot` — Get ref for search input
    - Type "AUDIT" into the search input
    - `browser_wait_for` — Wait a moment for debounced search (300ms)
    - `browser_snapshot` — Verify:
      - Only AUDIT_ customers are shown (AUDIT_Patricia Johnson, AUDIT_Marcus Rodriguez, AUDIT_Sarah Chen)
      - Non-AUDIT customers are filtered out
    - Record the filtered row count
    - `browser_take_screenshot` — filename: `qa-65-settings-customers-search.png`

10. **Clear search:**
    - Clear the search input
    - `browser_wait_for` — Wait for debounce
    - `browser_snapshot` — Verify all customers return

11. **Test tag filter (if tags exist):**
    - `browser_snapshot` — Look for tag filter UI
    - If tag filter is visible with preset tags (VIP, repeat, etc.):
      - Click a tag to filter
      - Record results
      - Click again to deselect
    - If no tags are set on any customer, note: "Tag filter present but no tags assigned to customers — filter not exercisable"

12. Record SETT-07: PASS if customer list displays with working search. Note tag filter status. FAIL if list doesn't render or search doesn't work.

**Part B: SETT-08 — Add customer**

13. `browser_snapshot` — Find the "Add Customer" button
14. `browser_click` — Click "Add Customer"
15. `browser_wait_for` — Wait for the Add Customer sheet to open
16. `browser_snapshot` — Record the Add Customer form fields:
    - Name (required)
    - Email (required)
    - Phone (optional)
    - Notes (optional)
    - Tags (optional)
    - SMS consent fields

17. **Fill in customer details:**
    - Name: "AUDIT_Settings Test Customer"
    - Email: "audit-settings-test@example.com"
    - Phone: leave empty
    - Notes: "Created during Phase 65 settings audit"

18. `browser_take_screenshot` — filename: `qa-65-settings-customer-add-filled.png`

19. **Submit the form:** Click the save/create button.
    - `browser_wait_for` — Wait for success toast or sheet to close
    - `browser_snapshot` — Verify:
      - Sheet closed
      - New customer appears in the list

20. **DB verification — customer created:**
    ```sql
    SELECT id, name, email, status, notes
    FROM customers
    WHERE name = 'AUDIT_Settings Test Customer'
    AND business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
    ```
    Verify: row exists, status = 'active'. Record the customer ID.

21. `browser_take_screenshot` — filename: `qa-65-settings-customer-added.png`

**Part C: SETT-08 — Edit customer**

22. **Open customer for editing:**
    - `browser_snapshot` — Find the row or action for "AUDIT_Settings Test Customer"
    - Click on the row or the edit action to open the detail drawer/edit sheet

23. `browser_wait_for` — Wait for drawer/sheet to open
24. `browser_snapshot` — Record the edit form/drawer fields:
    - Name field showing "AUDIT_Settings Test Customer"
    - Email field showing "audit-settings-test@example.com"
    - Notes field

25. **Make an edit:** Change the notes.
    - If the detail drawer has an auto-save notes field: type "AUDIT - Notes edited during Phase 65" and wait for auto-save (500ms debounce)
    - If the edit sheet has a save button: change notes and click save
    - `browser_take_screenshot` — filename: `qa-65-settings-customer-edited.png`

26. **Close the drawer/sheet.**

27. **DB verification — edit saved:**
    ```sql
    SELECT notes
    FROM customers
    WHERE name = 'AUDIT_Settings Test Customer'
    AND business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
    ```
    Verify: notes updated.

**Part D: SETT-08 — Archive customer**

28. **Find archive action:**
    - `browser_snapshot` — Find the action button/menu for "AUDIT_Settings Test Customer"
    - Look for "Archive" button in the actions column or detail drawer
    - `browser_click` — Click the Archive button/action

29. A confirmation dialog may appear.
    - If dialog: accept it
    - `browser_wait_for` — Wait for success toast or status change
    - `browser_snapshot` — Verify:
      - Customer status changes to "Archived" or customer disappears from active list
    - `browser_take_screenshot` — filename: `qa-65-settings-customer-archived.png`

30. **DB verification — customer archived:**
    ```sql
    SELECT status
    FROM customers
    WHERE name = 'AUDIT_Settings Test Customer'
    AND business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
    ```
    Verify: status = 'archived'.

31. Record SETT-08: PASS if add, edit, and archive all worked correctly (DB verified). FAIL if any operation failed.

**Part E: Update findings document**

32. Update `docs/qa-v3.1/65-settings-services-customers.md` — fill in SETT-07 and SETT-08 sections:

```markdown

## SETT-07: Customers tab — search and filters

**Status:** [PASS/FAIL]

### Customer list
- Total customers displayed: [N]
- Expected: ~7 (Test Technician, Bob Wilson, Jane Doe, John Smith, AUDIT_Patricia, AUDIT_Marcus, AUDIT_Sarah)
- Table columns observed: [list columns]

### Search test
| Search term | Expected results | Actual results | Match? |
|-------------|-----------------|----------------|--------|
| "AUDIT" | 3 AUDIT_ customers | [actual] | [YES/NO] |

### Tag filter
- Tag filter UI present: [YES/NO]
- Tags available: [list or "none assigned"]
- Filter tested: [YES/NO, with results or reason skipped]

### Evidence
- `qa-65-settings-customers-list.png`
- `qa-65-settings-customers-search.png`

### Bugs found
- [none or list]

---

## SETT-08: Customer add/edit/archive

**Status:** [PASS/FAIL]

### Add customer
1. Clicked "Add Customer"
2. Sheet opened: [YES/NO]
3. Filled: Name="AUDIT_Settings Test Customer", Email="audit-settings-test@example.com"
4. Submitted: [success/error]
5. Customer appeared in list: [YES/NO]
6. DB verification: [row exists, status=active]

### Edit customer
1. Opened customer detail/edit
2. Changed notes to: "AUDIT - Notes edited during Phase 65"
3. Save method: [auto-save / manual save button]
4. DB verification: [notes updated]

### Archive customer
1. Clicked Archive action
2. Confirmation dialog: [appeared / not needed]
3. Customer status changed: [YES/NO]
4. DB verification: [status=archived]

### Evidence
- `qa-65-settings-customer-add-filled.png`
- `qa-65-settings-customer-added.png`
- `qa-65-settings-customer-edited.png`
- `qa-65-settings-customer-archived.png`

### Bugs found
- [none or list]

---

## Overall Assessment (Part 2)

[Summary paragraph covering:
- How many requirements passed out of SETT-05, SETT-06, SETT-07, SETT-08, SETT-09 (Services)
- Any bugs found
- All test data created: AUDIT_Settings Test Customer (archived)
- Confirmation that service types were restored to original state
- Readiness statement for Plan 65-03 (Billing page)]
```

Fill in ALL [placeholder] values. Update the Summary table at the top with final statuses.
  </action>
  <verify>
1. `docs/qa-v3.1/65-settings-services-customers.md` has all 5 requirement sections filled (SETT-05, SETT-06, SETT-07, SETT-08, SETT-09 Services)
2. Summary table updated with all statuses (no "pending" remaining)
3. Screenshots captured: customers-list, customers-search, customer-add-filled, customer-added, customer-edited, customer-archived
4. DB verifications for customer create, edit, and archive
5. Customer AUDIT_Settings Test Customer left in archived state (no further cleanup needed)
6. Overall Assessment section exists
7. No placeholder text remains
  </verify>
  <done>
- SETT-07 marked PASS or FAIL with customer list display, search, and tag filter status documented
- SETT-08 marked PASS or FAIL with add, edit, and archive all DB-verified
- Summary table fully updated for all Part 2 requirements
- Overall Assessment includes any bugs found and readiness for Plan 65-03
- AUDIT_Settings Test Customer created, edited, and archived (test lifecycle complete)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Read `docs/qa-v3.1/65-settings-services-customers.md` in full
2. Confirm the Summary table has SETT-05, SETT-06, SETT-07, SETT-08, SETT-09 (Services) all with PASS/FAIL status
3. Confirm no placeholder text remains
4. Confirm service type settings were restored to original state
5. Confirm AUDIT_Settings Test Customer exists in DB with status=archived
6. Verify screenshots were captured (at least 10: services-initial, hvac-on, services-saved, services-persisted, other-enabled, custom-names, customers-list, customers-search, customer-add-filled, customer-archived)
</verification>

<success_criteria>
- SETT-05 (Service type toggles) tested with on/off/timing and DB verification
- SETT-06 (Custom service names) tested with add/remove when Other enabled
- SETT-07 (Customers tab search/filters) tested with search query
- SETT-08 (Customer add/edit/archive) tested end-to-end with DB verification
- SETT-09 partial (Services tab persistence) verified after page refresh
- Service type settings restored to original state (no persistent mutations)
- Test customer created and archived (clean lifecycle)
</success_criteria>

<output>
After completion, create `.planning/phases/65-settings-billing/65-02-SUMMARY.md`
</output>
