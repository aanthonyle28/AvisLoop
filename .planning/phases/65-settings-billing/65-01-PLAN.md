---
phase: 65-settings-billing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/qa-v3.1/65-settings-general-templates.md]
autonomous: true

must_haves:
  truths:
    - "General tab loads with business name, Google review link, and sender name fields pre-populated from DB"
    - "Editing business name and sender name in General tab and clicking Save persists changes — verified by page refresh showing new values"
    - "FormLinkSection displays a /complete/[token] URL in a read-only input with a working Copy button (aria-label='Copy form URL')"
    - "If no form_token exists, clicking 'Generate form link' creates one and the URL appears — verified by DB query showing form_token IS NOT NULL on the business row"
    - "Templates tab displays templates grouped by channel with 'Email' and 'SMS' badges on each template item"
    - "Creating a new email template via the MessageTemplateForm succeeds and the new template appears in the template list immediately"
    - "Deleting the newly created template removes it from the list — verified by DB query showing the template row no longer exists"
    - "All General tab changes (business name, sender name) still display correctly after a full page reload (SETT-09 partial)"
    - "Findings document docs/qa-v3.1/65-settings-general-templates.md exists with PASS/FAIL per requirement for SETT-01, SETT-02, SETT-03, SETT-04, and partial SETT-09"
  artifacts:
    - path: "docs/qa-v3.1/65-settings-general-templates.md"
      provides: "Settings General + Templates tab QA findings with pass/fail, screenshots, DB verification"
      min_lines: 100
  key_links: []
---

<objective>
Execute the Phase 65 QA audit of the Settings page General tab (SETT-01, SETT-02) and Templates tab (SETT-03, SETT-04), plus persistence verification for those tabs (partial SETT-09).

Purpose: The General tab holds core business identity fields and the form_token URL that Phase 67 (public form audit) depends on. The Templates tab manages message templates used by campaigns. This audit verifies edit/save/persist flows and captures the form_token for downstream use.

Output: `docs/qa-v3.1/65-settings-general-templates.md` — findings document with PASS/FAIL per requirement, DB verification evidence, and screenshot references. Also records the form_token value for Phase 67.
</objective>

<execution_context>
@C:\Users\aanth\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aanth\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/PROJECT_STATE.md
@docs/DATA_MODEL.md

Test account: audit-test@avisloop.com / AuditTest123!
App URL: http://localhost:3000
Output file: docs/qa-v3.1/65-settings-general-templates.md
Business name: "Audit Test HVAC" (id: 6ed94b54-6f35-4ede-8dcb-28f562052042)

**Settings page structure:**
- URL: `/settings`
- Component: `SettingsTabs` with 7 tabs: General, Templates, Services, Messaging, Integrations, Customers, Account
- General tab contains: `BusinessSettingsForm` (business name, google_review_link, sender name, default template select, Save Settings button) + `FormLinkSection` (form token URL with copy/regenerate)
- Templates tab contains: `TemplateList` (grouped by email/SMS with channel badges) + `MessageTemplateForm` (create new with Email/SMS channel tabs)

**BusinessSettingsForm fields:**
- Business Name (input, required, id="name")
- Google Review Link (input, type=url, id="googleReviewLink")
- Default Sender Name (input, id="defaultSenderName")
- Default Email Template (select, id="defaultTemplateId")
- Save Settings button

**FormLinkSection states:**
- If form_token exists: read-only Input with URL `{origin}/complete/{token}` + Copy button (aria-label="Copy form URL") + Regenerate link button
- If no form_token: "Generate form link" button

**TemplateListItem structure:**
- Channel badge: "Email" (blue) or "SMS" (green) with icon
- Template name
- "System Template" badge if is_default=true
- System templates: "Use this template" button (copies to user templates)
- User templates: "Delete" button
- Expandable body preview ("View message" / "Hide message")

**MessageTemplateForm structure:**
- Channel tabs: Email / SMS
- Email: name (required), subject (required), body (required textarea)
- SMS: name (required), body (required textarea with character counter)
- "Create Template" button

**IMPORTANT:** Record the form_token value from SETT-02 test. This is needed by Phase 67 (public form audit). If form_token already exists on the business, record it. If not, generate one.
</context>

<tasks>

<task type="auto">
  <name>Task 1: General tab audit — business profile edit/save, form link, persistence</name>
  <files>docs/qa-v3.1/65-settings-general-templates.md</files>
  <action>
This task tests SETT-01 (General tab fields edit/save), SETT-02 (form link section), and SETT-09 (persistence after refresh) for the General tab.

**IMPORTANT:** The dev server must be running (`pnpm dev`). Before starting Playwright steps, verify with `browser_navigate` to `http://localhost:3000`. If the server is not running, start it in the background first.

**Step-by-step Playwright MCP walkthrough:**

**Part A: Prerequisite — verify test business exists in DB**

1. Run DB query to confirm test business state:
    ```sql
    SELECT id, name, google_review_link, default_sender_name, form_token, tier
    FROM businesses
    WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
    ```
    Record: current business name, google_review_link, default_sender_name, form_token (may be NULL or existing).

**Part B: Login and navigate to Settings**

2. `browser_navigate` to `http://localhost:3000/auth/login`
3. `browser_snapshot` — Get refs for login form
4. `browser_fill_form` — Email: "audit-test@avisloop.com", Password: "AuditTest123!"
5. `browser_click` — Click "Sign in" / "Login" button
6. `browser_wait_for` — Wait for dashboard to load (max 10 seconds)
7. `browser_navigate` to `http://localhost:3000/settings`
8. `browser_wait_for` — Wait for "Settings" heading to appear
9. `browser_snapshot` — Record full page structure. Verify "General" tab is the default active tab.
10. `browser_take_screenshot` — filename: `qa-65-settings-general-initial.png`

**Part C: SETT-01 — Verify fields are pre-populated and editable**

11. From the snapshot, verify the General tab shows:
    - "Business Profile" heading
    - Business Name input (id="name") pre-populated with current business name
    - Google Review Link input (id="googleReviewLink") pre-populated (or empty if not set)
    - Default Sender Name input (id="defaultSenderName") pre-populated (or empty if not set)
    - Default Email Template select (id="defaultTemplateId")
    - "Save Settings" button
    Record the current values of all fields.

12. **Edit Business Name:** Clear the Business Name field and type a test value.
    - `browser_click` on the Business Name input
    - Triple-click or select-all then type: "AUDIT_Test HVAC Renamed"
    - Record that the field now shows "AUDIT_Test HVAC Renamed"

13. **Edit Sender Name:** Clear the Default Sender Name field and type a test value.
    - `browser_click` on the Default Sender Name input
    - Clear and type: "AUDIT Test Sender"

14. `browser_take_screenshot` — filename: `qa-65-settings-general-edited.png`

15. **Save settings:** Click the "Save Settings" button.
    - `browser_click` — Click "Save Settings"
    - `browser_wait_for` — Wait for success message ("Settings saved successfully!" or toast)
    - `browser_snapshot` — Verify success message appears
    - `browser_take_screenshot` — filename: `qa-65-settings-general-saved.png`

16. **DB verification — settings saved:**
    ```sql
    SELECT name, default_sender_name
    FROM businesses
    WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
    ```
    Verify: name = 'AUDIT_Test HVAC Renamed', default_sender_name = 'AUDIT Test Sender'.

17. **SETT-09 partial — Persistence after refresh:**
    - `browser_navigate` to `http://localhost:3000/settings` (full page reload)
    - `browser_wait_for` — Wait for Settings heading
    - `browser_snapshot` — Verify:
      - Business Name shows "AUDIT_Test HVAC Renamed"
      - Default Sender Name shows "AUDIT Test Sender"
    - `browser_take_screenshot` — filename: `qa-65-settings-general-persisted.png`

18. **Restore original business name:**
    - Clear Business Name and type back the original name: "Audit Test HVAC"
    - Clear Default Sender Name and type back original value (from step 1 DB query, or leave as "AUDIT Test Sender" if it was empty)
    - Click "Save Settings"
    - `browser_wait_for` — Wait for success message
    - Verify in DB:
    ```sql
    SELECT name, default_sender_name
    FROM businesses
    WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
    ```

19. Record SETT-01: PASS if all fields were pre-populated, editable, and saved correctly (DB verified). FAIL if any field was missing, not editable, or save failed.

**Part D: SETT-02 — Form link section**

20. `browser_navigate` to `http://localhost:3000/settings` (ensure fresh state)
21. `browser_wait_for` — Wait for Settings to load
22. `browser_snapshot` — Look for the "Job Completion Form" section below Business Profile in the General tab.

23. **Check form_token state:**
    - If the FormLinkSection shows a URL in a read-only input (form_token exists):
      - Record the URL value (format: `http://localhost:3000/complete/{token}`)
      - Extract the token from the URL
      - Verify the Copy button is visible (aria-label="Copy form URL")
      - Click the Copy button
      - `browser_snapshot` — Verify the copy icon changes to a check (success feedback)
      - `browser_take_screenshot` — filename: `qa-65-settings-form-link-exists.png`
      - Record the form_token value for Phase 67

    - If the FormLinkSection shows a "Generate form link" button (no form_token):
      - Click "Generate form link"
      - `browser_wait_for` — Wait for success toast ("Form link generated")
      - `browser_snapshot` — Verify:
        - URL now displayed in read-only input
        - Copy button is visible
        - Regenerate link button is visible
      - `browser_take_screenshot` — filename: `qa-65-settings-form-link-generated.png`
      - Record the form_token value for Phase 67

24. **DB verification — form_token exists:**
    ```sql
    SELECT form_token
    FROM businesses
    WHERE id = '6ed94b54-6f35-4ede-8dcb-28f562052042';
    ```
    Verify: form_token IS NOT NULL. Record the exact token value.

25. Record SETT-02: PASS if form link section displays URL with copy button (or generates successfully). FAIL if section is missing or generation fails.

**Part E: Initialize findings document**

26. Create `docs/qa-v3.1/65-settings-general-templates.md` with findings from this task:

```markdown
# Phase 65: Settings & Billing — QA Findings (Part 1: General + Templates)

**Tested:** [current date]
**Tester:** Claude (Playwright MCP + Supabase MCP)
**App URL:** http://localhost:3000
**Test account:** audit-test@avisloop.com
**Business:** Audit Test HVAC (6ed94b54-6f35-4ede-8dcb-28f562052042)

---

## Summary

| Requirement | Status | Notes |
|-------------|--------|-------|
| SETT-01: General tab fields edit/save | [PASS/FAIL] | [notes] |
| SETT-02: Form link with copy button | [PASS/FAIL] | [notes] |
| SETT-03: Templates with channel badges | [pending] | |
| SETT-04: Template create/edit/delete | [pending] | |
| SETT-09: Persistence after refresh (General) | [PASS/FAIL] | [notes] |

---

## SETT-01: General tab — fields edit and save

**Status:** [PASS/FAIL]

### Fields observed (pre-populated)
| Field | Expected | Actual |
|-------|----------|--------|
| Business Name | Audit Test HVAC | [actual] |
| Google Review Link | [from DB] | [actual] |
| Default Sender Name | [from DB] | [actual] |
| Default Email Template | [from DB] | [actual] |

### Edit and save test
1. Changed Business Name to: "AUDIT_Test HVAC Renamed"
2. Changed Default Sender Name to: "AUDIT Test Sender"
3. Clicked Save Settings
4. Success message: [text observed]
5. DB verification: [PASS/FAIL with query result]

### Persistence after refresh (SETT-09)
- Refreshed page after save
- Business Name shows: [value]
- Default Sender Name shows: [value]
- Result: [PASS/FAIL]

### Restored original values
- Business Name restored to: "Audit Test HVAC"
- DB verified: [YES/NO]

### Evidence
- `qa-65-settings-general-initial.png`
- `qa-65-settings-general-edited.png`
- `qa-65-settings-general-saved.png`
- `qa-65-settings-general-persisted.png`

### Bugs found
- [none or list]

---

## SETT-02: Form link section

**Status:** [PASS/FAIL]

### Form token state
- Initial state: [token exists / no token]
- Action taken: [copied existing / generated new]

### Form URL observed
- URL: [full URL]
- Token: [token value] ← **Phase 67 dependency**

### Copy button test
- Copy button visible: [YES/NO]
- aria-label="Copy form URL" present: [YES/NO]
- Copy click feedback (icon change): [YES/NO]

### DB verification
```
[paste SQL result showing form_token value]
```

### Evidence
- `qa-65-settings-form-link-exists.png` or `qa-65-settings-form-link-generated.png`

### Bugs found
- [none or list]

---
```

Fill in ALL [placeholder] values with actual observed results. Leave SETT-03 and SETT-04 as stubs (filled by Task 2).
  </action>
  <verify>
1. File `docs/qa-v3.1/65-settings-general-templates.md` exists with SETT-01 and SETT-02 sections filled in
2. Screenshots captured: qa-65-settings-general-initial.png, qa-65-settings-general-edited.png, qa-65-settings-general-saved.png, qa-65-settings-general-persisted.png, and form link screenshot
3. DB verification confirms edit was saved and then restored
4. form_token value is recorded in the findings document for Phase 67
5. SETT-09 (persistence) verified for General tab fields
  </verify>
  <done>
- SETT-01 marked PASS or FAIL with all 4 fields documented and edit/save DB-verified
- SETT-02 marked PASS or FAIL with form_token URL and copy button verified
- SETT-09 partial marked PASS or FAIL for General tab persistence
- form_token value explicitly recorded for Phase 67 dependency
- Business name restored to original "Audit Test HVAC"
- Findings document initialized with Task 1 sections filled
  </done>
</task>

<task type="auto">
  <name>Task 2: Templates tab audit — list display, channel badges, create template, delete template</name>
  <files>docs/qa-v3.1/65-settings-general-templates.md</files>
  <action>
This task tests SETT-03 (template list with channel badges) and SETT-04 (create/edit/delete template). Must run AFTER Task 1 because it depends on being logged in and the findings document existing.

**IMPORTANT:** If still logged in from Task 1, proceed directly. If session expired, re-login with audit-test@avisloop.com / AuditTest123!

**Step-by-step Playwright MCP walkthrough:**

**Part A: SETT-03 — Template list with channel badges**

1. `browser_navigate` to `http://localhost:3000/settings`
2. `browser_wait_for` — Wait for Settings heading
3. `browser_snapshot` — Find the "Templates" tab trigger and click it.
4. `browser_click` — Click the "Templates" tab
5. `browser_wait_for` — Wait for templates content to load
6. `browser_snapshot` — Record the templates tab content:
    - "Message Templates" heading
    - Variable placeholder instructions text
    - "EMAIL TEMPLATES (N)" section heading with count
    - "SMS TEMPLATES (N)" section heading with count
    - Individual template items with:
      - Channel badge ("Email" in blue or "SMS" in green)
      - Template name
      - "System Template" badge (if is_default)
      - Action buttons: "Use this template" (system) or "Delete" (user)
    - "Create New Template" section below

7. `browser_take_screenshot` — filename: `qa-65-settings-templates-list.png`

8. **Count templates by channel:**
    - Count email templates visible
    - Count SMS templates visible
    - Verify each template item has a channel badge (Email or SMS)
    - Note which templates are system templates vs user templates

9. **DB verification — template count:**
    ```sql
    SELECT channel, COUNT(*) as count,
           SUM(CASE WHEN is_default THEN 1 ELSE 0 END) as system_count
    FROM message_templates
    WHERE business_id = '6ed94b54-6f35-4ede-8dcb-28f562052042'
       OR is_default = true
    GROUP BY channel
    ORDER BY channel;
    ```
    Compare UI count with DB count.

10. **Verify channel badge styling:**
    - Email badges: blue/info color with envelope icon
    - SMS badges: green/success color with chat icon
    - Record whether badges are visually distinct

11. Record SETT-03: PASS if template list displays with correct channel badges, grouped by email/SMS, and counts match DB. FAIL if badges missing, grouping wrong, or count mismatch.

**Part B: SETT-04 — Create a new template**

12. Scroll down to the "Create New Template" section (below the template list).
    - `browser_snapshot` — Verify the MessageTemplateForm is visible with:
      - "Create Message Template" card heading
      - Email/SMS channel tabs
      - Form fields

13. **Create a new email template:**
    - Ensure the "Email" tab is active (should be default)
    - `browser_snapshot` — Get refs for form fields
    - Fill in:
      - Template Name: "AUDIT_Test Email Template"
      - Subject Line: "AUDIT How was your experience with {{BUSINESS_NAME}}?"
      - Email Body: "Hi {{CUSTOMER_NAME}},\n\nThank you for choosing {{BUSINESS_NAME}}! We'd love to hear about your experience.\n\nLeave a review: {{REVIEW_LINK}}\n\nThanks,\n{{SENDER_NAME}}"
    - `browser_take_screenshot` — filename: `qa-65-settings-template-create-filled.png`

14. Click "Create Template" button.
    - `browser_wait_for` — Wait for success toast ("Template created" or similar) or success state
    - `browser_snapshot` — Verify:
      - Success feedback (toast or inline message)
      - The new template appears in the email templates list above the form
    - `browser_take_screenshot` — filename: `qa-65-settings-template-created.png`

15. **DB verification — template created:**
    ```sql
    SELECT id, name, channel, subject, is_default, created_at
    FROM message_templates
    WHERE name = 'AUDIT_Test Email Template'
    ORDER BY created_at DESC
    LIMIT 1;
    ```
    Verify: row exists, channel='email', is_default=false. Record the template ID.

16. **Verify new template in list:**
    - Scroll up to the email templates section
    - `browser_snapshot` — Verify "AUDIT_Test Email Template" appears in the list with:
      - Email badge (blue)
      - Template name
      - NO "System Template" badge
      - "Delete" button (not "Use this template" — it's a user template)

**Part C: SETT-04 — Delete the test template**

17. Find the "AUDIT_Test Email Template" in the template list.
    - `browser_snapshot` — Get the ref for the Delete button on the AUDIT template
    - Click the "Delete" button

18. A confirmation dialog should appear (window.confirm: "Are you sure you want to delete this template?").
    - `browser_handle_dialog` — Accept the dialog
    - `browser_wait_for` — Wait for success toast ("Template deleted")
    - `browser_snapshot` — Verify:
      - "AUDIT_Test Email Template" is NO LONGER in the template list
    - `browser_take_screenshot` — filename: `qa-65-settings-template-deleted.png`

19. **DB verification — template deleted:**
    ```sql
    SELECT id, name
    FROM message_templates
    WHERE name = 'AUDIT_Test Email Template';
    ```
    Verify: no rows returned (template deleted from DB).

20. Record SETT-04: PASS if template was created successfully, appeared in list, then deleted successfully (DB verified both creation and deletion). FAIL if create or delete failed.

**Part D: Update findings document**

21. Update `docs/qa-v3.1/65-settings-general-templates.md` — fill in SETT-03 and SETT-04 sections:

```markdown

## SETT-03: Templates tab — list with channel badges

**Status:** [PASS/FAIL]

### Template list observed
| Section | Count (UI) | Count (DB) | Match? |
|---------|-----------|-----------|--------|
| Email Templates | [N] | [N] | [YES/NO] |
| SMS Templates | [N] | [N] | [YES/NO] |

### Channel badge styling
| Badge | Color | Icon | Distinct? |
|-------|-------|------|-----------|
| Email | [color] | [envelope/other] | [YES/NO] |
| SMS | [color] | [chat/other] | [YES/NO] |

### System vs User templates
- System templates identified by "System Template" badge: [YES/NO]
- System templates have "Use this template" button: [YES/NO]
- User templates have "Delete" button: [YES/NO]

### Evidence
- `qa-65-settings-templates-list.png`

### Bugs found
- [none or list]

---

## SETT-04: Template create/edit/delete

**Status:** [PASS/FAIL]

### Create template test
1. Channel: Email
2. Name: "AUDIT_Test Email Template"
3. Subject: "AUDIT How was your experience with {{BUSINESS_NAME}}?"
4. Body: [filled]
5. Submitted: [success/error]
6. Appeared in template list: [YES/NO]
7. DB verification: [row found with correct channel and name]

### Delete template test
1. Clicked Delete on "AUDIT_Test Email Template"
2. Confirmation dialog appeared: [YES/NO]
3. Accepted dialog
4. Template removed from list: [YES/NO]
5. DB verification: [row no longer exists]

### Evidence
- `qa-65-settings-template-create-filled.png`
- `qa-65-settings-template-created.png`
- `qa-65-settings-template-deleted.png`

### Bugs found
- [none or list]

---

## Overall Assessment (Part 1)

[Summary paragraph covering:
- How many requirements passed out of SETT-01, SETT-02, SETT-03, SETT-04, SETT-09 (General)
- form_token value for Phase 67: [token]
- Any bugs found
- Readiness statement for Plan 65-02 (Services + Customers tabs)]
```

Fill in ALL [placeholder] values. Update the Summary table at the top with final statuses for SETT-01 through SETT-04 and SETT-09 (General). Ensure no placeholder text remains.
  </action>
  <verify>
1. `docs/qa-v3.1/65-settings-general-templates.md` has SETT-01, SETT-02, SETT-03, SETT-04, and SETT-09 (General) sections all filled in
2. Summary table updated with all statuses (no "pending" remaining for these 5 items)
3. Screenshots captured: qa-65-settings-templates-list.png, qa-65-settings-template-create-filled.png, qa-65-settings-template-created.png, qa-65-settings-template-deleted.png
4. DB verification for template creation and deletion
5. Template count matches between UI and DB
6. AUDIT_Test Email Template was fully cleaned up (created then deleted)
7. Overall Assessment section includes form_token value for Phase 67
8. No placeholder text remains in the document
  </verify>
  <done>
- SETT-03 marked PASS or FAIL with template counts, channel badge styling, and system vs user template distinction documented
- SETT-04 marked PASS or FAIL with create and delete both DB-verified
- Summary table fully updated for all Part 1 requirements
- Overall Assessment includes form_token value for Phase 67
- Test template AUDIT_Test Email Template was created and then deleted (no test data left behind)
- Findings document complete for Part 1 with no remaining placeholders
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Read `docs/qa-v3.1/65-settings-general-templates.md` in full
2. Confirm the Summary table has SETT-01, SETT-02, SETT-03, SETT-04, SETT-09 (General) all with PASS/FAIL status
3. Confirm form_token value is explicitly recorded for Phase 67 in the Overall Assessment
4. Confirm no placeholder text remains ([brackets])
5. Confirm business name was restored to "Audit Test HVAC" (not left as test value)
6. Confirm AUDIT_Test Email Template was deleted (no orphan test data)
7. Verify screenshots were captured (at least 8: general-initial, general-edited, general-saved, general-persisted, form-link, templates-list, template-created, template-deleted)
</verification>

<success_criteria>
- SETT-01 (General tab fields edit/save) tested with DB verification
- SETT-02 (Form link with copy button) tested, form_token recorded for Phase 67
- SETT-03 (Template list with channel badges) tested with count verification
- SETT-04 (Template create/delete) tested with DB verification
- SETT-09 partial (General tab persistence) verified after page refresh
- form_token value captured and documented for Phase 67 dependency
- Business name restored to original (no persistent test mutations)
- All test templates cleaned up
</success_criteria>

<output>
After completion, create `.planning/phases/65-settings-billing/65-01-SUMMARY.md`
</output>
