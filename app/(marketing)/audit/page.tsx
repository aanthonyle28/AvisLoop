import { notFound } from 'next/navigation'

/*
 * Public audit landing page — /audit
 *
 * DISABLED: Feature not ready for production.
 * To re-enable, restore from git: git checkout HEAD~N -- app/(marketing)/audit/page.tsx
 * Original commit: b1b9013
 */

export default function AuditPage() {
  notFound()
}
